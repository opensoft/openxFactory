# gate-action record — a HUMAN-gate console action audit entry
# (gate-action-record.schema.yaml). A session verb is reachable only from the
# human console: a caller that cannot show it originates there is rejected and
# reported before any record is written. That is a console-PRESENCE control,
# not authentication (design D23) — `provenance`, where present, names the
# door this action came through and how presence was shown.
schema_version: 1
kind: gate-action-record
actor: Brett Heap (agent-driven T092 re-run)
action: create-document
target:
  document: ideation/staging/workbench-branch-sessions/t092-re-run-notebook-half-acceptance-evidence.md
  ref: draft/workbench-branch-sessions
provenance:
  surface: http
  console_presence: console-token
at: '2026-08-05T01:28:42Z'
artifacts:
- kind: document
  reference: ideation/staging/workbench-branch-sessions/t092-re-run-notebook-half-acceptance-evidence.md
- kind: commit
  reference: 20260805T012842Z
notes: 'staging workbench scope: staged workbench-branch-sessions'
