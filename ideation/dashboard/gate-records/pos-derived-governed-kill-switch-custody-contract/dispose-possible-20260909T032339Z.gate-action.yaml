# gate-action record — a HUMAN-gate console action audit entry
# (gate-action-record.schema.yaml). A session verb is reachable only from the
# human console: a caller that cannot show it originates there is rejected and
# reported before any record is written. That is a console-PRESENCE control,
# not authentication (design D23) — `provenance`, where present, names the
# door this action came through and how presence was shown.
schema_version: 1
kind: gate-action-record
actor: brett
action: dispose-possible
target:
  possible_id: pos-derived-governed-kill-switch-custody-contract
  outcome: deferred
provenance:
  surface: intent-plane
  console_presence: ingress-auth
at: '2026-09-09T03:23:39Z'
artifacts:
- kind: register-update
  reference: ideation/cross-reference.yaml#possibles_register
notes: 'Deferred for the live intent-plane dispatch exercise (openxFactory #656 D-2
  / split-opendox-two-layer-product 2.5) - a reversible disposition, not a substantive
  judgment on this possible; a real reviewer should decide it normally afterward.'
