# enable-live-openworkflow-factory

Implement the next runtime phase for the AI software development factory.

This change starts from the completed repo reorganization and content migration.
It uses `openWorkflow` as the canonical policy/contract source and turns the
existing Hermes/Omnigent proof surface into a repeatable live pilot path.
