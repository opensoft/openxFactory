schema: spec-driven
created: 2026-09-01
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:2026-09-01-retire-doxbench-chat-turn-v1
  reason: >-
    openxFactory issue #522 — "three contract-v2.0-targeted deprecations
    outlived their target" — measured 2026-08-31 against main tip `4290cad2`,
    with the memo durable on the operator host at
    `~/projects/xFactory/deprecations-522-memo-2026-08-31.md`. There is no
    staged topic: the subject arrived as a filed issue and was dispositioned by
    ruling, so `kind: staged` would claim a staging source that does not exist.

    THIS PACKET IS ENTRY 3 ALONE, AND THE SEPARATION IS THE RULING'S. Entries 1
    and 2 are the named sibling
    `retire-hermes-flat-keys-and-openworkflow-tokens`. The memo recommended
    entry 3 be "executed at contract-v3.0, but as its own Speckit slice, not
    folded into a combined cut", and Brett's ruling adopted that: this is the
    only one of the three that moves a DIGEST-PINNED SCHEMA's bytes, an
    inventory member's digest, twelve packaged fixtures, a committed
    byte-identity baseline test, and a server posture that must be REDESIGNED
    rather than deleted.

    IT IS ALSO THE ONLY ONE OF THE THREE WHOSE WARNING ACTUALLY FIRES. A
    default run of `scripts/validate-ideation-dashboard-contracts.py` over a
    clean checkout reports four warnings, every one naming a removal target the
    bundle passed one major and five minors ago.
  approved_by: >-
    BRETT HEAP, repository owner, 2026-09-01 in session, ruling on openxFactory
    issue #522 and recorded as a comment there: "execute all three retirements
    at contract-v3.0, as the memo recommends", entry 3 being "EXECUTE, its own
    slice", with the `serve.py` v1-fallback posture "redesigned, not deleted".
    The ruling authorizes the PROPOSAL; it does not ratify this text, and the
    packet is `Status: draft` accordingly.
  approved_on: 2026-09-01
related:
  - openxFactory:change:retire-hermes-flat-keys-and-openworkflow-tokens  # SIBLING, authored the same day from the same ruling. Entries 1 and 2. Lands at the SAME contract-v3.0 major and is deliberately a separate packet; neither is a precondition of the other, and this packet's delta touches only `ideation-dashboard` so it does not depend on the capability that one adds
  - openxFactory:change:add-doxbench-editing-phase-b   # ARCHIVED. The widening release, contract-v1.34: it introduced the co-resident `-v2` family, wrote the machine-readable `deprecated_envelopes` block, and recorded the removal target this packet executes
  - openxFactory:change:add-doxchat-model-intake       # ACTIVE. Touches the same chat rail and the same schema file; named so a cut ordering the two is a decision rather than a surprise
