schema: spec-driven
created: 2026-07-25
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:staging-workbench
  reason: >-
    Brett's Track B ruling 2026-07-25, made live against the running wheel in
    the same session that produced the expanded-tile action row and
    add-wheel-action-verbs: the staging area is where the real proposal gets
    developed, and its READ-ONLY foundation — a scoped per-tile workbench over
    the tile's documents, its interconnectedness, and its outline, plus a
    per-document completeness signal to see at a glance how far each doc has
    come — lands first, before the AI chat layer (Track C). The lineage is the
    ideation-action-plane staged topic (whose exits were
    add-ideation-intent-plane, then add-propose-verb, then
    add-wheel-action-verbs), but no staged fragment describes a workbench view
    or a completeness score, so this enters as an approved ad-hoc origin rather
    than a backdated staging claim.
  approved_by: Brett (openxFactory operator authority)
  approved_on: 2026-07-25
