# Operator Rulings — S5 Packets 1, 2 and 4 (R1–R12), recorded 2026-08-29

Ruled by Brett Heap (openxFactory operator authority) in-session on
2026-08-29, against the merged [S5 model-version governance research
report](research/s5-model-version-governance-research-report.md).
Each ruling settles the DIRECTION of one decision the report's packets asked
for; the delta or act named beneath it is the follow-up work that makes the
ruling enforceable. Until that delta or act lands, current enforcement
behavior is unchanged. Nothing here ticks a task, discharges an S5 gate,
authorizes implementation, or amends a contract.

## Decision-owner honesty note (read first)

The report separates owners deliberately, and this record does not collapse
them. The Gate-Rules COUNCIL selects a seat's model on duty-specific soak
evidence; the CONVENER accepts or rejects that output on record; the
roster-change LEAD accepts application to the enrolled roster through
`roster_change: lead_accepted_recorded`; the OPERATOR independently selects
and evidences exact provider identity; and a HUMAN RATIFIER issues or
re-issues by explicit register act (report §2 "Decision Requested", §3
"Identity Model", §5 "Lifecycle Roles").

The rulings below are Brett's acts as convener, operator and ratifier, and
they are the TARGET the Council is asked to confirm — they are not the
Council's selection act. Unless Brett explicitly waives them, two acts remain
OWED before issuance: the Council's selection act carrying the duty-specific
soak evidence §2 requires, and the Lead's `lead_accepted_recorded` roster
acceptance. A ruling that names a target does not perform the act that
selects it.

## Packet 1 — Council seat-to-model composition (R1–R4)

These four rulings answer report §2 "Decision Requested" and §2A "Diversity
Options And The Named Council Question", including the named question that
annex declines to answer: whether the recorded correlation argument, made
about one MODEL, reaches a one-VENDOR bench.

1. **R1 — Composition: OPTION D, all-Anthropic 2:2.** The target is the
   minimal-change alias-to-exact flip, not a different bench. Cross-provider
   diversity is NOT required: the binding constraint is the Gate-Rules
   Council's 2026-08-22 Q3 disposition — at least two distinct models across
   the three enrolled-surface seats — extended 2026-08-26 to a 2:2 split
   across all four seats, and Option D satisfies both. The named Council
   question is answered in the negative: the correlation argument does not
   require a second vendor. Option D's price is accepted knowingly as the
   report states it (forfeited cross-provider diversity, concentrated
   correlated failure), not overlooked.

2. **R2 — `lead-quality` → `claude-sonnet-5`.** Recorded review trigger: the
   measured substantive-return degeneracy travels with any sonnet-class pin,
   so it is watched on this seat rather than assumed retired by the flip
   (report §2A "Recommended Picks").

3. **R3 — `lead-security` → `claude-opus-5`; `lead-integration` →
   `claude-opus-5`.** Both on the register's longest published retirement
   floor for the surveyed snapshots, and both preserving the ruled 2:2 rather
   than minting a third distinct model (report §2A "Candidate Evidence
   Register", "Recommended Picks").

4. **R4 — `company-policy-lead`: the TENANT declares `claude-sonnet-5`.**
   This is a tenant act taken in the parallel tenant lane, mirroring the ruled
   2:2. The domain profile is a consumer bound to the tenant's declaration and
   claims no authority to choose the tenant representative; recording the
   spelling here is the tenant's own declaration, not the domain lane
   selecting on the tenant's behalf (report §2A "Recommended Picks", last
   row).

**What makes R1–R4 enforceable:** the Gate-Rules Council's selection record
(with the soak evidence §2 requires, its diversity finding, and the
convener's recorded acceptance), followed by the roster-change Lead's
`lead_accepted_recorded` acceptance applying it to the enrolled roster. Until
both land, the enrolled roster keeps the mutable `opus` / `sonnet` selectors
and task 7.5's model half stays UNTICKED. The Council must also rule
explicitly, in the same act, whether an alias-to-exact flip re-opens the soak
rather than letting that be settled by default (report §2A "Binding Diversity
Constraint").

## Packet 2 — Operator plane and execution identity (R5)

5. **R5 — Plane: DIRECT ANTHROPIC via Claude Code.** That is today's
   transport, and the report's workflow evidence resolves it rather than
   leaving the platform half of the pin undetermined: the
   council-deliberation worker passes its `SEAT_MODELS` selectors to Claude
   Code. The Operator record MUST assert that Anthropic's opt-in server-side
   `fallbacks` beta is UNSET — it routes a refused request to a different
   model and is a deliberate substitution mechanism, so its absence is
   asserted, never assumed — and MUST capture the Claude Code version, the
   configured provider surface, and the credential-binding reference. The
   partner-plane hazards the report records (Bedrock's own lifecycle dates,
   Foundry's `versionUpgradeOption` and the 12-month partner-model
   lifecycle, Vertex's provisional and re-verification-required rows) are
   INAPPLICABLE unless and until the plane changes; a plane change makes them
   live again and requires a fresh Operator record (report §1 "Governing
   Baseline", §3 "Identity Model", "Proposed S5 Evidence Fields By Provider",
   "Provider Mutation, Lifecycle, And Platform Facts").

**What makes R5 enforceable:** the Operator identity record itself — the
Packet 2 record carrying semantic identity, execution identity, ratification-
time and execution-time evidence, and the Operator's signature. A plane
ruling is not an identity record; until that record exists, governed issuance
and activation stay blocked exactly as report §7 states.

## Packet 3 — convener admissible-pin ruling: ALREADY RULED 2026-08-28

Nothing is pending on this packet and nothing here reopens it. The question —
whether a family pin carrying an attested resolved-version record is
admissible — was ruled on 2026-08-28 by ratification of openXwallet PR #3
(`add-composition-drift-cascade`) as authored: the delta's exact-version rule
stands, a declared model component names an exact version, a family pin is a
validation failure, and admitting a family pin would now be a NEW CHANGE act
rather than a pending convener decision. That outcome is recorded in
[`tasks.md`](tasks.md)'s dated 2026-08-29 RESOLVED addendum under task 8.1
and mirrored in report §4 "Ruling Of Record". It is cross-referenced here so
that a reader of this file does not mistake Packet 3 for an open packet.

## Packet 4 — canonical composition and re-issuance (R6–R12)

These rulings answer report §5 "Proposed Canonicalization And Attestation
Design", "Lifecycle Roles", and "In-Flight Behavior And Re-Issuance". They are
ratification TARGETS: none of them is implementable until the change that
carries them is authored and ratified.

6. **R6 — `model_version` digests the PROVIDER PLANE plus the exact published
   identifier.** This closes the residual §5 narrowed from D5: the plane sits
   INSIDE the digest, because the same immutable model does not carry the same
   string across planes and a pin is a pair. Execution and deployment identity
   (account, region, deployment, endpoint, authentication) stays OUTSIDE
   composition and is carried by REFERENCE to the Packet 2 evidence record,
   keeping the DRIFT blast radius where the report's default puts it. Effort,
   reasoning and thinking settings are generation parameters and belong under
   the Parameters component, not under `model_version`.

7. **R7 — Canonical serialization: JSON Canonicalization Scheme (RFC 8785).**
   The digest is `sha256:<lowercase-hex>`, recorded together with the
   canonicalization profile name and its version. This selects the named
   profile report §5 left open between JCS, canonical CBOR and another named
   profile; digest agility and any dual-digest transition remain open and
   belong to the carrying change.

8. **R8 — Re-issuance record grammar: the five fields are the MINIMUM.** A
   re-issuance act records the superseding grant reference, the superseded
   grant reference, the composition hash issued against, the ratifying human,
   and the effective time. The S5 implementer proposed these fields; this
   ruling adopts them as a floor a carrying change may extend, and NOT as a
   ceiling. It does not make them an already-required record format before
   that change is ratified.

9. **R9 — In-flight: a revoked holder PARKS the convening with a named
   refusal.** No grandfathering. An earlier admission stamp is not honored by
   an exercise that detects a composition mismatch. A parked convening resumes
   only under a new human-ratified issuance act — never by the runtime, never
   by elapsed time. This is the direction task 7.7's gate text already
   describes, ruled so the runbook is written against a settled target.

10. **R10 — Retrieval-corpus drift invalidates on IDENTITY or GOVERNING
    CONFIG, not on rows.** A change to corpus identity, or to the governing
    configuration digest (scope, selection configuration, admission policy,
    ontology package digest), invalidates the composition. A row-level content
    change inside the same governed corpus does not, by itself, invalidate.
    The binding remains to the pinned seat prompt and the pinned ontology
    package, and never to the candidate repository at HEAD.

11. **R11 — Lifecycle roles table adopted as proposed** (report §5 "Lifecycle
    Roles"): `propose → approve → attest → register → activate → revoke`, with
    REGISTER as the ONLY human-ratified act; REVOKE requires no human, because
    the cascade is fail-closed and a fail-closed cascade must not wait on a
    human; and RE-ISSUE always requires one. This is consistent with the
    2026-08-26 Q8(a) ruling that re-issuance is always an explicit register
    act.

12. **R12 — Attestation-envelope SIGNER: DEFERRED to the
    signed-execution-chain change** (staged; PR #452). Composition digests MAY
    be recorded UNSIGNED in the register in the meantime, and activation stays
    fail-closed on digest match, so the deferral narrows what is claimed
    rather than opening a window. The per-seat Ed25519 keys minted 2026-08-28
    are NOT to be used for self-attestation: a holder signing its own
    composition attests nothing an independent party can rely on. Report §5's
    remaining envelope questions — standard, key distribution, signature
    algorithm, evidence-retention location — travel with that change.

**What makes R6–R12 enforceable:** a FRESH OpenSpec change, authored after
issuance, carrying R6–R12 as ratified contract text (with R12's signer half
landing in signed-execution-chain instead). Until that change is authored,
ratified and realized, the canonical serialization, the digest, the
re-issuance grammar and the corpus-drift rule are recorded direction and NOT
enforced behavior: report §7's "Canonical serialization profile and value-
domain rules" and "Attestation envelope and signer trust model" rows stay
blocking. No implementation of R6–R12 precedes that change.

## Sequencing (constraints unchanged by these rulings)

The order below is the one the S5 ledger already forces; recording it here
performs no step in it and schedules none.

1. The Gate-Rules Council confirms R1–R3 with duty-specific soak evidence,
   and rules explicitly on whether the alias-to-exact flip re-opens the soak.
2. The roster-change Lead accepts application to the enrolled roster
   (`lead_accepted_recorded`).
3. The Operator writes the Packet 2 identity record per R5.
4. ONE ACT, not two: the alias-to-exact flip together with task 7.6's
   re-issuance runbook walk — the flip IS the deliberate composition bump the
   runbook is walked against.
5. The human-ratified register act issues against the resulting composition.
6. THEN a fresh OpenSpec change carries R6–R12.

`company-policy-lead` (R4) runs in the parallel tenant lane, because the
tenant declares its own representative and that declaration must land before
or with the domain mirror.
