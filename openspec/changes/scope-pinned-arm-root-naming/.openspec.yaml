schema: spec-driven
created: 2026-09-15
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:2026-09-15-scope-pinned-arm-root-naming
  reason: >-
    THE SUCCESSOR THE ARCHIVE ACT COULD NOT PERFORM. openxFactory issue #1047, filed UNCLAIMED on 2026-09-15 at the completion of the `extend-prose-tagging-target-to-pinned-capabilities` arc (FILED #994 → RATIFIED #1019 → REALIZED #1040 → ARCHIVED #1042 → `8944758c`). The promoted `document-lifecycle` requirement *Prose tagging marker hygiene* states that EVERY finding the pinned arm emits SHALL NAME THE ROOT OR ROOTS it resolved against, or failed to. TWO of the realized arm's fifteen findings are emitted BEFORE ANY ROOT IS SELECTED and cannot obey it — the lexically malformed pinned value, refused at `scripts/doc_health/families.py:1572` and naming THE VALUE at `:1573-1576`, and the document whose repository has no resolution root in the run, naming THE REPOSITORY at `:1587-1592` where `_pin_roots` returned an empty list. The other thirteen name their root already. Surfaced by Copilot on the archive pull request #1042 and RULED STANDS there — the sentence is ratified canon promoted BYTE-IDENTICAL and an archive act may not edit the text it promotes — which is why the remedy is a SUCCESSOR PACKET and not a patch, and why #1047 was filed rather than fixed in place. The delta scopes the obligation to findings that reach root selection and names the two that do not with what each names instead; the realized behaviour already matches the clarified reading, measured rather than assumed. WHY kind ad_hoc AND NOT kind staged, CHECKED RATHER THAN ASSUMED. The organized source is openxFactory issue #1047 together with the archived packet's own design D-2 root paragraph, which wrote the sentence and gave its rationale; `ideation/staging/` carries no topic naming the pinned arm, root selection, the root-naming obligation or marker-target resolution, so `kind: staged` would claim a staging source that does not resolve.
  proposed_by: >-
    Lane `openxfactory-2` (display `openXfactory-2`), session c0d09b6d (workstation Eagle), as the standing claim holder of openxFactory issue #1047 (CLAIMED 2026-09-15T20:43:33Z). NO APPROVAL PAIR IS DECLARED AND NONE IS IMPLIED. No word ratifies this packet — the lane claimed a filed finding and authored a proposal, which decides nothing and admits no text to canon. The lawful unapproved shape is the one `add-drafted-proposal-origin` (issue #318) defined — drafting provenance, no `approved_by`, no `approved_on` — and this packet's three lifecycle documents — `proposal.md`, `design.md` and `tasks.md` — carry `Status: draft` to match; its one spec delta carries no lifecycle header. Approval, when it comes, is a pure ADDITION beside a fixed `kind` and `id`. THE DECISIONS MOST WORTH A VETO ARE `design.md` D-1, D-2 and D-3, put as ONE multiple choice at `proposal.md` § The decision, put for a veto (OQ-1) with the recommendation first — the reading as encoded, widening the arm instead, or deleting the sentence. NOTHING IS PROMOTED, NOTHING IS REALIZED AND NOTHING IS ARCHIVED BY THIS FILING. This pull request edits no file under `openspec/specs/` and adds no script, no validator and no test; `code_surface` is empty, so under `release-realization` the archive is a separate act on a separate word, and openxFactory #1047 stays OPEN and closes THERE.
  proposed_on: 2026-09-15
related:
  - openxFactory:issue:1047                                                                    # THE GOVERNING FINDING. Filed UNCLAIMED at the arc's completion, claimed by lane openxfactory-2 on 2026-09-15; stays OPEN and closes at the ARCHIVE, never on this filing
  - openxFactory:change:archive/2026-09-15-extend-prose-tagging-target-to-pinned-capabilities   # ARCHIVED. Promoted *Prose tagging marker hygiene* in the form this packet MODIFIES, and its design D-2 root paragraph is where the root-naming sentence and its rationale were written
  - openxFactory:pr:1042                                                                        # MERGED 2026-09-15 to `8944758c`. The archive act where the defect was surfaced and RULED STANDS — the sentence promoted byte-identical, which the archive may not edit
  - openxFactory:pr:1040                                                                        # MERGED. The realization that landed `_pinned_arm` and `_judge_pinned_record`, whose two pre-selection findings this delta names
  - openxFactory:change:prepare-openspec-1-12-readiness                                         # ACTIVE. The only other `document-lifecycle` delta in the active corpus — an `## ADDED` requirement touching nothing this packet writes, so no collision and no `sequenced_after` is owed
