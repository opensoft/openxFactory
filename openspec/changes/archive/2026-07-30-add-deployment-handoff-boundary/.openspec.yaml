schema: spec-driven
created: 2026-07-28
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:2026-07-28-add-deployment-handoff-boundary
  reason: >-
    SWEPT BY RULING 2026-08-25, verbatim: "sweep the 23 derivable
    proposal-origin warnings". DERIVED FROM THIS PACKET'S OWN RECORD,
    never invented. The approval act is this packet's own lifecycle
    citation, quoted verbatim under approved_by below. The forcing fact is
    stated in this packet's own proposal under its Why heading, which this
    metadata-only edit leaves untouched. KIND IS ad_hoc, not staged: this
    packet records no Staging ID header, and no staging topic declares it
    as an exit in ideation/staging/INDEX.md — checked in both directions,
    because a topic that merely mentions a change is not that change's
    origin. The origin act is the approval quoted under approved_by. The
    created date 2026-07-28 was already recorded in this packet's own
    .openspec.yaml and is VERIFIED, NOT REWRITTEN: it is consistent with
    the packet's true first commit 1c59a343 of 2026-07-30, which a packet
    is authored before. This declaration supplies the origin and approval
    pair the proposal-origin contract requires and changes nothing else
    about the packet: no lifecycle header, no content, no status.
  approved_by: >-
    Per this packet's own "Ratified" citation, quoted verbatim: "Brett's
    approval of `add-deployment-handoff-boundary` on 2026-07-29, on his
    seven clarifying resolutions of 2026-07-24 carried as design decisions
    2–9 (all-actor scope, credentials-primary enforcement, grant-issuance
    and GitOps-merge gates, correlation stamping, standing bench requests,
    retroactive break-glass with escrow-owned custody, phased-never-gapped
    adoption, codexFactory sole first consumer) plus the capability-home
    confirmation at the proposal gate; the four residual questions (QA
    approval calibration, ACR namespace scope map, preview-environment
    threshold, break-glass window) are deliberately deferred to the
    successor realizations" — recorded in its proposal at authoring time.
    That is the approval provenance the ad-hoc exception requires, and it
    is why this packet fell in the derivable class rather than the stop
    class of the 2026-08-25 sweep.
  approved_on: 2026-07-29
