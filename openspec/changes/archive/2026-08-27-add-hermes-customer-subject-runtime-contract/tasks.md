Status: ratified
Ratified by: user approval of `add-hermes-customer-subject-runtime-contract` on 2026-07-12

## 1. Governance And Handoff

- [x] 1.1 Record the user-ratified neutral customer-subject-instance decision, scope, compatibility policy, and Gate G0 exit conditions in the proposal, design, and delta specs.
- [x] 1.2 Hand this OpenSpec change one-to-one to one openxFactory Speckit feature; its `tasks.md` becomes the executable implementation source of truth.
- [x] 1.3 Keep the OpenSpec design and deltas aligned when implementation discovers a material contract, security, migration, or release decision.

## 2. Canonical Contract Acceptance

- [x] 2.1 Accept Speckit evidence that every topology, identity, isolation, authority, binding, artifact, approval, traceability, migration, compatibility, and release requirement in this change is realized without weakening v1 or existing DomainxFactory conformance.
- [x] 2.2 Accept independent architecture, contract/data, security, privacy, operations, and release review with no unresolved blocking finding.

## 3. Verification Acceptance

- [x] 3.1 Accept reproducible evidence for the two-Customer installation, cross-domain neutrality, static-template compatibility, positive/negative semantic fixture matrix, and existing-regression suite.
- [x] 3.2 Accept real-Postgres evidence for trusted scope, default-deny isolation, transactional authorization/revocation, immutable records, deterministic migration/reconciliation, quarantine, DDL drift, crash/retry, and concurrency behavior.
- [x] 3.3 Accept strict OpenSpec validation and clean Speckit consistency analysis for the exact implementation candidate.

## 4. Publication Acceptance

- [x] 4.1 After final rebase and release metadata allocation, accept all verification and independent review against the exact unchanged candidate commit.
- [x] 4.2 Accept independently reproduced remote evidence that the exact reviewed commit is reachable from published `origin/main`, the annotated bundle tag dereferences to it, and its manifest, digest inventory, and raw Git blob hashes agree.

## 5. External Hermes Install Completion Gate

- [x] 5.1 Hand the published bundle evidence to the existing Hermes Install OpenSpec/Speckit feature; downstream neutralization, checker implementation, and pinning remain owned there.
- [x] 5.2 Accept Hermes Install evidence tied to its exact landed commit and repository-relative manifest/checker/runtime-binding/evidence paths and digests, including online/offline checks, externally bound manifest digest, exact published pin, deliberate drift failures, strict OpenSpec validation, and clean Speckit analysis.
- [x] 5.3 Record Gate G0/T009 closure only after the published openxFactory evidence and Hermes Install pin independently reproduce every required digest and dependent multi-subject implementation remains stopped until then.
