# Archive Record: adopt-codexfactory-repository-identity

Status: record
Kind: report
Recorded: 2026-09-10
Recorder: lane `provenance-autonomous-merge`.
Archive pull request: [openxFactory #879](https://github.com/opensoft/openxFactory/pull/879)
(AUTHORED, not merged — the merge is the act).

Ratified: 2026-09-07, Brett Heap (convener), verbatim *"accept all [A] and
ratify 763"*, posted 2026-09-07T22:21:44Z on
[PR #763](https://github.com/opensoft/openxFactory/pull/763#issuecomment-5576174434);
ratified head `5bfdcf3c06478167bd7509f1576db3528ce4ede1`; record
`review/ratification-2026-09-07.md`. The proposal merged as
[#763](https://github.com/opensoft/openxFactory/pull/763) → `eb30db7a`,
2026-09-08T03:38:07Z.

---

## 1. Why this packet archives on evidence and not on landing

`code_surface:` is **NON-EMPTY** — 123 live occurrences across 60 files were
respelled, the factory-origin identity records were re-issued, and eight
`contract-v3.4` inventory members moved — so under `release-realization`'s
**Realization archive gate** this packet archives on **merged-plus-green
realization evidence**, not on the landing of its proposal:

> A change with a non-empty code surface SHALL NOT archive until realization
> evidence exists: its code merged on the implemented target through the owning
> domain's engineering gates, and — where the surface is runnable — a green run
> of that surface.

Both halves are **CITED BELOW RATHER THAN ASSERTED**: every merge is a commit
on `main` an outside reader resolves, and every green run is a workflow run id.

**THE SEPARATE ARCHIVE WORD IS NOT YET GIVEN, AND THIS RECORD DOES NOT CLAIM
IT.** The house practice its two most recent predecessors followed
(`archive/2026-09-09-bump-openspec-cli-pin-to-1.12`, on Brett Heap's *"archive
the bump change"*; `archive/2026-09-09-amend-marker-defect-reporting`, on *"do
1, then 2"*) is that the act takes a separate word on the day it happens. This
record is authored ahead of that word: **the archive pull request is AUTHORED,
NOT MERGED, and the merge is what performs the act** — on Brett Heap's standing
word, via lane `provenance-autonomous-merge`. If the word names a different UTC
day, the archive directory's date and this record's date move with it rather
than staying as written, because
`tests/sequenced_after/test_archive_commit_dates.py` measures the directory's
date against the UTC date of the commit that added it.

## 2. Realization evidence — MERGED

Every row is a merge commit on `opensoft/openxFactory` `main` unless the
repository is named otherwise. Every openxFactory sha below answers
`git merge-base --is-ancestor <sha> origin/main`, verified 2026-09-10.

| slice | pull request | merge commit | merged (UTC) | what it realized |
| --- | --- | --- | --- | --- |
| **B1** | [#801](https://github.com/opensoft/openxFactory/pull/801) | `e86eca35` | 15:40:56Z | the review-lane decision-core pin, the four workflows, the operator tools (42 occurrences / 15 files) — GATED on runbook Phase 1, step 1.6 included |
| **B1b** | [#853](https://github.com/opensoft/openxFactory/pull/853) | `4d6796ef` | 18:11:58Z | the doc-health nightly's owner slot: codexFactory is a FOREIGN owner, reached through the openxfactory App |
| **B2** | [#802](https://github.com/opensoft/openxFactory/pull/802) | `20298c64` | 19:24:13Z | re-issue of the factory-origin identity — register row, wallet, grant, custody attestation — plus the occurrence no slice owned (37/15). **HUMAN-ONLY surface: merged on Brett Heap's own word**, per runbook 10.3 |
| **B3** | [#805](https://github.com/opensoft/openxFactory/pull/805) | `00d368a4` | 20:00:17Z | the eight `contract-v3.4` inventoried members; the supported-domain regression denominator re-sorted (12/9) |
| **B4** | [#806](https://github.com/opensoft/openxFactory/pull/806) | `95e67ab0` | 20:44:01Z | the remaining live documents and `README.md`, per line (34/15 — 30 respelled, 4 frozen) |
| **bundle cut** | [#866](https://github.com/opensoft/openxFactory/pull/866) | `a37ae0cd` | 21:47:48Z | **`contract-v3.5`** — `contracts/manifest.yaml`, the CHANGELOG entry, `contracts/releases/contract-v3.5.digests.yaml` (283 members, membership UNCHANGED, twelve digests re-baselined), and the intent-compliance release-boundary test |
| **B4b** | [#867](https://github.com/opensoft/openxFactory/pull/867) | `111a5034` | 21:48:20Z | the two post-reconciliation stragglers follow the repository to `codeXfactory` |
| **ticks** | [#873](https://github.com/opensoft/openxFactory/pull/873) | `8480378a` | 23:32:31Z | tasks.md ticked against landed evidence — 7.1, 7.4, 7.5, 6.5 and the rest |

All eight merged 2026-09-09. Two more merges are the realization's other halves,
in the repositories that own them:

| repository | pull request | merge commit | merged (UTC) | what it realized |
| --- | --- | --- | --- | --- |
| `opensoft/xFactory` (aggregation) | [#376](https://github.com/opensoft/xFactory/pull/376) | `376275c4` | 19:29:34Z | **Phase 2 lockstep** — `.gitmodules`, the `xFactories/codexFactory` gitlink, `README.md`, seven council/lane workflows, `.github/merge-approval-envelope.yml`, `docs/council-lane-app-registration.md` and the two workflow pin tests: fifteen files, ONE commit, **no sha moved** |
| `codeXfactory/codexFactory` | [#308](https://github.com/codeXfactory/codexFactory/pull/308) | `373410bc` | 19:16:31Z | **Org move Phase 4** — codexFactory's own self-references, 49 files, carrying its own 710-line sweep record `docs/org-move-phase4-self-reference-sweep-2026-09-09.md` |

### 2.1 The bundle cut and its tag

`contract-v3.5` was **allocated at realization, not reserved at proposal**, per
`docs/contract-versioning-policy.md` § Bundle Realization Order. `contract-v3.4`
was already cut and tagged (`807a4f47`) and owed nothing, so `contract-v3.5` is
the next available additive minor and no number was reused.

The **annotated tag** `contract-v3.5` was published by Brett Heap (operator) at
2026-09-09T22:31:05Z at the LANDED commit — never from the cutting branch,
because a merge always creates a different commit and skipping the
re-verification is what made `contract-v3.1` defective. Verified 2026-09-10 from
this tree:

```
$ git ls-remote --tags origin contract-v3.5
6c602f3cbf4796fad58263a96313132c30e26599	refs/tags/contract-v3.5
a37ae0cdabed0b2407b7d6b2da4d24fb12718059	refs/tags/contract-v3.5^{}
```

— the tag OBJECT is `6c602f3c` and it dereferences to `a37ae0cd`, which is
#866's merge commit exactly.

**THE THREE VERIFICATION ARMS, READ HONESTLY.** Re-run 2026-09-10 against the
published state:

| arm | verdict now | reading |
| --- | --- | --- |
| `verify-commit --commit a37ae0cd` | **pass** (`inventory=contracts/releases/contract-v3.5.digests.yaml`) | the landed commit's tree still matches the inventory it declares |
| `verify-tag --remote origin --tag contract-v3.5` | **pass** | the published annotated tag is well-formed and resolves where it must |
| `verify-promotion --commit a37ae0cd --remote origin --tag contract-v3.5` | `HGR-RELEASE-TAG-EXISTS` — *"the bundle tag already exists on the remote"* | **EXPECTED, and not a defect.** `verify_promotion` is the PRE-publication readiness check: `scripts/hermes_runtime_validation/release.py:998` raises this finding precisely when `refs/tags/<tag>` is already on the remote. It PASSED at 22:31:05Z, before the tag existed (task 7.4), and it must refuse afterwards or it would be asserting nothing. The post-publication arm is `verify-tag`, and that passes. |

This record states that asymmetry rather than reporting "all three pass",
because all three passing today would mean the tag had never been published.

### 2.2 The occurrence accounting at closure — task 6.5

6.5 is the box that says the sweep is complete, and it closed **LAST**, per the
ceremony's merge order. Its final re-derivation and the ruling that closed it are
both on `codeXfactory/codexFactory`
[issue #279](https://github.com/codeXfactory/codexFactory/issues/279), posted by
this lane:

- **"6.5 FINAL re-derivation at openxFactory@111a5034 2026-09-09T22:10Z"**,
  2026-09-09T21:54:58Z — read-only, in a detached worktree of `origin/main`,
  removed afterwards; nothing pushed, no file modified.
- **"6.5 FINAL ruling (lane) 2026-09-09T21:58Z"**, 2026-09-09T21:55:37Z.

At `origin/main@111a5034` the whole-tree count is **319 occurrences across 125
files**, and it closes exactly:

```
RENAME-pathspec remainder   11 /   5   (every one ruled FROZEN by class)
FROZEN subtotal            117 /  62
NOT-SWEPT subtotal         191 /  58
                          ----------
                           319 / 125
```

**ZERO UNASSIGNED OCCURRENCES.** The 11/5 remainder is frozen by a cited rule in
each case, never by silence: `contracts/policies/repository-identity.yaml:101,106`
(the mapping's own `former:` lookup key — permanent by construction);
`docs/dogfood-content-migration-plan.md:49`; `README.md:895,1446,1458` (dated
realization narration); `contracts/CHANGELOG.md:16,86,123,258` and
`tests/intent-compliance/test_release_boundary.py:102` — the five that arrived
WITH the `contract-v3.5` cut itself, where the former identity is the SUBJECT of
a dated entry and respelling `:258` would invert the meaning of a
validator-rejection sentence.

## 3. Realization evidence — GREEN

`pytest-suite` is the REQUIRED runnable surface. The strongest single citation
is on `main` itself:

- **`pytest-suite` run
  [34418435909](https://github.com/opensoft/openxFactory/actions/runs/34418435909)
  — `success` on `main` at `9c0e2cda`** (2026-09-09T23:47:08Z), and `9c0e2cda`
  is a DESCENDANT of every merge in § 2 (all eight openxFactory shas answer
  `git merge-base --is-ancestor <sha> origin/main`).

Green **ON the merge commit itself** for four of the eight:

| merge commit | slice | run | conclusion |
| --- | --- | --- | --- |
| `e86eca35` | B1 | [34371838117](https://github.com/opensoft/openxFactory/actions/runs/34371838117) | success |
| `00d368a4` | B3 | [34398421372](https://github.com/opensoft/openxFactory/actions/runs/34398421372) | success |
| `95e67ab0` | B4 | [34402819203](https://github.com/opensoft/openxFactory/actions/runs/34402819203) | success |
| `111a5034` | B4b | [34408911110](https://github.com/opensoft/openxFactory/actions/runs/34408911110) | success |

**AND THE OTHER FOUR ARE STATED, NOT GLOSSED.** `4d6796ef` (B1b, run
34387548502), `20298c64` (B2, run 34394849924), `a37ae0cd` (the cut, run
34408865333) and `8480378a` (the ticks, run 34417390262) each had their own
`main`-push run **CANCELLED** — the workflow's concurrency group cancels an
in-flight `main` run when the next push arrives, and these four were followed
within minutes. Their green is carried by the next descendant that completed:
`111a5034`'s success (34408911110) covers `4d6796ef`, `20298c64` and `a37ae0cd`;
`9c0e2cda`'s success (34418435909) covers `8480378a`. **A cancelled run is not a
failed run and is not being counted as green here** — the green cited is the
descendant's, and this record names which.

Task-level green, recorded at the tick rather than re-derived here (task 7.1, on
the fully renamed tree at the bundle-cut candidate): `pytest tests/clearing
tests/factory_identity tests/review_lane_pin` → **481 passed, 2 skipped, 54
subtests**; `pytest tests/hermes_runtime_contracts` → **577 passed**;
`pytest tests/intent-compliance tests/clearing tests/credential_contracts` →
**739 passed**; and seven validators green, `validate-signed-execution-chain.py`
included at **0 errors / 0 warnings, 34 signature-covered files byte-unchanged**
— which is the measurement that proves the cryptographic freeze this packet's
first ADDED requirement governs.

Task 7.5's drift reading is the family working as designed: `verify-commit` at
the pre-cut tip `95e67ab0` reported **ten** `HGR-RELEASE-DIGEST-MISMATCH`
findings (the EIGHT non-editorial members at `ERROR`, the predicted transient,
plus two editorial at `INFO`), and after the cut `doc-health --family
release-inventory-drift` reports **No findings**.

## 4. What promotes

**FOUR ADDED REQUIREMENTS, TWELVE SCENARIOS, NO `MODIFIED` BLOCK ANYWHERE:**

1. *A cryptographically covered former identity is frozen by construction* (3 scenarios)
2. *A repository identity that is an authorization scope is re-issued with the transfer* (3)
3. *A transfer disposition is a published rule with a recorded sweep* (3)
4. *A cross-organization governed reference is an identity, not a redirect* (3)

**AND THIS ARCHIVE CREATES THE CAPABILITY DIRECTORY, WHICH IS WORTH SAYING
PLAINLY.** `openspec/specs/repository-identity/` does **not exist** on `main` at
`9c0e2cda`: the capability's exemplar, `adopt-medxsoft-repository-identity`, is
still an ACTIVE change carrying `Status: draft`, and it is the packet this one
declares `sequenced_after: [adopt-medxsoft-repository-identity]` on. So the
promoted spec count moves **61 → 62** and the file is written by the
SEQUENCED-AFTER packet rather than by its antecedent. Two consequences, both
recorded rather than left to be found:

- **`scripts/validate-sequenced-after.py` does not refuse it.** Its declared
  contract resolves a `sequenced_after` entry against "the ACTIVE and ARCHIVED
  corpora both"; it gates on malformed, dangling and cyclic declarations and on
  archive-date agreement, **not on archive ORDER**. The antecedent stays
  resolvable as an active change, and `adopt-configured-notebook-hosting-identity`
  — which declares `sequenced_after` on THIS packet — stays resolvable against
  the archived corpus after the move.
- **A floor regeneration is OWED AFTER THIS LANDS, and is graced before it.**
  `review-lane-floor-mirror`'s promoted completeness rule has a scenario written
  for exactly this shape — *"The candidate that promotes a capability creates its
  own spec path"*: a tracked path under the floored prefix that the floor does
  not name is **COVERED-PENDING** when the candidate's own diff CREATES it, so
  the REQUIRED coverage assertion does not fail, the advisory lane reports it
  covered-pending, and neither lane adds it to the floor. The regeneration
  follows the landing; it does not gate it.

The four ADDED requirement titles are disjoint from the four the exemplar's own
delta ADDs (*Canonical repository identity*, *Published repository transfer
mapping*, *Immutable and dated records keep their recorded identity*, *A transfer
is an additive content change*), so nothing this packet promotes forecloses
anything the exemplar promotes when its turn comes.

## 5. The open follow-ons — EIGHT, NAMED, NOT HIDDEN

Fifty of the fifty-eight boxes were already `[x]` before this act, and **no box
is ticked by the archive.** The eight that remain carry the house's reserved
DEFERRED marker `- [~]`, each with its own note in `tasks.md` naming the holder
and what has and has not happened. The marker is used in preference to a tick
because `scripts/proposal-support.py … archive` refuses any packet whose
`tasks.md` still matches `^- \[ \]` (*"change has incomplete tasks"*) and
carries **no bypass flag** — so ticking a box whose work did not happen would buy
that refusal off with a false claim.

| box | holder | standing |
| --- | --- | --- |
| **5.7** | the operator | Custody check of codexFactory's `worker-credentials` environment and `FACTORY_ORIGIN_SIGNING_KEY` across the transfer. **NOT PERFORMED**, and its subject is not readable from this corpus at all — it lives inside the private repository that moved. The box's own first words are *"OPERATOR HANDOFF, not performed here"*. |
| **8.4** | whoever next runs the projection sync | `sync-notebooklm-books.py … --apply`. **NO EVIDENCE IT RAN.** The box was authored `- [ ]` and already said the projection state is not verifiable from the corpus alone; doc-health's `notebook-projection-drift` family is SKIPPED on every run of this corpus for the same reason. |
| **9.1** | the operator | The ceremony in full. **PARTLY PERFORMED** — transfer, visibility flips and lockstep are done; the App installations, the secret inventory (5.7), the GHCR window and prefix flip (**OQ-4, still UNRULED**) and the SonarCloud rebind are not. The openxFactory visibility flip **still gates on OQ-3's four ideation-split moves landing first**, which this archive does not assert have landed. |
| **9.2** | `installs/hermes-install` | **NOTHING HAS SWEPT IT.** 181 occurrences across 84 files stand, `config/clients/opensoft/overlay.yaml` and `tests/unit/test_subject_pin_guard.py` included. The box DECLINES the exemplar's "at its next pin bump" deferral and asks for a scheduled step with a test-count proof; that request is carried forward unanswered. |
| **9.3** | the operator, on the runbook | **LARGELY PERFORMED** — xFactory #376 → `376275c4`, fifteen files in one commit, no sha moved. Open because no aggregation-side re-count of the 25-across-14 measurement was filed here. |
| **9.4** | `opensoft/OpsxFactory` | **NOTHING HAS AMENDED THE PLAN**, so the revocation this box exists to record has not been triggered: task 4.2's approval still stands over the OLD digest (`4e1a4b76…`), and amending the plan REVOKES it and owes a re-approval under the plan's requirement 2. |
| **9.5** | `codeXfactory/codexFactory` | **LARGELY PERFORMED** — codexFactory #308 → `373410bc`, 49 files plus a 710-line sweep record. Open because that record, not this packet, is the authority on the residue of the 532-across-211 measurement, and no re-count was filed here. |
| **9.6** | a future change, if a convener rules one is owed | **UNRULED.** No check family verifying that no live surface names a mapped former identity exists, and none was authored — *"Building it here would be a second change riding a first"*. Both inputs such a family needs are now on record. |

**TWO OPEN QUESTIONS ALSO SURVIVE THE ARCHIVE**, and the ratification said so:
**OQ-4** (GHCR sequencing) and **OQ-6** (canonical case spelling) were NOT
answered by the word of 2026-09-07 and are not answered here. OQ-1 (Enterprise
membership, VERIFIED `plan=enterprise` 2026-09-07T16:41:04Z), OQ-2 (Apache-2.0,
landed openxFactory #762 / openXwallet #22), OQ-3 (the ideation-split decision
sheet's four dispositions) and OQ-5 (exercise the `MIGRATION_PIN` re-point
ceremony rather than riding GitHub's redirect) are ruled.

## 6. How the act was performed

Through the house entrypoint, never a bare `openspec archive`:

```
TZ=UTC python3 scripts/proposal-support.py . archive \
    adopt-codexfactory-repository-identity --date 2026-09-10 --yes
```

The wrapper resolves `@fission-ai/openspec@1.12.0` from the content-addressed
pin cache over the pinned dependency closure, fires the origin-retention gate
before it moves anything, refuses on any `^- \[ \]` box, and fixes the archive
directory's date to UTC — the pinned CLI having no date option of its own.
`--date` must be today in UTC or the wrapper refuses before the CLI runs, which
is why this record is dated 2026-09-10: UTC crossed midnight at
2026-09-10T00:00Z, between the ticks merge and this authoring.

One correction of stale state rides along, in the class precedent A
(`archive/2026-09-09-bump-openspec-cli-pin-to-1.12`) named for the same act:
`proposal.md` carried its `Status:`/`Ratified:` pair ONLY in the YAML front
matter and doc-health's `status-validity` family had been reporting the body
header as missing. The body now carries `Status: ratified`; the ONE citation
line stays in the front matter, which is what `ratified-provenance` counts
(precedent `archive/2026-09-09-amend-marker-defect-reporting`).

## 7. What this archive does NOT claim

- It does not claim the operator ceremony is finished. See 5.7, 9.1, 9.2, 9.4.
- It does not claim OQ-4 or OQ-6 are answered.
- It does not claim the NotebookLM projection reflects these doc changes.
- It does not claim `adopt-medxsoft-repository-identity` is ratified, archived,
  or superseded. It is active and `Status: draft`, and this packet's
  `sequenced_after` declaration on it survives the move intact.
- It does not claim the openxFactory visibility flip has happened, nor that
  OQ-3's four gating moves have landed.
- It does not claim the review-authority floor already names
  `openspec/specs/repository-identity/spec.md`. It does not; the regeneration is
  owed after this lands, and the covered-pending grace is what carries the
  candidate until then.
