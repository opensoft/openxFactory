# Closure as re-homed: add-doxchat-model-intake

Status: record
Kind: report
Closure date: 2026-09-16 (UTC)
Authority: RULING **Q6**, Brett Heap, 2026-09-04T17:49Z, `opensoft/openxFactory`
issue #656 — encoded at `split-opendox-two-layer-product` `tasks.md` § 6.3 and
`design.md` § D9
Destination: `opensoft/openDox` — the spec leg `opensoft/openDox-spec`, change
`openspec/changes/add-doxchat-model-intake`, PR
[openDox-spec #13](https://github.com/opensoft/openDox-spec/pull/13)
Lane: openxfactory-4 (openXfactory-4-openDox_extraction); CLAIM
[#656 5690514951](https://github.com/opensoft/openxFactory/issues/656#issuecomment-5690514951)
Precedent: § 6.4's closure record,
`openspec/changes/archive/2026-09-16-add-composed-view-authoring/review/rehome-2026-09-16.md`
(PR #1056), which settled the FORM this record follows.

## The disposition

`tasks.md` § 6.3: **`add-doxchat-model-intake` → openDox.** *"Built but
unarchived; its code moves with the carve as `moved_with_declared_edit` rows, its
four ADDED requirements are re-authored in openDox, and its one additive schema
enum member is already `openxFactory` contract bytes and STAYS."*

The packet's own delta states the two admissible forms
(`openspec/changes/split-opendox-two-layer-product/specs/ideation-dashboard/spec.md`,
§ THE SIBLING COLLISION): *"archived, or closed as re-homed with its successor
destination named in the receiving repository"*. This takes the second.

## What it does

1. The packet is RELOCATED, unedited, from `openspec/changes/` to
   `openspec/changes/archive/2026-09-16-add-doxchat-model-intake/` — `git mv` and
   nothing else, so the move is five pure renames in the diff, `Status: ratified`
   header included.
2. This record is added beside them.
3. The README "OpenSpec Records" active bullet retires; an archived-ledger entry
   replaces it.
4. `tests/sequenced_after/corpus-ledger.yaml`'s row moves `active` → `archived`.
5. `tests/doc-health/test_modified_block_currency_self_gate.py`'s
   `_LEDGER_SUBJECTS` loses this packet's row, which retires with it.

**NO DELTA IS PROMOTED**, for the reason § 6.4's record sets out at length and
which is sharper here: this packet's four `## ADDED` requirements are four of the
TWELVE titles the split packet's § THE SIBLING COLLISION (b) records as *"not in
canon and NOT removed here"*, with the disposition stated in terms — *"their
`## ADDED` blocks never promote HERE and their content is re-authored in the
receiving repository"*. Promoting them would put four requirements into a
capability the split packet's `## REMOVED` block does not name and therefore
cannot remove: *"A `## REMOVED Requirements` block over a title the promoted
specification does not carry would be a removal of nothing."* They would outlive
the capability's exit. So the relocation is a `git mv`, never `openspec archive`
or `scripts/proposal-support.py … archive`, both of which always promote.

## THIS IS THE FIRST CLOSURE TO EXERCISE HALF (b)

§ 6.4 carried one `## MODIFIED` block and nothing else, so it touched only half
(a) of the collision — the four promoted titles a sibling modifies. This packet
carries **1 MODIFIED + 4 ADDED** and is therefore the first time the (b) half
actually lands anywhere. Recorded here because the two halves have different
reasons for not promoting: (a) would have re-stated a requirement hours before
the split packet removes it; (b) would have created requirements nothing can
remove.

## What travels, what stays, and what is somebody else's box

| | |
| --- | --- |
| TRAVELS | the whole delta — 1 MODIFIED + 4 ADDED — byte-identical to openDox-spec PR #13 |
| TRAVELS OPEN | § 4.1 live-console proof, and § 4.2 realization evidence. RULING Q6 freezes the five WHERE THEY STAND, so a closure with open boxes is the ruled disposition |
| **STAYS** | the one additive `action` enum member in `contracts/schemas/gate-action-record.schema.yaml`. Already openxFactory contract bytes at `contract-v1.45`; `tasks.md` § 6.3 says it STAYS. No byte is added, moved or reverted here, and `contract-v1.45` stays true as published |
| NOT THIS CLOSURE'S | the built code, which moves with the carve as `moved_with_declared_edit` rows of `docs/opendox-carve-manifest.yaml` (§ 3). No code moves in this act |

## Measured at the closure

| measurement | value |
| --- | --- |
| `…/specs/ideation-dashboard/spec.md` here | 16,813 B, `sha256 3168ad8f6f31c8dbacdc772d933508943f7b2c7cf373de2357eb8958d4bebee1` |
| the same file at openDox-spec `change/receive-doxchat-model-intake` | **the same 16,813 B and the same sha256** — `diff` reports no difference |
| `grep -c openxFactory` over that delta | **0** — no subject edit was owed, so none was made |

## The promotion obligation, and its recorded discharge

`document-lifecycle`'s *Ratified spec deltas reach the promoted specification*
(`openspec/specs/document-lifecycle/spec.md:1055`) attaches to the ARCHIVE ACT,
and this packet declares `Status: ratified`, which is not edited. So the
obligation attaches, `scripts/doc_health/promotion_fidelity.py` carries it at
`_LAUNCH_SEVERITY = ERROR`, and this closure takes the second of the two
discharges the checker's own action text names — *"or record the non-promotion as
deliberate"* — through `health/dispositions.yaml` at the xFactory aggregation
root, batched across the five § 6 closures with `tasks.md` § 6.6. Because this
packet carries five requirements rather than one, its entry is the coarse
path-keyed form (no `requirement:` narrowing), which disposes every finding the
family raises against this one delta file.

**The caveat, from the loader's own docstring:** a `--single-repo` self-gate run
has no aggregation root and applies no disposition, so that run reports these
findings until § 5 removes the capability. The closing pull request reports both
numbers rather than suppressing either.

## Pinned references, checked before the move rather than after

`grep -rn 'add-doxchat-model-intake' --include='*.py' --include='*.yaml'
--include='*.yml' --include='*.json'` over `cb2d3a2c` returns sixteen hits. One
is a live coupling and it moves in this pull request: the `_LEDGER_SUBJECTS` row
above. Two look like couplings and are not, and both are stated so the next
reader does not re-derive them:

- `tests/doc-health/test_modified_block_currency_fixtures.py`'s `DELTA351` is a
  path-shaped LABEL on a RECONSTRUCTED historical text under the synthetic
  repository `intakeFactory`; it never reads this tree.
- `openspec/changes/retire-doxbench-chat-turn-v1/.openspec.yaml`'s `related:`
  comment naming this change ACTIVE is § 6.2's to re-read, and § 6.2 is sequenced
  after this box.

**`contracts/openspec-cli-pin.yaml` carries NO disposition for this change**, so
§ 6.4's `pin-disposition-stale` refusal has no counterpart here — checked, not
assumed.
