schema: spec-driven
created: 2026-09-08
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:2026-09-08-pin-openspec-cli-dependency-closure
  reason: >-
    THE OPEN ITEM #667 NAMED, TAKEN ON A RULING. `add-openspec-cli-pin`'s
    design.md § "Open items" records OI-1 in as many words: "the referent
    addresses the CLI's bytes, not its nine caret-ranged dependencies. Declared
    in the pin's header, mitigated by `--ignore-scripts`, not repaired. Closing
    it needs a lockfile the publisher does not ship; whether to vendor one, or
    to pin the resolved tree ourselves, is not decided here." Task 5.2 of that
    packet carries the same words as an untouched box, and
    `bump-openspec-cli-pin-to-1.12` task 6.4 restates it unchanged after the
    version move ("The pinned artifact's DEPENDENCY CLOSURE is still unpinned").
    THIS PACKET DECIDES IT AND CLOSES IT. The decision was not the lane's: four
    exits were put to Brett Heap in session and he chose one, quoted verbatim
    below.
    WHY `kind: ad_hoc` AND NOT `kind: staged`, CHECKED RATHER THAN ASSUMED.
    `ideation/staging/` was enumerated and `INDEX.md` read on 2026-09-08: no
    topic carries the OpenSpec CLI pin, a dependency closure, a lockfile or a
    package-manager consumption question. `kind: staged` would claim a staging
    source that does not resolve. The organized source is #667's own design
    § OI-1 and the operator ruling quoted below.
  approved_by: >-
    Brett Heap (openxFactory operator authority) — 2026-09-08T14:14:49Z,
    first-hand, in session, on a FOUR-OPTION packet put to him: (1) vendor a
    lockfile, (2) enumerate the resolved tree in the pin contract, (3) vendor
    the built tree as one artifact by digest, (4) accept the shortfall as
    declared. He chose verbatim **"Vendor a lockfile (Recommended)"**,
    described to him as: *an authored lockfile committed beside the pin, its
    digest recorded in the pin, the installer and verifier bound to it;
    standard npm mechanism, regenerated per bump.* THE RULING AUTHORIZED THE
    AUTHORING AND NOT THE CONTENT — it settles WHICH of four exits is taken and
    nothing else. This packet carries `Status: draft`, carries no ratification
    citation, and owes one from a separate act. The ruling is recorded on
    openxFactory PR #667 in this lane's CLAIMED comment at that timestamp.
  approved_on: 2026-09-08
related:
  - openxFactory:change:add-openspec-cli-pin              # ACTIVE. #667. Cut the pin, DECLARED this shortfall in the pin's header, and carries it as design OI-1 and task 5.2
  - openxFactory:change:bump-openspec-cli-pin-to-1.12     # ACTIVE. #677. Moved the referent to 1.12.0 and restated the shortfall unchanged as task 6.4
  - openxFactory:change:disposition-codexfactory-declared-renames  # ARCHIVED-ADJACENT precedent for a third change touching this pin family without co-modifying its deltas
