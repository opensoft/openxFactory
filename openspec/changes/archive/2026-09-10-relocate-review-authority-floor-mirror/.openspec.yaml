schema: spec-driven
created: 2026-09-08
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:2026-09-08-relocate-review-authority-floor-mirror
  reason: >-
    THE SOURCE IS ANOTHER REPOSITORY'S RULED SHAPE, and this packet is the half
    that repository cannot author. codexFactory issue #232's finding (3) was
    STOPPED rather than performed on 2026-09-08T13:35:01Z (comment 5585989018),
    and Brett Heap ruled shape 1 at 13:49:30Z (comment 5586188401, mirrored on
    openxFactory issue #745), verbatim "rule shape 1, measure first, this lane
    realizes it": relocate the floored document
    `scripts/merge_master/openxfactory-review-authority-floor.yaml` off every
    CODEOWNERS prefix, no bypass actor anywhere. The codexFactory packet
    `relocate-review-authority-floor` (pull request #293, DRAFT) names THIS
    packet as its owed companion in decision D-7 and sequences it FIRST in
    decision D-2, because this repository's re-pin lane fetches that document BY
    PATH and would 404 if codexFactory moved first. Not from an openxFactory
    staged topic, and that was CHECKED rather than assumed: no folder under
    `ideation/staging/` names the review-lane pin, the floor document, its path
    or CODEOWNERS routing, and `ideation/staging/INDEX.md` declares no topic
    exit naming any of them. ad_hoc is the correct kind and is not a substitute
    for staging.
  approved_by: >-
    Brett Heap (openxFactory repository owner) — in session, lane
    openxfactory-2 (openXfactory-2), session 78b27179, 2026-09-08T13:49:30Z,
    verbatim **"rule shape 1, measure first, this lane realizes it"**, recorded
    on codexFactory issue #232 (comment 5586188401) and mirrored on openxFactory
    issue #745. THE WORD RULES THE SHAPE AND NAMES THE LANE; IT RATIFIES NO
    TEXT, and it did not separately name this companion — the companion is
    owed BY the shape, because the shape cannot be realized without it. Whether
    this packet's text is the right dual-path acceptance was, at the time this
    field was written, a separate act that had not happened and was Brett Heap's
    alone. AT THAT TIME the packet was `Status: draft`, carried no ratification
    citation and owed none — the lifecycle rule requires one only for
    `Status: ratified` — and every box in `tasks.md` was unticked; filing it
    moved no workflow, script, contract, test or pin byte.
    THE PACKET HAS SINCE BEEN RATIFIED — Brett Heap, in session,
    2026-09-08T23:51Z, verbatim **"ratify 293 and 817 when green, then realize
    them"**, recorded on openxFactory issue #745 and mirrored on codexFactory
    issue #232, over pull request #817; record
    `review/ratification-2026-09-08.md`. THE SAME WORD RATIFIED THE SIBLING
    `codexFactory:change:relocate-review-authority-floor` (codexFactory #293),
    which answers MQ-1: the same word, not separately. Decisions M-1 through M-7
    stand as recommended and no veto was exercised. THE CONDITION "when green"
    WAS NOT MET WHEN THE WORD WAS GIVEN and that is recorded rather than
    smoothed: `pytest-suite` was red for two causes, both this packet's own — an
    undeclared sibling-pairing on the `## MODIFIED` block and a missing
    corpus-ledger row — and both were fixed in the packet, never in a test,
    before the ratifying commit was made. THIS FIELD STILL RECORDS
    AUTHORIZATION TO FILE ONLY; NOTHING IS REALIZED BY EITHER ACT — `tasks.md`
    ticks exactly boxes 1.1, 1.2, 5.1 and 5.2, boxes 1.3 (MQ-2) and 1.4 (MQ-3)
    stay open and neither blocks realization (1), and *"then realize them"* is a
    second act that lands as separate pull requests with THIS repository's step
    (1) first. THE OPEN QUESTION THIS FIELD ONCE CARRIED IS NOW ANSWERED: it
    asked (as OQ-3 of #293, MQ-1 here) whether this packet would ratify on the
    same word as its codexFactory sibling or separately, and the word of
    2026-09-08T23:51Z ratified both — the same word, not separately.
  approved_on: 2026-09-08
related:
  - codexFactory:change:relocate-review-authority-floor   # the sibling this mirrors; RATIFIED 2026-09-08 by the same word, codexFactory #293, carries the measurement record
  - openxFactory:change:mirror-floor-regeneration-automation  # the packet whose requirement 4 this amends; ratified 2026-09-06
  - codexFactory:change:amend-floor-regeneration-merge-authority  # ratified and realized 2026-09-08 (`93f0f0d7`); its realization #292 arms auto-merge
