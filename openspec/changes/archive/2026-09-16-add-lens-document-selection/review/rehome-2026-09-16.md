# Closure as re-homed: add-lens-document-selection

Status: record
Kind: report
Closure date: 2026-09-16 (UTC)
Authority: RULING **Q6**, Brett Heap, 2026-09-04T17:49Z, and RULING **DQ-1**,
Brett Heap, 2026-09-04T22:14Z (`opensoft/openxFactory` issue #656, comment
`5547049745`) — encoded at `split-opendox-two-layer-product` `tasks.md` § 6.5 and
`design.md` § D9
Destination: `opensoft/openDox` — the spec leg `opensoft/openDox-spec`, change
`openspec/changes/add-lens-document-selection`, PR
[openDox-spec #14](https://github.com/opensoft/openDox-spec/pull/14)
Lane: openxfactory-4 (openXfactory-4-openDox_extraction); CLAIM
[#656 5690674977](https://github.com/opensoft/openxFactory/issues/656#issuecomment-5690674977)
Precedents: § 6.4 (PR #1056), § 6.3 (PR #1057) and § 6.1 (PR #1060), whose
closure records settled the FORM this one follows.

## The disposition, and the one thing that makes it the hardest of the five

`tasks.md` § 6.5 and `design.md` § D9 say it in the same words: this is **"THE
ONLY ONE OF THE FIVE WHOSE CONTENT DOES NOT LAND IN ONE PLACE."** The set-builder
half is openDox's; its `doc_health.staging_seed` drafter and its serve route
**STAY in `openxFactory`'s own adapter** under RULING DQ-1 — *"no longer a
`codexDox` question"*.

**AND THE SPLIT DOES NOT FALL BETWEEN REQUIREMENTS. IT FALLS INSIDE ONE.**
Measured rather than assumed — the packet's five `## ADDED` requirements are:

| # | requirement | side |
| --- | --- | --- |
| 1 | *Every view of a document is one hover away from the others* | app — openDox |
| 2 | *A document selection drafts a staging-queue seed* | **BOTH** |
| 3 | *A finding is stated before it is drawn* | app — openDox |
| 4 | *A panel in contested space carries one row of chrome* | app — openDox |
| 5 | *The theme owns every colour a view chooses* | app — openDox |

Requirement 2 carries both halves in one sentence — *"The lens matrix SHALL let a
human select documents and SHALL draft a staging-queue fragment covering exactly
the selected set"* — and the packet's own `code_surface:` names them as two
surfaces on one line: *"lens matrix selection + cross-view document highlight; …
`doc_health.staging_seed` drafter and its serve route"*. Its drafting clauses are
the openxFactory half in terms: the draft *"SHALL write nothing"*, the evidence
*"SHALL be recomputed by the serving side from its own snapshot"*, and an unknown
document *"SHALL be refused by name"* — obligations on the drafter and the route,
not on the matrix.

## What this closure does

1. The packet is RELOCATED, unedited, to
   `openspec/changes/archive/2026-09-16-add-lens-document-selection/` — `git mv`
   and nothing else, four pure renames, `Status: ratified` header included
   (Brett's three annotations of 2026-08-08).
2. This record is added beside it, and it is the only file that did not exist
   before the closure.
3. The README "OpenSpec Records" active bullet retires; an archived-ledger entry
   replaces it.
4. `tests/sequenced_after/corpus-ledger.yaml`'s row moves `active` → `archived`,
   re-seeded by `scripts/validate-sequenced-after.py . --seed-ledger --moved-by
   '#<this PR — filled in the ledger-seed commit below>'`.

**NO DELTA IS PROMOTED**, for the reason §§ 6.4, 6.3 and 6.1 set out and which is
sharpest here: all FIVE of this packet's requirements are `## ADDED` titles the
split packet's § THE SIBLING COLLISION (b) records as *"not in canon and NOT
removed here"* — five of its TWELVE — with the disposition stated in terms,
*"their `## ADDED` blocks never promote HERE and their content is re-authored in
the receiving repository"*. Promoting any of them would put a requirement into a
capability the split packet's `## REMOVED` block cannot name and therefore cannot
remove: *"a removal of nothing"*.

The relocation is a `git mv` rather than the governed archive route for the
reason § 6.4's record states in full: `scripts/proposal-support.py … archive`
REFUSES this packet outright — `archive_change` raises `change has incomplete
tasks`, and § 4.4 travels open under RULING Q6 — and it exposes no non-promoting
mode if it ran. The pinned CLI underneath it DOES take `--skip-specs` (measured
at `1.12.0`): a real non-promoting archive mode, reachable only by going around
the wrapper, and not this closure's route.

## THE DELTA TRAVELS WHOLE, AND THE NARROWING IS NAMED RATHER THAN PERFORMED

This is the substantive judgement of this box, and it is recorded rather than
left in a diff.

**All five requirements travel to openDox-spec byte-identical** — 8,363 B,
`sha256 48b9a60c8de454e30041249fa1e638054b9a3e45bb061908d084d6ea89a2422c` on both
sides, `diff`-verified — and the narrowing requirement 2 owes is carried as a
BLOCKED OPEN BOX at the destination (`tasks.md` § 2.1), with a bar on ratifying
that requirement until it lands (§ 2.2).

Three reasons, in the order they bind:

1. **Splitting a ratified requirement's sentences is AUTHORING, not carriage.**
   RULING Q6 freezes the five where they stand. Every other § 6 closure moved
   ratified text unedited; performing an intra-requirement split here would make
   this the one closure that rewrote what it carried, in the act whose whole
   evidence is that it did not.
2. **The openxFactory half has nowhere to land today.** Under DQ-1 the drafter
   and route belong to `openxFactory`'s OWN ADAPTER, whose successor capability
   is this same packet's § 5.2a — **unbuilt** — and `corpus-adapter-seam` is an
   unpromoted delta of the same packet. A requirement authored against an
   undeclared capability would be authored again the moment it is declared. This
   is exactly § 6.1's position with its seven `doc-health` requirements, and it
   is stated the same way rather than quietly repeated.
3. **Carrying whole loses nothing and is reversible.** Dropping requirement 2
   would lose it from every destination at once; giving it to openDox whole
   without the box would assert openDox owns a `doc_health` drafter DQ-1 says it
   does not.

**What is satisfied absolutely either way: nothing promotes in `openxFactory`.**
The drafter half is not lost, not silently given to openDox, and not smuggled
into openxFactory canon by an archive — it is named as openxFactory's, owed to
§ 5.2a, here and in the receiving change's own tasks.

**If a ruling directs that the intra-requirement split be performed now rather
than named, the receiving change is re-done under it; this record is where that
decision is visible.**

## Pinned references, checked before the move

`grep -rn 'add-lens-document-selection' --include='*.py' --include='*.yaml'
--include='*.yml' --include='*.json'` over `cb2d3a2c` returns FOUR hits outside
the packet and **not one is a live coupling**:

- `tests/sequenced_after/corpus-ledger.yaml`'s row — which moves with this
  closure, and is the bookkeeping rather than a coupling.
- `tests/ideation-dashboard/test_staging_seed.py`'s module docstring, a citation
  by identity.
- `tests/doc-health/test_modified_block_currency_self_gate.py`'s § 6.3 note,
  which lists this change among those holding NO row in `_LEDGER_SUBJECTS` —
  true before this closure and true after it.
- `scripts/proposal-support.py`'s ORIGIN-RETENTION comment, a DATED measurement
  (*"Four of the thirty-five active changes on `main` at the time of writing"*)
  naming this change among four whose origin post-dates their ratifying commit.
  It stays true as written: a statement about a measured moment, not a live
  enumeration. Three of those four have now left the active corpus (§§ 6.4, 6.3
  and this box), and the comment's own *"at the time of writing"* is what makes
  that harmless rather than stale.

Every one survives unedited under `release-realization`'s *A packet reference
resolves by identity, not by path* (landed 2026-09-15).

**`contracts/openspec-cli-pin.yaml` carries NO disposition for this change and
`scripts/target-release-register.yaml` carries NO entry** — so neither § 6.4's
`pin-disposition-stale` refusal nor § 6.1's lockstep pair has a counterpart here.
Checked, not assumed. The register's silence is the right silence:
`proposal.md:3` declares `target_release: implemented`, the one value the
promoted sentence names outright, which needs no exception. **The README's
retiring active bullet said *"target release: none"***, which the packet's own
front matter contradicts; the archived entry reports the declaration, the same
correction § 6.4's record records for `tasks.md` § 6.4's shorthand.

## The promotion obligation, and its recorded discharge

As with §§ 6.4, 6.3 and 6.1: `document-lifecycle`'s *Ratified spec deltas reach
the promoted specification* attaches to the ARCHIVE ACT, this packet declares
`Status: ratified` and that header is not edited, and
`scripts/doc_health/promotion_fidelity.py` carries the obligation at
`_LAUNCH_SEVERITY = ERROR`. This closure takes the second discharge the checker's
own action text names — *"or record the non-promotion as deliberate"* — through
`health/dispositions.yaml` at the xFactory aggregation root, filed ONCE for the
five § 6 closures rather than five times. **That batch is not `tasks.md` § 6.6**,
which is the separate rule that no new dashboard change opens in `openxFactory`:
the gate this closure feeds is § 8.5 and the severity reading is § 8.9's.

**MEASURED.** `python3 -m scripts.doc_health.runner --single-repo . --family
promotion-fidelity` reads `0 error` at `main` `cb2d3a2c`, 1 at § 6.4's head, 6 at
§ 6.3's, 15 at § 6.1's and **20 at this one**: this packet contributes **FIVE**,
one per ADDED requirement, none of which is in canon:

> ratified ADDED requirement 'Every view of a document is one hover away from the others' is absent from openspec/specs/ideation-dashboard/spec.md
> ratified ADDED requirement 'A document selection drafts a staging-queue seed' is absent from openspec/specs/ideation-dashboard/spec.md
> ratified ADDED requirement 'A finding is stated before it is drawn' is absent from openspec/specs/ideation-dashboard/spec.md
> ratified ADDED requirement 'A panel in contested space carries one row of chrome' is absent from openspec/specs/ideation-dashboard/spec.md
> ratified ADDED requirement 'The theme owns every colour a view chooses' is absent from openspec/specs/ideation-dashboard/spec.md

One coarse path-keyed entry disposes all five, as § 6.3's does for its five.
A `--single-repo` self-gate run has no aggregation root and applies no
disposition, so that run reports them until § 5 removes the capability. Both
numbers are reported on the closing pull request; neither is suppressed.

## What travels open

`tasks.md` § 4.4 — *"Brett drafts and places the first staging seed from a real
convergence, and the fragment gets its INDEX.md row"* — was this packet's one
unticked box. **It travels OPEN, and it is the box that most needs its three
parts named**: the SELECTION is openDox's, the DRAFTING is `openxFactory`'s under
DQ-1, and the PLACING is a human act in `openxFactory`'s own `ideation/staging/`,
whose `INDEX.md` is this repository's file. It is carried at the destination with
that split stated (`tasks.md` § 4.1 there), because a box no single repository
can close is a box that must say so.
