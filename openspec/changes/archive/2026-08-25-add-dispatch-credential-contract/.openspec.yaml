schema: spec-driven
created: 2026-08-13
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:2026-08-13-add-dispatch-credential-contract
  reason: >-
    SWEPT BY RULING 2026-08-25, verbatim: "sweep the 23 derivable
    proposal-origin warnings". DERIVED FROM THIS PACKET'S OWN RECORD,
    never invented. The approval act is this packet's own lifecycle
    citation, quoted verbatim under approved_by below. The forcing fact is
    stated in this packet's own proposal under its Why heading, which this
    metadata-only edit leaves untouched. KIND IS ad_hoc, not staged: this
    packet records no Staging ID header, and no staging topic declares it
    as an exit in ideation/staging/INDEX.md — checked in both directions,
    because a topic that merely mentions a change is not that change's
    origin. The origin act is the approval quoted under approved_by. The
    created date 2026-08-13 is DERIVED from this packet's true first
    commit 4e4190cb in git, not from a folder name or a file timestamp.
    This declaration supplies the origin and approval pair the
    proposal-origin contract requires and changes nothing else about the
    packet: no lifecycle header, no content, no status.
  approved_by: >-
    Per this packet's own "Ratified" citation, quoted verbatim:
    "2026-08-13 by Brett Heap — the landing of PR `#168` (merged
    2026-08-13T18:46:52Z, merge commit 4e4190c) was the approval act this
    change's own task 1.2 conditioned on: the PR body states "Merging this
    ratifies the openXdox naming record (`draft` to `ratified`, its task
    1.2)", task 1.2 is ticked in tasks.md, docs/openxdox-naming.md carries
    "Ratified by `add-dispatch-credential-contract` (2026-08-13) ...
    landing that change is its lifecycle ratification", and the
    realization PRs `#171` (2026-08-13) and `#172` (2026-08-14) merged on
    main. Slice 5A of `govern-openspec-corpus-membership` first derived
    `draft` here on an incomplete hunt that missed all four of those
    record points; the value is corrected under Brett's 2026-08-23
    in-session ruling." — recorded in its proposal at authoring time. That
    is the approval provenance the ad-hoc exception requires, and it is
    why this packet fell in the derivable class rather than the stop class
    of the 2026-08-25 sweep.
  approved_on: 2026-08-13
