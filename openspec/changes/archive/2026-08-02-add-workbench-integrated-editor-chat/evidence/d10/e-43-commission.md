# Step E — First real propose commission (`add-propose-verb` 4.3)

Status: record · 2026-08-01T01:21Z (session of 2026-07-31)

## Precondition

The P5 topic reached the gate honestly: merged (PR #47 merge commit
`52153ce`, both documents on `main` and in the published snapshot) and
session-free (session ended via `abandon-session` after the merge-ending
observation was environmentally blocked — the engine's own prescribed
alternative; teardown report in `d-92-session.md`). Live health at the
click: ready · 0 blockers · 0.925.

## The commission

| Item | Value |
|---|---|
| Action | `▶ draft proposal` clicked by Brett on the `consent-instrument-contract` tile (main plane, serving `52153cedb09e`) |
| Panel report (verbatim) | `consent-instrument-contract → proposal authoring commissioned (proposal-authoring); the change arrives for review + ratify` |
| Gate-action record | `ideation/dashboard/gate-records/consent-instrument-contract/propose-20260801T012134Z.gate-action.yaml` — `actor: brettheap`, `action: propose`, target `consent-instrument-contract` |
| Commission descriptor | `ideation/dashboard/gate-records/consent-instrument-contract/propose-20260801T012134Z.workflow-job.yaml` — `workflow: proposal-authoring`, `gate_contract: workflow-gate`, `status: dispatched`, `dispatched_by: brettheap`, outline naming the full `add-consent-instrument` authoring deliverable |
| Commit sha | NONE — the propose verb records without committing; both artifacts land UNTRACKED in the served checkout (the same designed record placement as `open-pr`, see F10). Landing them on `main` is a follow-up governance commit; the runbook's "commit sha" slot is recorded as not-applicable-with-cause |
| First refusal (evidence of FR-023/D15) | The first click was REFUSED because the branch session was still live-in-registry: "commissioning over it would propose one thing while another is being written". Cleared by the engine-prescribed `abandon-session`; the refusal text is preserved in the session transcript |

## Clause verdict

Descriptor + record both present in the checkout, end to end, attributable
to Brett's single human action. The commissioned change is
`add-consent-instrument` — the real next change of the DTN-016 exit path,
not ceremony. Pass/Fail: Brett's sign-off cell.
