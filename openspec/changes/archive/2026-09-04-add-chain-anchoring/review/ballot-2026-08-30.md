# BALLOT — Combined §7.4 Council Review, 2026-08-30: openxFactory PRs #510, #513 and #509

Status: record
Record scope: **PUT TO BRETT HEAP. NO DISPOSITION IS CARRIED HERE.**

> **THIS BALLOT DECIDES NOTHING AND ADOPTS NOTHING.** Lines marked
> ***RECOMMENDATION (the convener's)*** are the convening's own, written before
> any ruling. **When Brett rules, the disposition of record will be §8 of the
> council review record**, and where that section and this ballot differ, that
> section governs.

For: **Brett Heap**, convener and reviewer of record
Council: `gate_rules_council` (codexFactory
  `hermes/domain/review-councils/gate-rules.yaml`), sitting in the **§7.4
  no-class shape** — put to you as **T-0(a)**, because the charter does not reach
  the object and the standing sanction was given for a sitting over ONE proposal.
Convened: 2026-08-30

```
openxFactory origin/main       afe29561a317853eb22f1e6b248f7029fa2fb2a7
shared merge-base (all three)  b710976b4a4fef277cdd292a407942369e9b9efa
PR #510 head                   e7f6ae0e400719894fbaa0d14be274ba86b2e3b3
PR #513 head                   cf5a24b89870a87663c595550210ee4deb0ac28e
PR #509 head                   26c7e778682089789795befad4c28af509ff2706
codexFactory (read-only)       3c71ddc910b8e0b119d0e1ee40a5523dfa294f07
contracts/manifest.yaml:3      contract_bundle_version: contract-v2.2
```

Packet: `openspec/changes/add-chain-attestation/review/convening-packet-2026-08-30.md`
Seat returns (verbatim): `openspec/changes/add-chain-attestation/review/seat-returns-2026-08-30/`
Records: `.../add-chain-attestation/review/council-review-2026-08-30.md` and
  `.../add-chain-anchoring/review/council-review-2026-08-30.md`

> **THIS BALLOT IS THE AUTHORITATIVE SCHEDULE.** The council records do not
> restate it, on the lesson the 2026-08-28 round wrote down: a record that
> *condensed* a schedule which then *competed* with the returns it condensed
> shipped **one fabrication and six omissions** and had to be rebuilt.
> **WHERE THIS BALLOT AND A RETURN DIVERGE, THE RETURN GOVERNS.**

> **NUMERIC CLAIMS APPEARING IN TWO RETURNS WERE RECONCILED BEFORE ENTERING THIS
> BALLOT**, on the 2026-08-29 sitting's closing instruction. Three seats measured
> tranche one's gate scenarios independently; all three returned **nine**. No two
> returns gave conflicting figures for the same object anywhere in this sitting.

---

## 1. THE HEADLINE — read this before the schedule

**NO SEAT REFUSED ANY SUBJECT. The threshold question was ruled NOT PREMATURE by
all three seats whose charter reaches it, so #510's round did NOT end. And the
bench is UNANIMOUS 4/4 that #510 as drafted is not ratifiable.**

**The decisive finding was reached BY CONSTRUCTION, and only by construction.**
`lead-architect` copied `invmain/openspec`, dropped #510's delta beside tranche
one's, and ran `openspec archive` **twice** to build the canon these two changes
would actually promote. The result is 18 requirements / 104 scenarios,
**mechanically clean — `openspec` reports `~ 0` modified — and holding two
contradictory scenarios on one antecedent**:

```
promoted canon :552   a chain carrying no attestation link
                      -> "validates links 1–3 … the absent later link is not reported as a break"
promoted canon :1141  the same chain, after this tranche
                      -> REFUSES
```

Both normative. Both promoted. **#510's prose that tranche one's scope note is
*"SELF-LIMITING and SPENT HERE"* cannot repeal a scenario living in a different
requirement** — and no validator in the estate notices, because each delta is
independently valid. LA-A1 is the repair: a scenario-complete `## MODIFIED` block
at **all nine** scenarios.

**A second, independent defect fell out of the same construction.** Tranche one
carries *"**THE LIST IS CLOSED** … every requirement of **this capability** is
either walked here or has its enforcement point named below"* — scoped to the
CAPABILITY, not the tranche. Its mapping table has **9 rows against 18 promoted
requirements**, so canon would assert that #510's own nine requirements are ones
*"this capability does not enforce"*. **#510 never touches that clause** — grep
returns zero. LA-A2.

**And #510 mints the one vocabulary the staged topic names as the family's FIRST
risk.** LA resolved all six cited authorities: five are genuinely CONSUMED (the
omnigent citation verifies byte-exact against the schema — six booleans,
`additionalProperties: false`, both consts false, five archetypes). The sixth is
minted: an evidence class whose member **`hardware_attested` is precisely the
`excluded_models: asserted_hardware_backing` entry that the ratified
`contracts/trust-anchor/trust-anchor-chain-custody.registry.yaml` rules out**
(*"hardware … belongs in `notes`, never a tier"*). Zero references to that
registry anywhere in #510. LA-A3.

**Two of #510's three closed Codex P1 fixes do not hold as applied** — reached
independently by `lead-security` and `lead-quality`, from different directions.
And the convening measured why that was likely: **Codex reviewed `bf021888` and
never saw the fix round `e7f6ae0e`** (§X.8). The primary precedent's sentence
applies unmodified: *"a prescribed fix applied without a verifier is an
unverified change, whatever its provenance."*

**What no seat attacked was either packet's purpose.** LA on #513: *"Its largest
decision (D8) is a faithful, well-recorded narrowing … it consumes the estate's
vocabulary and mints none."* LQ on #510: *"genuinely well-built — every count it
makes about itself is correct … it does not outrun its machinery anywhere I could
find, including under a live ruleset read."*

---

## 2. THE TALLY — one verdict per seat per subject

| Seat | #509 | #510 | #513 |
|---|---|---|---|
| **lead-architect** (LA) | ACCEPT AS AMENDED | **ACCEPT AS AMENDED** | **ACCEPT** |
| **lead-security** (LS) | **ACCEPT** *(disclosure limb only — LS-C2)* | **ACCEPT AS AMENDED** | ACCEPT AS AMENDED |
| **lead-quality** (LQ) | ACCEPT AS AMENDED | **ACCEPT AS AMENDED** *(recording, in the 2026-08-29 formula, that the text as drafted is **not ratifiable**)* | ACCEPT AS AMENDED |
| **company-policy-lead** (CPL, client) | ACCEPT AS AMENDED | ACCEPT AS AMENDED *(charter limb; conditional on T-1)* | ACCEPT AS AMENDED *(charter limb)* |
| ~~client-security-compliance-officer~~ | **NOT SEATED — and `lead-security` PARKS the question. See §6.2** | | |
| ~~intent-owner role slot~~ | vacant-symbolic, `binding: symbolic_until_project_roster` — not missing | | |

# **NO REFUSALS ON ANY SUBJECT. #510 IS UNANIMOUS 4/4 ACCEPT AS AMENDED.**

**`split_vote: park_for_liaison` is NOT engaged on any verdict** — this is the
first §7.4 sitting since the procedure was defined at which it is not.
`missing_required_seat: refused` is **satisfied**: the unconditional domain
roster is complete and the required client seat sat.

**BUT SLOT 2 IS ENGAGED ANYWAY, by a seat-level park** — `lead-security` parks
one item under its charter's standing `security_ambiguity → park_for_liaison`
must-not. **§6.2.**

**Blocking amendments: 16 across the three subjects.**

| Subject | Blocking | Held by |
|---|---|---|
| **#510** | **11** — LA-A1, LA-A2, LA-A3, LA-A5 · LS-A1, LS-A2, LS-A3 · LQ-A1, LQ-A2, LQ-A3, LQ-A4 | three seats; CPL holds none |
| **#513** | **3** — LS-A5, LS-A6 · LQ-A5 (its #513 limb) | two seats; LA and CPL hold none |
| **#509** | **3** — LQ-A5 (its #509 limb), LQ-A6, LQ-A7 | LQ only |
| **against the convening** | LQ-A15 (SHOULD-FIX) | §Y.2 |

### 2.1 Independence, and where it is weaker

**Three seats measured tranche one's gate scenarios independently and all three
returned NINE** (LA wrote its own counter; LS counted rather than read; LQ ran a
script). **They confirm the convening's §0.4 rather than adopt it**, and each says
so in terms. The same is true of §0.7: LA and LS and LQ each reproduced the
zero-hit grep.

**This is nonetheless the weakest independence in the sitting**, because those two
facts were carried INTO the packet the seats read (§Y.2). Brett should weight
them below the findings no seat was pointed at — LA-F1/F2 (the constructed
canon), LS-F1/F3 (the subtraction drives), LQ-F9/F10 (the collision and the
deferred leaf), none of which appears anywhere in the packet.

---

## 3. T-1 — THE THRESHOLD. **#510 IS NOT PREMATURE. THE ROUND DOES NOT END.**

**Ruled first, as #510's own `tasks.md:2.2` requires.** Three seats reach it;
CPL returns NOT MY SEAT and names `lead-architect`.

| Seat | Position |
|---|---|
| **lead-architect** *(whose question it is, per packet §4)* | **(c) NOT PREMATURE BUT NARROWED** |
| **lead-security** | **(a) NOT PREMATURE** on the security limb, with a caveat |
| **lead-quality** | **(a) NOT PREMATURE** on the testability limb, with a caveat |
| **company-policy-lead** | NOT MY SEAT → `lead-architect` |

**LA's reasoning, which is the ruling of the bench on this point** — it turns on
keeping three texts' OBJECTS distinct:

> The estate contains three texts and they are consistent **only if their objects
> are kept distinct** … Q4's object is the **boundary**; the Exit path's object is
> the **contract text** (*"may now NAME the mechanism"*). Drafting is permitted.
> But tranche one's ratified `tasks.md:5.3` fixes the trigger at "**when each is
> raised**" — #510 IS the raising, relocates it to realization, and never cites
> it.

**LA verified D7's supporting precedent holds** (`add-trust-anchor` stayed
conformant against a CA the family does not operate) — *"which is why (c) not
(b)"*. **The narrowing is LA-A5, and it is BLOCKING**: perform the raising-time
re-derivation now, cite `add-signed-execution-chain/tasks.md:5.3` by path and
text, state what exists TODAY, state the boundary derived from it, and keep
`tasks.md:5.1` as the SECOND, realization-time re-derivation rather than a
substitute for the first. **LA is explicit that this is an act of authorship, not
a re-ruling of Q4** — *"Q4 stays exactly as Brett ruled it."*

**The two caveats are not the same caveat, and both are blocking in their own
right.**

* **LS:** *"Writing a refusal down before its enforcer exists is how fail-closed
  design is supposed to proceed, provided the packet **declares** rather than
  **asserts** the enforcement — and #510 does declare … **The caveat is LS-F1**:
  the one place #510 asserts an enforcement its instrument declares it cannot
  provide."*
* **LQ:** *"the packet's own answer is to carry the obligation as an
  undischargeable gate at `tasks.md:5.1`, and **that gate is not decidable** …
  A packet that answers a prematurity objection with a gate owes a gate that can
  close."* (LQ-A13, SHOULD-FIX: name the observable that makes each plane "real"
  and the artifact the re-derivation produces.)

> ***RECOMMENDATION (the convener's): rule T-1 as (c), LA's narrowing.*** It is the
> only option that reconciles all three ruled texts without reading any of them
> down, it takes the seats' unanimous NOT-PREMATURE outcome, and it converts the
> one thing the packet actually skipped — the raising-time re-derivation tranche
> one's ratified tasks ask for — into a named, dischargeable act. **This is the
> convening's judgment, not a finding.**

**T-2 — a ruling on #510 does NOT bind #513. Unanimous among the three seats that
reach it** (CPL: NOT MY SEAT). LA verified the independence architecturally
rather than accepting the packet's word; LQ read #513's entire `spec.md` and
confirmed it cites **no requirement of #510**; LS: *"None of my #513 findings
depends on #510's fate."* **#513 and #509 stand on their own whatever is ruled on
#510.** LQ-C1 makes its #510 verdict conditional on T-1 and expressly preserves
its #509 and #513 verdicts.

---

## 4. #510 — THE ELEVEN BLOCKING AMENDMENTS

Each cites its return. Each names what would discharge it. **Where a discharge
names a verifier, that is the seat's own text, not the convening's.**

### 4.1 The promotion defects — LA-A1, LA-A2 *(reached by building the canon)*

**LA-A1 — Reconcile the two gate requirements with a MODIFIED delta, not with
prose.** *Discharged by* a `## MODIFIED Requirements` block over tranche one's
gate requirement, **scenario-complete at all NINE scenarios** (per PR #331's
promotion-fidelity lesson — a MODIFIED delta restating a subset silently drops
the rest), whose only substantive change is the `a tranche-two link does not
exist yet` scenario and the links-1–3 scope sentence. *Verifier:* re-run LA's
construction and confirm no two scenarios in the promoted file share an
antecedent with opposite consequents.

**This answers ballot B-2 against the packet**, and LA answers the packet's own
framing directly: *"the gate self-limits its scope note and NOT its closed-list
claim."*

**LA-A2 — Repair the orphaned closed-list obligation.** *Discharged by* either
(i) extending the mapping table to cover #510's nine requirements, or (ii)
re-scoping the clause from *"every requirement of this capability"* to the links
the requirement walks. **LA judges (i) the better repair.** *Verifier:* every
`### Requirement:` heading in the promoted file appears in the mapping table or
is named in a walked check.

### 4.2 The minted vocabulary — LA-A3

**LA-A3 — Compose the evidence class with the ratified assurance registry, or
drop `hardware_attested`.** *Discharged by* (a) a design decision and task naming
`contracts/trust-anchor/trust-anchor-chain-custody.registry.yaml` and stating
whether the evidence class is a distinct axis or a facet of it; (b) if distinct,
adopting that registry's own `composes_with` + declared-ordering pattern so the
two *"cannot drift into two custody models"*; (c) either justifying
`hardware_attested` against `excluded_models: asserted_hardware_backing`, renaming
it to what it actually asserts (independent observation), or dropping to two
classes.

### 4.3 The threshold narrowing — LA-A5

Stated in full at §3.

### 4.4 The fail-closed defects — LS-A1, LS-A2, LS-A3 *(reached by subtraction)*

**LS-A1 — Declare the revocation-at-exercise shortfall, or bind closure to an
instrument that can serve it.** LS's strongest finding: the P1-3 fix binds link-10
closure — the link **#510 itself calls *"the one with no gate behind it"*** — to
revocation standing *"current at exercise"*, drawn from
`governance/review-authority/register.yaml`, **whose own ratified header declares
a file-based register cannot satisfy revocation-at-exercise and *"must not be
pretended into a revocation surface"***. Zero hits in #510 for register /
revocation surface / live lookup / S5. *Discharged by* (a) a declared shortfall on
`add-trust-anchor`'s pattern naming the Hermes-runtime live lookup as what closes
it, or (b) requirement text conditioning closure on a surface that answers at
exercise. Cite `register.yaml:19-25`.

**LS-A2 — Give the attribution refusal a floor.** P1-1's mandatory refusal
(*"SHALL REFUSE a request it cannot so attribute"*) is undone 19 lines later by a
declared-shortfall escape; `grep "per-task attribution"` returns **two hits, both
inside the escape itself**. **The disqualified set has no members.** *Discharged
by* naming at least one use a declared shortfall disqualifies — specifically that
such a chain **SHALL NOT satisfy the links 1–6 gate for the terminal act** — or
that its link-5 facts carry a class weaker than `controller_corroborated`.

**LS-A3 — Make *"in every configuration"* reach REACHABILITY, not only
hand-over.** Subtraction showed every enumerated condition genuinely refuses when
the others are removed — *"the depth is real"* — but
`grep -E "co-locat|isolat|trust boundary|same host"` across all seven files
returns **zero**. A controller co-resident with a self-hosted runner hands nothing
across and is **conformant as drafted**. *Discharged by* one added condition and
one scenario: the tier-2 key SHALL NOT be readable from any host, process or
credential scope a worker, runner or lane executes within, and a co-located design
is REFUSED with the same force as the crossing refusals.

### 4.5 The testability defects — LQ-A1, LQ-A2, LQ-A3, LQ-A4

**LQ-A1 — Correct "twelve scenarios" to NINE at all four sites.** *"Owed
whichever way B-2 is ruled — if the repair is taken, a restatement briefed at
twelve that lands nine is itself a promotion-fidelity defect."* **(LA-A4 is the
same amendment at SHOULD-FIX; LQ holds it BLOCKING. The ballot carries both
weights rather than picking.)**

**LQ-A2 — Resolve the attribution collision.** Two scenarios (`:159`, `:177`)
give **opposite consequents on the same "cannot attribute" antecedent** with no
distinguisher in the requirement body. *Discharged by* stating the distinguisher
— a **request** the controller cannot attribute is refused at signing; a
**platform** that cannot attribute any request declares the shortfall and its
attestations are refused at use — plus one scenario fixing which branch governs
when both hold. **This is the same defect family as LA-F1, found independently in
a different requirement.**

**LQ-A3 — Close the deferred-leaf path.** *"Nothing orders link-5 leaves before
link-6's, so a lane that defers an unwanted leaf past its successor passes the
gate with an EQUAL enumeration. The P1-2 fix moved the attack rather than closing
it."* *Discharged by* requiring every link-5 leaf for a chain identity to be
written **before** that chain's link-6 leaf and refusing one sequenced after its
successor; **or** replacing the *"at or before the successor's own leaf"* bound
with a rule that no link-5 leaf for the identity exists outside the enumeration.
Plus one scenario.

> **NOTE THE CONFLICT, PRESERVED:** LA judged the P1-2 fix *"the sitting's best
> engineering … I could not break it."* **LQ broke it**, by a route LA did not
> take. **Both returns stand; the ballot does not reconcile them, because they are
> a disagreement of judgement resting on different attacks, not of measurement.**

**LQ-A4 — Give D6 something to rule on.** The revocation-after-signing horizon is
routed to this council at `tasks.md:2.5` and **has zero scenarios and no SHALL**.
*Discharged by* two scenarios under requirement 1 (revoked AT signing → refused
outright; revoked AFTER → what the gate already permitted stands, everything not
yet permitted refused) plus a `SHALL` on the revocation-standing sentence.

**B-4 is therefore unanswerable as posed** — the bench cannot rule a decision
that has no requirement text.

---

## 5. #513 — THREE BLOCKING, AND THE PRE-FLAGGED ITEMS ANSWERED

### 5.1 C-1 — THE TWO-ANCHOR NARROWING (D8). **The bench holds it CORRECT.**

**LA (the seat whose obligation it was): faithful.** Its #513 verdict is
**ACCEPT**, grounded first on this: *"Its largest decision (D8) is a faithful,
well-recorded narrowing of an UNRULED design observation that the mechanics make
literally unachievable."* **LS: correct and faithfully recorded on my limb.**

**The convening's own §0.6 correction is what makes this answerable**, and it ran
against the packet the convening was scrutinising: the anchor-late sentence sits
in the topic's `## Conflicts` section — whose second conflict is stamped
*"RESOLVED 2026-08-29"* and whose third is *"left OPEN by design"* — and **carries
no ruling stamp, no date and no disposition-by line**. It is an unruled design
observation inside a fully-ruled topic. #513 says exactly that in its own
requirement text (`spec.md:316`).

**No seat asked for the narrowing to be undone.** The one residual: the packet
recorded it and did **not** route it among its six council decisions (§0.6), which
is why it is on this ballot at all.

> ***RECOMMENDATION (the convener's): rule C-1 as (i) — correct and faithfully
> recorded — and note for the record that a packet narrowing a topic constraint
> SHOULD route it to the council rather than only record it.*** Recorded is not
> ruled; #513's own sentence — *"a topic's constraint is not a packet's to quietly
> reinterpret"* — is the argument for routing it, and the packet did not take its
> own advice.

### 5.2 C-2 — MISSING-WITNESS SEMANTICS (D2 / D-A). **Right in design; one blocking gap.**

**LS's answer, from driving it:** `455bbdaa` **genuinely fixed** the collision it
names — *"by the receipt's own rule, not an exception"* — and the asymmetry with
requirement 6 (a missing WITNESS degrades a CLAIM about the past; a missing
CONSENT ANSWER refuses an ACT in the present) is **deliberate and correct**. The
claim fails closed; the factory does not.

**But — LS-A5, BLOCKING:** the fix makes the receipt **stateless**, so *"a
one-entry receipt handed to an independent verifier — the flow requirement 1
exists to enable — is byte-indistinguishable from a one-witness configuration.
The fail-closed disclosure cannot travel with the artifact the design exists to
make independently checkable."* *Discharged by* requiring the receipt (or a
companion it commits to) to carry the **CONFIGURED witness set at mint time**.
LS notes this is configuration, not *"what has not happened yet"*, **so it does
not disturb the receipt/state split**.

**LS-A6, BLOCKING — Q6's two unspecified parameters.** #513 holds Q6 faithfully
and by checks a validator can really run, but `grep -E "entropy|random|unguessable"`
returns **zero**, and the **KEY** of the keyed commitment has **no custody rule**
where the salt has both a reference and a reachability refusal. *"Q6's erasure
property — the authority for the whole narrowing — rests on two parameters the
contract never fixes, against precisely the attack its own EDPB citation names."*
*Discharged by* a KEY custody reference on the salt's footing, plus a declared
minimum salt entropy (or CSPRNG source and width) with a refusal for a declared
construction that omits it.

### 5.3 C-4 / B-7 — NAMING. **No seat asked for a rename.**

Tranche one names both successors *"(working id)"* on their face, and **#513
records the divergence in a dedicated section** (`proposal.md:96-106`) while
**#510 does not** — grep for the long id returns zero in #510. LA holds the
back-citation at **SHOULD-FIX** (LA-A10). No seat treats the short ids as a
breach.

> ***RECOMMENDATION (the convener's): let the short ids stand and require the
> back-citation half.*** The ratified text's own *"(working id)"* admits the
> change; what it does not do is make the ratified packet resolvable to its
> successors. #513 already discharged this and #510 has not.

### 5.4 C-3 — the unratified-sibling dependency. **Honest and architecturally true.**

LA verified it rather than accepting it: #513's gates are Q3 and Q6, both open and
neither dependent on tranche two; its subject is reachable from tranche one's log
alone; **anchoring a checkpoint of that log requires no link 4**. LQ read the
whole `spec.md` and confirmed **no requirement of #510 is cited**. LA-A7
(SHOULD-FIX) asks only that the *"IN FLIGHT at this revision"* status come out of
**normative** text at three sites — *"promotion carries it verbatim"* — while the
commit's stronger half (*"the obligation does not depend on the citation … the
citation is PROVENANCE"*) stays.

### 5.5 C-5, C-6, C-7 — the packet's other routed decisions

* **C-6 (Q5's standing tension): held correctly.** LQ verified positively — *"Q5
  complied with, no 'never' and no trigger."*
* **D-F / the vendored study: the standing rule is respected.** LQ executed it:
  **#513 edits nothing in the staged topic directory, study included** (empty
  diff).
* **C-5 otherwise and C-7 (D1, D9 unrouted):** no seat found a defect requiring a
  ruling. LQ recorded #513 as *"the strongest-drafted of the three on my
  measures — 52 scenarios that all test system behaviour with **no
  assertions-as-scenarios**, where #510 has three."*

---

## 6. THE SITTING ITSELF — T-0, AND THE PARK

### 6.1 T-0(a) — was a §7.4 proposal review within this council's reach?

| Seat | Position |
|---|---|
| **lead-architect** | **(ii) it needs a chartered third body** — *"Reading the charter to reach proposal review makes `scope` decorative, and a charter whose scope line does not bind is the same defect this council rules against elsewhere."* **The act is nonetheless lawful for a different reason — the 2026-08-29 sanction — and I do not disturb it.** |
| **lead-security** | **(i) this council extends** — a bench owning `fail_closed_defaults` is competent over objects whose whole content is fail-closed rules, and *"refusing the seating would leave these three PRs with no bench at all, which is worse than a disclosed stretch."* |
| **lead-quality** | **(i) this council extends** — *"A proposal review is a branch review against review standards; it is the thing this seat is defined to do, whatever the council's `scope:` line says."* |
| **company-policy-lead** *(whose charter decision is literally `is_this_allowed_here`)* | **Lawful on the standing sanction — and ESCALATED.** *"a 'sanctioned pending amendment' status that is now reused a second time, unrenewed, while the promised amendment (#131) is still unlanded"* |

**2 for (i), 1 for (ii), and the client seat rules it lawful but escalates. The
2026-08-29 bench split the same way and Brett declined to choose a road; every
seat again flags that the sanction was given for ONE subject.**

**CPL-C2 is a standing condition on the convening pattern, not on any subject:**
*"Brett should close codexFactory issue #131 or issue an explicit renewed sanction
for multi-subject §7.4 sittings before a fourth one of this shape is convened
without either."*

### 6.2 T-0(b) AND THE PARK — SLOT 2 IS ENGAGED

**`lead-security` PARKS, under its charter's standing `security_ambiguity →
park_for_liaison` must-not.** This is the second consecutive sitting at which the
CSC ground is disputed by this seat, and it has now escalated from a dissent to a
park.

**LS-C4, verbatim:**

> I park **the client-layer exposure verdict the unseated
> `client-security-compliance-officer` would have carried**. I carried and
> discharged the **domain-layer** exposure reading as §3.2 assigned; I cannot
> supply a client-layer voice, and I will not wave one through by treating my own
> reading as covering both. **What I could not resolve:** whether a client-layer
> security-compliance voice is owed on a sitting that sets no rule — because the
> predicate that would have answered it returns `ConditionUnevaluable`, not
> `False`, and its own module says the caller must then refuse the rule or **park
> the convening**, with no rule here to refuse. **What would resolve it:** either
> (i) Brett seats the CSC for a supplementary reading limited to LS-F1, LS-F3,
> LS-F7 and LS-F8, or (ii) Brett rules on the record that no client-layer exposure
> voice is required for a §7.4 proposal review that sets no rule — which would
> also discharge the 2026-08-29 dissent that has now stood unresolved through two
> sittings. Either is a real answer; the present state, where the ground is
> recorded as *false* and the code says *unevaluable*, is not.

**The other three seats:**

* **LA — the decline was RIGHT IN OUTCOME; the honest ground is *inapplicable*,
  not *false*.** LA re-offers the repair Brett's 2026-08-29 ruling left undecided:
  *"the predicate has no subject — that is unevaluable, and `lead-security`'s
  reading of `seat_resolution.py:40-44` is correct as a matter of code."*
* **LQ — NOT MY SEAT**, recording only the mechanical fact that none of the three
  PRs touches any listed security surface.
* **CPL — policy-level support for the dissent**, not ruling the technical ground.

> ***RECOMMENDATION (the convener's): take route (ii) and rule the ground.*** Route
> (i) reopens a completed bench for four findings the domain seat already
> discharged on its own limb. What is actually broken is that the estate has now
> recorded *false* twice where its own code says *unevaluable*, and **LA has
> offered the exact wording that fixes it at no cost** — *inapplicable*, because
> the predicate's input contract is definition-time and no rule is being defined.
> **Ruling it would discharge a dissent standing through three sittings.**

### 6.3 T-0(c) — **ALL FOUR SEATS SAY THE COMBINATION COST THEM. UNANIMOUS.**

Asked to answer against the convening's interest, every seat did.

* **LA:** *"lawful but was the wrong instrument, and it did degrade my reading … I
  gave #509 materially less than I gave #510, and **I would not have found LA-F1
  had I paced all three equally** — it cost a build, two archives and a diff of
  the result."* LA's correction: **§0.9's argument argues for SEQUENCING #509
  before #513, not for one sitting.** The right instrument was **two** sittings —
  the chain pair together, #509 by ordinary PR.
* **LS:** *"Three subjects degraded my reading, and I can say where: I gave #509 a
  disclosure-limb pass rather than the line-by-line provenance drive I would have
  given a single subject, and **I did not drive #510's requirements 6–9 by
  subtraction as I drove requirement 4.** It was lawful but costly."*
* **LQ:** *"It did not degrade the mechanical half … **It did compress the
  judgement half.**"* — six requirements read in full against their bodies, the
  other twelve swept.
* **CPL:** *"**Yes, measurably, and I disclose it against my own return** … I
  sampled `design.md`/`spec.md`/`tasks.md` for #510 and #513 by targeted grep and
  section reads rather than reading either packet's ~1,700–1,900 lines end to
  end."*

**LS-F3's subtraction drive found a live defect on requirement 4. LS says plainly
it did not run that drive on requirements 6–9.** Brett should read the #510
security verdict as covering less than a single-subject sitting would have.

> ***RECOMMENDATION (the convener's): record that the combination was the wrong
> instrument, and split future sittings.*** This is the convening's own error and
> the bench is unanimous about it. **§Y.3.**

### 6.4 T-3 — was the drafting authorized? **CPL rules it; the others defer to CPL.**

**CPL's ruling, its seat:** *"a draft filed FOR REVIEW without a pre-existing
drafting authorization is a lawful object before this council, **provided the
packet says so plainly rather than implying otherwise** — both packets clear that
bar. Requiring authorization to *precede* the sitting as a hard rule would
collapse the review-before-ratify pattern this family has now used twice."*

LA, LS and LQ each return NOT MY SEAT and name CPL. LS adds that a draft creating
nothing, moving no key material and touching no `contracts/` byte *"is not a
security-relevant unauthorized act, whatever the governance answer."*

**All four seats recorded the packets' declining-to-claim-an-approver in their
FAVOUR.** CPL: *"genuinely exemplary, not merely adequate … #513 names the exact
prior overreach (tranche one's own corrected origin record) it is deliberately not
repeating."* CPL swept all **28** "Brett" citations across both packets: *"No
sentence reads as though Brett agreed to either packet's drafted content."*

---

## 7. #509 — THREE BLOCKING, AND THE INSTRUMENT QUESTION

**The bench converged hard here, and independently.** The mapping appendix — *"the
one part of a brainstorm that is not free-form"* — carries checkable falsehoods
about ruled dispositions. **LA checked 16 rows and found 13 true, 3 false. LQ
checked 17 targets and found 15 verify, with three checkable falsehoods and one
wrong figure. CPL found four verifiable inaccuracies. All three found the SAME
core three**, and none had seen the convening's own list (§X.3, withheld).

**LQ-A5 [509][513] — BLOCKING. "Eighteen months" is wrong, and #513 carries it
into normative requirement text.** 2024-11-10 → 2026-08-29 = **657 days**.
*Discharged by* correcting it at #509 `:102` and `:207` **and at #513
`spec.md:658-659`**, or striking the interval from #513's requirement text, which
does not depend on it. **CPL found this independently and traced the propagation
into #513's spec itself.** *(LA and CPL hold it SHOULD-FIX; LQ holds it BLOCKING.
Both weights carried.)*

**LQ-A6 [509] — BLOCKING. The disposition label is transplanted.** The appendix
attaches Q6's *"CONFIRMED … OPERATIVE FORM"* stamp to **Q2**, whose own status is
*"ruled … AS RECOMMENDED"*. *Discharged by* attributing the salted-keyed-commitment
ruling and the erasure property to **Q6**, and citing **Q2** for the boundary and
the refusing validator — *"which is what each actually says."* **Found by all four
seats.**

**LQ-A7 [509] — BLOCKING. Two invented names.** *"calls the commitments +
anchoring pattern"* and *"calls chain inception's fidelity property"* — **0 hits
repo-wide**, grep-verified by LA, LQ and CPL separately. *Discharged by* deleting
them or replacing each with a phrase that resolves.

**LQ-C2 — a condition, not an amendment.** The one existing imported brainstorm
carries a standing disclaimer — *"These notes are imported evidence and idea
material. They do not decide policy, memory, release scope, or OpenSpec
approval."* **#509 carries none.** *"That is a precedent and not a contract, so I
attach it as a condition: **either carry the disclaimer or record that it was
considered and declined.**"*

**LQ-C3 — the class question this raises.** *"LQ-F5 and LQ-F6 are exactly the
class no checker in this estate reads: a prose appendix asserting facts about
ruled dispositions and about what a document 'calls' something. Three of them
survived authoring, a bot round and the convening."*

### 7.1 A-5 — was a council the right instrument? **LA: no.**

**LA: *"a council was the wrong instrument — ordinary PR review, appendix called
out."*** The convening's own measurement agrees on placement: `find ideation -type
d -name review` returns **0**, and no brainstorm vendoring in this repository has
ever carried a review record.

### 7.2 What verified CLEAN on #509

* **doc-health zero-new and zero regressions**, measured with matched basenames
  per issue #342, by LQ and by the convening independently.
* **Lifecycle-header conformance against the code that enforces it** —
  `scripts/ideation_dashboard/authoring.py`'s `REQUIRED_HEADER_FIELDS` and
  `BRAINSTORM_SUFFIX`. LQ: *"more so than the one existing imported precedent."*
* **LS on its own limb: ACCEPT.** *"the vendored document discloses no credential,
  secret, key, PHI or identifier, and the unsafe 2024 architecture it records is
  explicitly corrected in its own appendix so it cannot be read as endorsed."*
* **CPL:** the dated section boundary (`## How MedxChain meets… — 2026-08-29`) is
  *"a clean, reusable pattern for future vendorings."*

---

## 8. THE SHOULD-FIX SCHEDULE — owed regardless

**LA:** A4 (twelve→nine, and it *"prices LA-A1's discharge"*) · A6 (constrain the
archive order: `add-signed-execution-chain` archives BEFORE
`add-chain-attestation`, since whichever archives first CREATES the capability —
LA demonstrated both orders succeed silently) · A7 (#513 in-flight status out of
normative text) · A8 (#509's three mapping defects) · A9 (consume R12 as a named
carry) · A10 (id back-citation).

**LS:** A4 (a fourth evidence class for the controller's own uncorroborated
self-report, settling half of `tasks.md:4.2`) · A7 (#513: bound capture by a
declared confirmation depth / finality condition) · A8 (#513: make the
anchor-state record derivable from and EQUAL to its evidence-plane leaves — *"the
same repair #510's P1-2 applied to enumerations"*) · A9 (resolve
`anchor_pending`/`anchor_incomplete` in the healthy path) · **A10 [X] — write a
floor under the declared-shortfall pattern**: *"Five load-bearing sites, no floor
anywhere."*

**LQ:** A8 (rewrite three reader-correction scenarios so each THEN names a system
consequent, on #513's pattern) · A9 (missing conjunct scenarios) · A10 (#513: a
scenario for the archival-node condition — the one of Q3's three with none) ·
A11 (#513: extend the capture-time refusal to all four per-chain elements) ·
A12 (#513: the aggregation Merkle path needs a scenario and a negative example) ·
A13 (**make `tasks.md:5.1` decidable** — *"the gate the packet offers as its
answer to T-1; an undecidable gate is a weak answer to a prematurity
objection"*) · A14 (#513: the 984 figure is stale — today **1032**).

**CPL:** A1–A6, all SHOULD-FIX, of which **A5** is the tenant-facing one: hedge
mid-entry present-tense operative language (*"takes effect"*, *"BECOME CONTRACT
TEXT"*) in both 35- and 83-line README entries, whose own opening and closing
sentences are already correctly draft-qualified.

**LS-A10 and LQ-A13 are the two the convening would flag to Brett as larger than
their SHOULD-FIX label suggests** — the first because the declared-shortfall
pattern is what LS-A1 and LS-A2 both turn on, and the second because it is the
packet's own answer to the threshold question.

---

## 9. B-5 AND B-6 — THE INHERITED SURFACE, AND R12

**B-5 — do the three closed P1 fixes hold as applied? TWO OF THREE DO NOT.**

* **LS:** P1-1's repair *"repairs the record, not the control: the exact shape D10
  says all three P1s had"* (LS-A2). P1-3's repair binds closure to an instrument
  that declares it cannot serve the requirement (LS-A1) — *"a bot-prescribed fix
  applied without running it against its instrument."*
* **LQ:** P1-2's repair *"moved the attack rather than closing it"* (LQ-A3).
* **LA:** P1-2 is *"the sitting's best engineering (log-derived set, EQUALITY both
  directions) — I could not break it."* **Preserved as a conflict with LQ; see
  §4.5.**

**And the convening measured the enabling condition** (§X.8): **Codex reviewed
`bf021888` and never saw the fix round.** `tasks.md:1.9` records the three as
*"CLOSED, AND RECORDED AS CORRECTIONS"* without stating who verified the closure.

**B-6 — must #510 consume R12?** **LS: yes** — *"it IS the attestation tranche and
R12 bears on LS-F4."* **LA: SHOULD-FIX not blocking** (LA-A9), because R12 landed
after the branch cut and *"the substantive convergence is already present in the
drafted text"* — R12's no-self-attestation rule and #510's *"BIND BEFORE SIGN …
never a notary"* are **the same principle reached twice**.

> ***RECOMMENDATION (the convener's): take LA-A9's shape — a named carry, not a
> blocking rewrite.*** The bench found the principle already honoured; what is
> missing is the citation and the disposition of R12's other three envelope
> questions.

---

## 10. X — CROSS-CUTTING

**X-1 — claims outrunning the machinery.** **LQ found NOTHING BLOCKING**, which is
the single most favourable finding in this sitting given the family's record. The
sweep classified every asserted gate, check, refusal and enforcement as DECLARED
or ASSERTED, and **the one ASSERTED live claim survives a LIVE RULESET READ**:
ruleset `21538893`, enforcement **active**, `~DEFAULT_BRANCH`,
`required_status_checks: wallet-validation, pytest-suite`. *"Everything else is
DECLARED with a cite."*

**X-2 — vocabulary consumed or reinvented?** **LS: consumed, not reinvented, on
every path I resolved.** **LA: #510 consumes five of six and MINTS one** (LA-A3);
**#513 mints nothing.**

**X-3 — two ADDED deltas into one unpromoted capability.** **LA demonstrated the
hazard rather than describing it**: archiving #510 first yields
`signed-execution-chain: create` carrying **only tranche two's nine
requirements**. **Both orders succeed silently.** LA-A6 constrains the order.

---

## 11. THE OPERATOR SLOTS

| Slot | Act | State |
|---|---|---|
| 1 | `human_step: acknowledgement_of_notice` | **OPEN.** The non-blocking notice — never a sign-off |
| 2 | Liaison disposition | **OPEN and ENGAGED** — not by a split (there is none) but by **LS-C4's park**, §6.2 |
| 3 | `output_disposition: convener_accepts_or_rejects_on_record` | **OPEN** |

**THIS RECORD IS NOT VALID UNTIL ALL THREE ARE FILLED.**

---

## 12. OPEN DECISIONS FOR BRETT — the convener's recommendations, marked as such

**Every line below is a recommendation. Brett decides.**

| # | Decision | ***RECOMMENDATION (the convener's)*** |
|---|---|---|
| **1** | **T-1, the threshold** | **(c) NOT PREMATURE BUT NARROWED**, LA's option. Unanimous NOT-PREMATURE among the three seats reaching it; (c) is the only reading that keeps Q4's *boundary*, the Exit path's *contract text*, and tranche one's *"when each is raised"* all standing |
| **2** | **#510's eleven blocking amendments** | **ACCEPT ALL ELEVEN**, one fix round, ratification read after. The bench is unanimous 4/4 that the text as drafted is not ratifiable, and every one names its own discharge |
| **3** | **#513's three blocking** | **ACCEPT ALL THREE.** None disturbs D8 or the receipt/state split; LS-A6 closes the two parameters Q6's erasure property rests on |
| **4** | **#509's three blocking** | **ACCEPT ALL THREE**, and rule LQ-C2 (disclaimer: carry or decline on the record). **These are the cheapest fixes in the sitting and the appendix is the one non-free-form part of a brainstorm** |
| **5** | **LQ-A5's #513 limb** | **Carry it into #513's requirement text**, not only #509's. It is the one place an error has already propagated from brainstorm colour into proposed contract text |
| **6** | **C-1, the D8 narrowing** | **(i) correct and faithfully recorded** — and record that a packet narrowing a topic constraint SHOULD route it, not merely record it |
| **7** | **T-0(b) / LS-C4's park** | **Route (ii): rule the ground.** Adopt LA's *inapplicable* wording. Discharges a dissent standing through three sittings at no cost |
| **8** | **T-0(a) and CPL-C2** | **Close codexFactory #131 or issue a renewed sanction.** Every seat flagged that a "pending" sanction has now been reused twice for a shape it was not given for |
| **9** | **T-0(c), the combination** | **Record it as the wrong instrument and split future sittings** — the chain pair together, #509 by ordinary PR. Unanimous, and it is the convening's error |
| **10** | **A-5, #509's instrument** | **Land #509 by ordinary PR review with the appendix corrected.** No `review/` directory: `find ideation -type d -name review` = 0 |
| **11** | **B-6 / R12** | **LA-A9's shape** — a named carry with the three remaining envelope questions dispositioned, not a blocking rewrite |
| **12** | **B-7 / naming** | **Short ids stand; #510 owes the back-citation** #513 already made |
| **13** | **LS-A10 and LQ-A13** | **Consider elevating both.** The declared-shortfall floor is what LS-A1 and LS-A2 turn on; the decidable gate is #510's own answer to decision 1 |
| **14** | **#509's open bot round** | **Not covered by anything above.** Three Codex P2s and a Copilot finding sit unaddressed on its only commit (§X.7), and **one of them lands on #513's requirement 8**. Direct them somewhere |
| **15** | **LQ-A15** | **Correct the convening's two figures in this ballot's own record** — done at §Y.2, and flagged here so it is ruled rather than absorbed |

**What this sitting did NOT decide, so no silence is read as a ruling:** the
SHOULD-FIX schedule item by item (§8); the LA/LQ conflict over P1-2 (§4.5),
preserved rather than reconciled; B-8 (whether a draft governance packet on `main`
commits anything) which no seat treated as decisive though **neither PR is a
GitHub draft** (§X.9); and every convening finding at §X, which are **not seat
amendments** and so are **not** covered by any "accept all blocking".

---
## X. WHAT THE CONVENING FOUND ITSELF — reproduced, and labelled as the convening's

**These are NOT seat amendments.** They were reached by the convening's own
execution and, except for two, were deliberately WITHHELD from the seat round —
injecting a convener-found fact into a seat round makes the convener a
participant. They are put here so Brett sees them BESIDE the returns rather than
through them. **Where a seat found the same thing independently, that is recorded
and it is the stronger evidence.**

### X.1 #510 says "twelve scenarios"; tranche one's gate requirement has NINE — asserted at FOUR sites

*(This one WAS carried into the packet, at §0.4, with an instruction to
re-verify. Ground: it bears on a question #510 itself asks the council — B-2's
ADDED-vs-MODIFIED choice — and the 2026-08-29 sitting recorded as its own fourth
error that it "asked B12 and did not itself execute the fix it asked about." The
departure from that sitting's withholding practice is disclosed in the record's
§"what the convening got wrong".)*

```
design.md:114   "Restating it would also cost twelve scenarios of verbatim carry"
design.md:124   "all twelve scenarios, not the two that change"
tasks.md:99     "all twelve scenarios, not the two that change"
proposal.md:243 "all twelve scenarios, not the two that change"
```

Measured at `origin/main` `afe29561` AND at PR #510's own head `e7f6ae0e` — the
two copies are byte-identical (`git diff` over
`openspec/changes/add-signed-execution-chain/` between them returns empty):

```
  scenarios per requirement, add-signed-execution-chain/specs/signed-execution-chain/spec.md
    5   6  Ratification presents wallet-carried authority, proven by possession
  103   4  The actor a chain records is bound to the wallet that signed
  151   6  Ratification and chain inception are one signed act
  241   5  One digest construction governs every digest this capability computes
  305   3  The signed ratification travels with the work
  337   4  The signed transparency log is the record
  391   9  A gate validates the short chain as a hash-linked chain   <-- THE SUBJECT
  557   4  Ratifying authority is human-held
  613   4  The capability confers and refuses nothing until a named reader runs as a required check
  TOTAL: 9 requirements, 45 scenarios.  No requirement carries twelve.
```

**Why it matters, stated without inflation.** It does not decide B-2, and D3's
*quotations* from tranche one were checked and are accurate. What it does is
**overstate by a third the cost of the alternative the packet argues against**, in
the one paragraph where that cost IS the argument — and the same paragraph
invokes the `#329`/`#330` scenario-loss class as the reason a restatement's count
must be exact.

### X.2 Tranche one's RATIFIED task list fixes the re-derivation trigger at "WHEN EACH IS RAISED", and neither packet cites it

*(Also carried into the packet, at §0.7, on the same ground: it bears on T-1,
which #510 itself routes to the council.)*

`openspec/changes/add-signed-execution-chain/tasks.md:187-190`, on main, in a
RATIFIED packet:

> - [ ] 5.3 Neither successor's content enters this packet. **Re-derive the
>   tranche two/three boundary against what actually exists when each is
>   raised**, per the topic's Q4 — a tranche that depends on an unbuilt layer is
>   a plan, not a tranche.

**#510 IS the raising of tranche two; #513 IS the raising of tranche three.** #510
reads the trigger as REALIZATION and carries it to `tasks.md:5.1`. Grep for *"when
each is raised"* and *"task 5.3"* across both packets' `proposal.md`,
`design.md`, `tasks.md` and `.openspec.yaml`: **zero hits in all eight files.**

**Its limits, stated:** tranche one's `tasks.md` is a task list, not a
requirement; Q4's disposition itself names no binding moment beyond *"not fixed
now"*; and the staged topic's Exit path still says tranche two's *"contract text
may now NAME the mechanism"*. **But it is the only text in the estate that fixes a
trigger, it sits in a ratified packet, and it says "when each is raised."**

### X.3 #509 — SIX estate-facing claims that do not hold. WITHHELD from the seats.

**The distinction this section turns on:** Brett's own 2024 speculative content
(lines 26–99) is legitimate brainstorm material and is **not** judged for
correctness — `docs/document-lifecycle.md:41` makes `brainstorm` *"Non-normative;
contradiction legal"*. What must be true is the **vendoring author's framing
claims about this estate**, in the 2026-08-29 mapping appendix (lines 100–223).
Six do not hold. All six measured at `origin/main` `afe29561`.

| # | The claim | Measured | Verdict |
|---|---|---|---|
| 1 | `:115` — *"the pattern `signed-execution-chain` calls the **commitments + anchoring** pattern"* | `"commitments + anchoring"`: **0 hits in the topic dir, 0 repo-wide** | **INVENTED NAMING** |
| 2 | `:126` — *"the same move as `signed-execution-chain`'s **plane separation**"* | `"plane separation"`: **0 in the topic dir**; 3 repo-wide, all in unrelated domain-ontology docs | **INVENTED NAMING** |
| 3 | `:131` — *"the property `signed-execution-chain` calls **chain inception's fidelity property**"* | `"fidelity property"`: **0 repo-wide.** `"chain inception"`: **0 in the topic dir**, 12 repo-wide — all in the ACTIVE CHANGE, not the staged topic the appendix declares as its citation surface (`:108`), and there it means registry registration minting the chain identity, not a verify-without-the-original property | **INVENTED CONCEPT + MISPLACED SOURCE** |
| 4 | `:146` — *"**Q2**, ruled 2026-08-29 (**CONFIRMED operative form**)"* | `signed-execution-chain.md:568` — Q2's status is *"ruled 2026-08-29 — Brett Heap, in session; **AS RECOMMENDED**"*. `:753-754` — *"**CONFIRMED** 2026-08-29 … and this is the ruling's **OPERATIVE FORM**"* is **Q6's** label | **RULING MISATTRIBUTION — Q2's identity carries Q6's label** |
| 5 | `:213-217` — *"**The ruled architecture** subsumes content-addressing generically under its commitment scheme … so IPFS is not a separate primitive to carry forward"* | `IPFS`, `content-address*`: **0 hits in the topic dir.** Nothing in the topic, the study, the change packet or the ratification record rules on content-addressing or IPFS | **UNSOURCED RULE ATTRIBUTED TO "THE RULED ARCHITECTURE"** |
| 6 | `:103`, `:204-205`, and the commit message — *"**eighteen months**"* | 2024-11-10 → 2026-08-29 = **657 days = 21.6 months** | **ARITHMETIC — understated by ~3.6 months** |

**Three further items are extrapolations spoken in the estate's voice** rather
than flatly false, and are listed separately because they may be the author's own
reading rather than a claim about the record: `:167-168` *"anchors only batched
checkpoints"* applied to the TRANSPARENCY log (the topic anchors consent-log
checkpoints; batching is an OpenTimestamps property from the study); `:157-158`
*"consent state **and identifiers**"* (the topic's table says *"consent state, and
the salts"*; `identifiers` appears nowhere in the topic dir); and `:136-137`
describing the transparency log as *"a signed, append-only record of who touched
what, when"* (the topic's leaves are ratifications, attestations, verdicts and
merge decisions — governed acts, not access events).

**One omission, not a false claim:** `:103-104` says the clarify sitting *"ruled
that topic's **Q2, Q3, and Q6**"*. It ruled **all seven** — `:501` is headed
*"## Open questions — all seven SETTLED 2026-08-29"*. Accurate as far as it goes,
materially incomplete.

**And one thing the appendix never records:** it speaks throughout of the *staged
topic* and *"any future MedxChain build"*, and never notes that tranche one was
**ratified and raised as an active change on the same date the appendix is
stamped**.

### X.4 #509's provenance statement is internally self-limiting

`:16-24` asserts both *"no content reworded"* and that the vendoring
*"reconstructs the notes' section structure and enumerated claims from that
transcript **rather than reproducing byte-identical prose**"*. Those two
sentences do not describe the same act. The convening records this as a finding
about the document's own fidelity claim, not about its good faith — the second
sentence is a disclosure the first would otherwise have obscured.

**And the OneNote original is not in the repository**, so no claim about
Brett's lines 26–99 is checkable in principle. That makes the appendix's
estate-facing claims (X.3) the only checkable surface — which is why they carry
the weight they do.

### X.5 #513 — two small internal defects, and one bot finding still open

* **`proposal.md:87` says "Two obligations carried from a named source"**; the
  section it summarizes is headed *"## The three obligations carried from the
  MedxChain notes"* (`:145`), enumerates three, and is cited as "three" at
  `proposal.md:233`, `tasks.md:59-63`, `README.md:456-461` and
  `INDEX.md:1931`. `:87` is the lone outlier.
* **A Copilot finding raised three separate times is unaddressed at the branch
  head** — `README.md:461`, *"(MedxChain/HealthLinc are MedxFactory's, LedgerLinc
  LedgerxFactory's;"* — a missing verb on the tenant-facing README surface.
  Verified still present at `cf5a24b8`.

### X.6 The convening reproduced issue #342 while measuring #509, and reports it

A first doc-health run comparing worktrees named `invbase` and `inv509`
manufactured **15 phantom "new findings"**, because doc-health derives `repo`
from the checkout BASENAME and every finding therefore read as
resolved-and-re-added. That is openxFactory issue **#342** exactly, reproduced
accidentally. **The run was discarded, not reported.** The valid run — identical
basenames under different parents — returns `[]` new findings and a clean
report:

```
new-findings: []
report diff: canon share 31.7% -> 31.6%; brainstorm 178 -> 179 docs (+1697 words)
headline: 4 critical, 4 error, 38 warning, 11 info. New regressions vs previous report: 0.
```

**#509 introduces zero doc-health findings and zero regressions**, and the file
is never named in the report. Recorded in the change's favour.

### X.7 #509 CARRIES AN ENTIRELY UNADDRESSED BOT ROUND — three Codex P2s and a Copilot finding, all open

Measured against the GitHub API. **PR #509 has exactly ONE commit, `26c7e778`,
and all three bot reviews are against that same commit.** There is no fix round.
**Every finding is open at the head this council judged.**

```
sourcery-ai[bot]                    commit=26c7e77868  COMMENTED   (upsell stub)
copilot-pull-request-reviewer[bot]  commit=26c7e77868  COMMENTED   1 finding, open
chatgpt-codex-connector[bot]        commit=26c7e77868  COMMENTED   3x P2, all open
head = 26c7e778  (the only commit)
```

**Codex P2 — "Limit verification to parties holding commitment secrets"**
(`:133`): under the Q2 architecture the appendix itself maps onto, the public
value is a KEYED commitment whose salt stays in the governed layer, so *"a later
holder with only the record and public anchor cannot recompute and verify it.
This sentence carries over the public-verification property of a bare hash even
though the privacy upgrade removes it."*

**Codex P2 — "Keep clinical access events distinct from the execution log"**
(`:166`): the appendix claims the existing transparency log already retains
granular access events, *"but the ratified requirement records only wallet
ratification, chain inception, traveling-contract issuance, and gate verdicts
(`add-signed-execution-chain/specs/signed-execution-chain/spec.md:339-344`)"*.
**This independently corroborates the convening's own extrapolation finding
(X.3, item 9 of the three extrapolations) by a different route**, and states the
consequence the convening did not: the mapping *"incorrectly makes required
access-audit events appear implemented by the execution log."*

**Codex P2 — "Require de-identification before treating metadata as reusable"**
(`:190`): querying MetaDataDB without PIIDB does not make a result
de-identified, and *"the repository's medical-data model explicitly requires a
named Safe Harbor or Expert Determination de-identification gate
(`docs/knowledge-lifecycle-model.md:131-134`), so describing this analysis lane
as already supported could let tranche 3 bypass that privacy boundary."*
**Note the reach: this is a finding about #509 whose stated consequence lands on
#513**, whose requirement 8 is the meta-analysis lane and which cites this very
file as its provenance (packet §0.9).

**Copilot** (`:150`): the appendix says it *"ruled that topic's Q2, Q3, and Q6"*
and cites by question anchors, *"but the body only cites Q2/Q3; Q6 is only
mentioned in the intro."* **This reaches the convening's own X.3 omission finding
from the opposite direction** — the convening found the Q-list understated the
sitting; Copilot found the body under-delivers on the Q-list it states.

### X.8 #510's THREE CODEX P1s WERE FIXED AND THE PRESCRIBER NEVER RE-READ THE FIXES

```
sourcery-ai[bot]                    commit=bf0218883e
copilot-pull-request-reviewer[bot]  commit=bf0218883e
chatgpt-codex-connector[bot]        commit=bf0218883e   <-- the three P1s
copilot-pull-request-reviewer[bot]  commit=e7f6ae0e40   <-- the fix round; COPILOT re-read
commits: bf021888  ->  e7f6ae0e (head, "Fix round: three records that named what they had to establish")
```

**Codex reviewed `bf021888` and nothing after it.** The fix round that took its
three P1s is `e7f6ae0e`, and Codex did not see it. Copilot did re-review the
head, which is real verification by a different reader and is not the same
thing.

**This is the 2026-08-29 sitting's central lesson recurring, one week later, in
the very next packet** — and the difference matters: there Codex was
PROVIDER-REFUSED and the packet disclosed it at §0.2; here Codex was simply not
re-invoked, and **neither the packet nor `tasks.md:1.9` discloses that the
prescriber never re-read the repairs.** `tasks.md:1.9` records the three findings
as *"CLOSED, AND RECORDED AS CORRECTIONS"* without stating who verified the
closure.

The precedent's own sentence applies without modification:

> **A prescribed fix applied without a verifier is an unverified change, whatever
> its provenance.**

The three P1s, for the record, were: *"Authenticate and bind the signing
request"* (`spec.md:102` — recording a request does not establish who made it);
*"Anchor the complete runner set before checking subsets"* (`:344` — the gate has
no authoritative set to take a subset OF); and *"Verify the review record before
allowing closure"* (`:426` — binding link 10 to review-record BYTES establishes
integrity, not that council seats produced them).

### X.9 #510 IS NOT A DRAFT ON GITHUB, THOUGH ITS TEXT AND TITLE SAY DRAFT

`gh pr view 510 --json isDraft` returns **`false`**. `proposal.md:4` reads
`Status: draft`, `tasks.md` opens *"**This change is a DRAFT.**"*, and the PR
title ends *"(draft, for §7.4 review)"*. #513 is likewise `isDraft: false` while
its `proposal.md:8` reads `Status: draft`.

Recorded without inflation: the 2026-08-29 precedent's subject was a GitHub
DRAFT and its record leaned on that (*"The pull request is a DRAFT and remains
one"*). **These are not.** All three carry `reviewDecision: REVIEW_REQUIRED` and
three green checks (`merge-master-approval`, `wallet-validation`,
`pytest-suite`), so nothing merges without a review — but the mechanical
draft-state guard the precedent relied on is absent here, and ballot **B-8** is
where that lands.

---

## Y. WHAT THE CONVENING GOT WRONG, on its own record

Recorded here rather than left for a fidelity audit, on the 2026-08-28 and
2026-08-29 precedents. The body's standard is that a record which lists only
others' defects misreports what happened.

1. **THE PACKET'S FIRST DRAFT CALLED THE ANCHOR-LATE CONSTRAINT "RULED". IT IS
   NOT.** The convening wrote §0.6 and §1.2 describing #513's D8 as narrowing *"a
   constraint written into the ruled staged topic"* and *"a ruled constraint"*.
   Measured afterwards: the anchor-late sentence sits in the topic's
   `## Conflicts` section, which holds three conflicts — the second stamped
   *"RESOLVED 2026-08-29"*, the third *"left OPEN by design"* — and **the
   anchor-late one carries no ruling stamp, no date and no disposition-by line**.
   None of the seven Q-dispositions reaches it. The topic is fully ruled as to
   its seven questions and unruled as to this constraint, and **#513 says so in
   its own requirement text** (`spec.md:316`) while the convening's first draft
   did not. **Corrected in the packet before the seats read it**, with both edges
   given, because the error ran AGAINST the packet under review and correcting it
   only in the ballot would have let a mischaracterization do work in the seat
   round. **Mine.**

2. **THE CONVENING CARRIED TWO OF ITS OWN FINDINGS INTO THE SEAT PACKET, WHICH
   THE PRIMARY PRECEDENT DELIBERATELY DID NOT DO — AND THE PRECEDENTS CONFLICT
   ON THIS POINT.** The 2026-08-29 sitting WITHHELD its own `#329`/`#330`
   finding from the seats, on the stated ground that *"injecting a convener-found
   fact into a seat round makes the convener a participant"*, and sent it to the
   ballot. The same sitting's §9.4 then records as its own **fourth error**:
   *"The packet asked B12 and did not itself execute the fix it asked about. Had
   the convening built the prescribed schema when it wrote §0.2, the decisive
   finding would have been in the packet rather than in a supplementary round."*
   **Those two instructions point opposite ways and the convening had to choose.**

   The line taken, stated so it can be judged: **a mechanical fact bearing on a
   question the SUBJECT ITSELF routes to the council goes into the packet,
   labelled as the convening's and marked for re-verification; a convener finding
   the subject did not ask about is withheld and goes to the ballot.** On that
   line §0.4 (the twelve-vs-nine count, bearing on #510's own B-2) and §0.7 (the
   "when each is raised" trigger, bearing on #510's own T-1) were carried; **X.3
   through X.9 were withheld.** The packet disclosed the withholding at §3.4 —
   *"the absence of a finding in this packet is not evidence that there is none"*
   — so no seat could read the packet as complete.

   **The cost is real and is not argued away**: two findings the bench might have
   reached independently were handed to it, and the independence of any seat
   agreement on those two is correspondingly weaker. **All three domain seats
   re-measured rather than adopted** — LA wrote its own counter, LS counted
   rather than read, LQ ran a script — and each said so explicitly, which is the
   best available mitigation and is not the same as independence. Brett should
   discount accordingly (§2.1).

3. **THE CONVENING PUT TWO UNMEASURED FIGURES INTO THE PACKET THE SEATS READ, AND
   `lead-quality` CAUGHT BOTH.** LQ-F2 and LQ-A15, verbatim: *"The convening said
   it may have found only one wrong number. **It found one and made two.**"*

   | The convening's claim | Where | Measured | |
   |---|---|---|---|
   | *"#510 cites `add-wallet-carried-review-authority` **five times**"* | packet §0.5 | **7** over the four files named (2/2/2/1); **8** with `spec.md` | **CONVENING WRONG, low by 2** |
   | *"#513 declares the relation as 'Sequencing, not blocking' at **four sites**"* | packet T-2, §1.2 | the literal phrase returns **2** (`design.md:415`, `tasks.md:177`); a third site says *"Sequencing rather than blocking"* | **CONVENING WRONG, high by 2** |

   **Re-verified by the convening against LQ's return before this ballot was
   written, and LQ is right on both.** Both substantive conclusions survive
   unchanged — neither packet cites R12, and #513 does declare the relation
   non-blocking — but the figures were carried from a research brief rather than
   counted, **which is the identical failure the convening had just written down
   as X.1 about somebody else's packet.**

   **The packet is NOT edited to fix this.** It is the evidence of what the seats
   actually read, and correcting it now would falsify that record — the same
   discipline the 2026-08-29 ballot applied when it preserved its own
   eight-versus-four error and disclosed it rather than tidying it. **The
   correction lives here and in the council records. LQ's measurements govern.**

4. **THREE SUBJECTS IN ONE SITTING WAS THE CONVENING'S CHOICE, AND THE BENCH IS
   UNANIMOUS THAT IT COST THEM.** Every §7.4 precedent sat over ONE object, and
   the sanction Brett gave on 2026-08-29 was for a sitting over one proposal. The
   convening put it to the bench as T-0(c) precisely because it could not judge
   its own instrument. **All four seats answered against the convening**, and one
   of them quantified the damage in the sharpest possible terms:

   > **LA:** *"I would not have found LA-F1 had I paced all three equally — it
   > cost a build, two archives and a diff of the result."*

   **LA-F1 is the decisive finding of this entire sitting** (§1). On LA's own
   account it survived only because LA gave #509 less than it deserved. **LS did
   not run its subtraction drive on #510's requirements 6–9** and says so; LS-F3,
   which found a live defect, came from that same drive on requirement 4. **LQ
   compressed the judgement half. CPL sampled both 1,700+ line packets rather
   than reading them.**

   **The one argument in favour emerged only during preparation** — #513's
   requirement text cites #509's file at three normative sites (§0.9) — and **LA
   corrected even that**: it argues for **SEQUENCING #509 before #513, not for
   one sitting.** LA names the right instrument: two sittings, the chain pair
   together, #509 by ordinary PR.

   **The convening accepts this finding in full.** It is the clearest thing the
   bench proved about the convening rather than about the packets.

5. **THE PACKET'S "EVERY SEAT" ROW NAMED #510's AND #513's BOT ROUNDS AND OMITTED
   #509's.** The convening later measured that **#509 carries three unaddressed
   Codex P2s and an open Copilot finding on its only commit** (X.7). The seats
   were told to attack the inherited bot fixes on the two chain packets and were
   told nothing about #509's open round. That is an asymmetry in the instruction,
   not a withholding decision taken on principle — **the convening had not yet
   run the query when the packet was written.** Any seat that missed #509's
   defects was working from an instruction that did not point at them.

6. **THE PACKET ASSERTED A "FIFTH CONSECUTIVE CONVENING" FOR R26 BEFORE THE
   CONVENING HAD CHECKED THE ORDINAL — it has since checked it, and it holds.**
   The resolver failure itself was executed at codexFactory `3c71ddc9` and the
   `ConditionError` reproduced, so the FACT was always measured. The ORDINAL was
   at first carried from the previous record's "fourth" plus one, which is exactly
   the inherited-number habit this bench's own rule refuses. Traced afterwards
   through the estate's own headings:

   ```
   2026-08-28-gate-rules-openxfactory-substantive-classes.md:192
     "### 4.1 The shipped resolver cannot read this council's document — third consecutive convening"
   2026-08-29-council-review-add-binding-consumer-identity.md:228
     "### 4.1 The shipped resolver cannot read this council's document — FOURTH consecutive convening"
   ```

   **Third, then fourth, so this sitting is the fifth. The ordinal is correct.**
   It is left in this section anyway, because *"a number nobody executed is a
   number nobody has"* was true of it at the moment it was written into the
   packet the seats read, and a figure that turns out right is not the same as a
   figure that was checked.

7. **THE ARTIFACTS ARE PLACED IN THE WRONG REPOSITORY BY THE PRECEDENT'S OWN
   SHAPE, AND A MIRROR IS OWED.** The 2026-08-29 sitting put its packet, returns,
   ballot and council record in codexFactory `hermes/domain/review-councils/`,
   keeping the council's records in one place across sittings. This sitting could
   not write there and put everything in openxFactory instead. **The convening
   states plainly that this fragments the council's record**, that the mirror is
   an obligation rather than a nicety, and that #510's change directory was
   chosen as the physical home for a REASON (it is the threshold subject) and not
   because that placement is neutral.

8. **THE CONVENING REPRODUCED ISSUE #342 ON ITSELF WHILE MEASURING #509** (X.6) —
   a first doc-health comparison across differently-named worktrees manufactured
   fifteen phantom regressions. **It was caught and discarded rather than
   reported**, and the valid run is the one in the ballot. It is recorded because
   a harness that agrees with the answer you expected is the defect this body
   keeps finding, and this one disagreed loudly enough to be caught. **A quieter
   version of the same error would have shipped.**
