# Archive on its own evidence: retire-doxbench-chat-turn-v1

Status: record
Kind: report
Closure date: 2026-09-16 (UTC)
Closed by: [openxFactory PR #1066](https://github.com/opensoft/openxFactory/pull/1066)
Authority: `split-opendox-two-layer-product` `tasks.md` § 6.2 and `design.md`
§ D9 — *"archives in `openxFactory` on its own evidence, and only its FORWARD
half — the surviving `-v2` family's requirements — re-homes to openDox"*
Forward half: `opensoft/openDox-spec`, change
`openspec/changes/carry-doxbench-chat-turn-v2-family`, PR
[openDox-spec #15](https://github.com/opensoft/openDox-spec/pull/15)
Lane: openxfactory-4 (openXfactory-4-openDox_extraction); CLAIM
[#656 5698423421](https://github.com/opensoft/openxFactory/issues/656#issuecomment-5698423421)
Siblings: § 6.4 (PR #1056), § 6.3 (PR #1057), § 6.1 (PR #1060), § 6.5 (PR #1065)

## THIS IS NOT ONE OF THE FOUR, AND THE DIFFERENCE GOVERNS EVERY ACT BELOW

The packet's own `specs/ideation-dashboard/spec.md` says **"under RULING Q6 those
FOUR changes stop where they stand, so their `## ADDED` blocks never promote
HERE"** — four, not five. This is the fifth, and it is the exception the sentence
is counting around. It is not frozen and it is not closed as re-homed: its
realization **merged in this repository**, its `contract-v3.0` cut **is
published**, and what it asserts is a fact about openxFactory's own released
contract surface. So it archives the ordinary way, and **its delta PROMOTES.**

Two consequences follow, and both are measured further down rather than asserted:

1. **The promoted specification gains all three blocks** — `openspec/specs/`
   `ideation-dashboard/spec.md`, **+186 lines, 0 deletions** — **157** the
   promotion, every byte taken from the ratified delta by script, and **29** the
   dated ERRATUM placed in the promoted specification immediately after the
   first requirement, marked in its own first line as no part of the ratified
   text: one clause of that requirement names `typed_proposal` as reachable from
   the surviving `-v2` family, which this packet's own `tasks.md` § 2.1 measured
   FALSE and its § 6.8 dispositioned (RETAINED, unreferenced). The clause stands
   unedited because it is ratified; the correction stands beside it because
   promoting it moved it into canon, where nothing else would. The figure read
   157 until that erratum was added.
2. **There is NO `health/dispositions.yaml` entry for this packet**, and there
   must not be. The aggregation companion
   ([opensoft/xFactory #454](https://github.com/opensoft/xFactory/pull/454))
   disposes the FOUR re-homed closures' non-promotion as deliberate and carries a
   header comment forbidding a fifth by analogy. This one's promotion obligation
   is discharged by **promotion**, which is the only discharge the
   `document-lifecycle` rule *"Ratified spec deltas reach the promoted
   specification"* actually prefers.

**AND ONE BLOCK CAN ONLY LAND HERE.** Of the delta's three requirements the
forward half carries two. The third — *The doxBench chat-turn v1 envelope family
is REMOVED at contract-v3.0* — does not travel, because openDox removes nothing
and never served the v1 family. Had this packet closed the way the other four
did, that ratified requirement would exist in no promoted specification anywhere.

## The mechanical form, and why the governed route is not available

`git mv openspec/changes/retire-doxbench-chat-turn-v1 openspec/changes/archive/`
`2026-09-16-retire-doxbench-chat-turn-v1` (six files, `.openspec.yaml`
included), then the promotion by script, then the README ledger, then the sweep
ledger.

**`scripts/proposal-support.py archive` cannot be used**, and the reason is not
the one the four sibling records first gave. Its `archive_change` raises **`change
has incomplete tasks`** on any `- [ ]` line *before* it ever resolves the CLI, and
this packet has seven. The pinned CLI itself (**1.12.0**,
`contracts/openspec-cli-pin.yaml`) does expose `--skip-specs`; that is not the
obstacle and the sibling records were corrected to say so. Here `--skip-specs`
would be the wrong flag anyway: **this closure WANTS the promotion.**

**Pinned-reference sweep, run before the move** —
`grep -rn "retire-doxbench-chat-turn-v1" --include='*.py' --include='*.yaml'
--include='*.yml' --include='*.json'` over the tree. Thirty hits outside the
packet, **exactly one of them a live coupling**: `tests/sequenced_after/`
`corpus-ledger.yaml`'s row, moved by `--seed-ledger` below. The rest are
historical prose in test comments (`test_doxbench_contracts.py`,
`test_doxbench_routes.py`, the two openXdox submodule mirrors) that name the
change as the AUTHORITY for a re-pin already taken; they are correct as written
and moving the packet does not falsify one of them.

**`_LEDGER_SUBJECTS` does NOT move, and that was predicted in the tree.** The
constant's own comment — written by § 6.3's closure — reads *"the remaining three
(`add-nightly-dashboard-refresh`, `add-lens-document-selection`,
`retire-doxbench-chat-turn-v1`) hold no row here, so this is the last movement
§ 6 makes to this constant."* Confirmed: no row, no change. There is likewise no
`contracts/openspec-cli-pin.yaml` disposition and no
`scripts/target-release-register.yaml` entry for this id.

## THE SEVEN OPEN BOXES, EACH DISPOSITIONED BY MEASUREMENT

`tasks.md` closed with 37 boxes ticked and seven open: 1.8, 7.1, 7.2, 8.1, 8.3,
8.4, 8.5. **This record does not tick them and does not edit `tasks.md`** — in
this lane every `tasks.md` tick rides the bookkeeper's single amendment, and the
packet's text is evidence of what that session did. What follows is the evidence
each box asked for, taken now, so the amendment can tick from a measurement
rather than from a memory.

### 7.1 — the validator at the tag. **MET, three conjuncts.**

The published annotated tag read **from the remote** —
`git ls-remote --tags origin contract-v3.0` → `59f4f51f2e0ac7c833cdaee9f385e9e83777650e`,
peeling to `ff9ed81541ab3eb2ebeb2e79676e5a875dd58064` — checked out detached and
run there:

```
$ python3 scripts/validate-ideation-dashboard-contracts.py
note  examples: 42 valid example(s) confirmed valid, 80 negative example(s) confirmed invalid, 8 transition pair(s) confirmed
validate-ideation-dashboard-contracts: 0 error(s), 0 warning(s)      # exit 0
$ python3 scripts/validate-ideation-dashboard-contracts.py --strict  # exit 0
$ python3 scripts/validate-ideation-dashboard-contracts.py \
      examples/ideation-dashboard/negative/workbench-chat-turn-unrecognized-kind.negative.yaml
ERROR [kind] …/workbench-chat-turn-unrecognized-kind.negative.yaml: unrecognized document (no known kind, no 'possibles_register' section)
```

Zero chat-turn deprecation warnings where a default run before the removal
reported four; `--strict` exits 0 where it exited 1; and the third conjunct — *"a
v1 instance is refused"* — is taken on the packaged fixture whose `kind` is
`workbench-chat-turn`, **a retired one**, which task 4.5 chose precisely so the
refusal of a retired kind would be a packaged artifact rather than a claim.

### 7.2 — the fallback against the RUNNING SERVE, both directions. **PERFORMED.**

This box is the one an earlier session reported as no longer performable in this
repository, because § 5.2's shed (`cc4ae9d3`, PR #940) removed
`scripts/ideation_dashboard/serve.py` and
`scripts/validate-ideation-dashboard-contracts.py` from `main`
(`scripts/ideation_dashboard/` is **90** files at the tag and **10** at this
closure's head). **That report was about the wrong tree.** § 7.1 already directs
the verification at the TAG, the tag still carries the serve, and a serve at the
published tag is the serve this release cut. Taken there, on a real
`ThreadingHTTPServer` bound to loopback and driven over a real socket by
`http.client` — the suite's own `build_server` harness, so the thing answering is
the server and not a handler called in-process:

**Direction 1 — a RETIRED v1 kind with a wire-valid turn identity** ·
`POST /actions/workbench/chat-turn`, `kind: "workbench-chat-turn"`,
`client_turn_id: "turn-0001"` → **HTTP 400**, `application/json; charset=utf-8`:

```json
{"schema_version": 1, "kind": "workbench-chat-turn-v2-failure", "client_turn_id": "turn-0001", "error": "unrecognized_turn_kind", "message": "this route does not serve the chat-turn kind the request declared"}
```

**Direction 2 — the same kind with NO wire-valid turn identity** → **HTTP 400**:

```json
{"ok": false, "error": "unrecognized_turn_kind", "message": "this route does not serve the chat-turn kind the request declared"}
```

**CONTROL — a kind that NEVER existed** (`"never-was-a-kind"`), with identity →
**HTTP 400**, and its body is byte-for-byte direction 1's but for the correlation
key the client itself sent. That control is the requirement's central claim
answered on the wire: *"after the removal a retired kind and a kind that never
existed are the same fact about the wire."*

In all three the injected model port recorded **zero calls** — the refusal never
reaches a provider. Both directions carry the same `error` code; direction 1 is
the surviving family's closed failure envelope, direction 2 the pre-identity
shape with **no fabricated `client_turn_id`**, which is the half the requirement
says must stay unchanged. The driver is kept at
`…/attachments/lane-opendox/substrate/task72-live-driver.py` and the transcript at
`ev-7.2-live.txt`; neither is committed, the tag tree being left byte-clean.

### 8.1 — merged, and green. **MET — and the green run is NAMED, not waived.**

*"§§ 2-5 merged on `openxFactory` `main`"* — MET: PR **#564**, squash
**`6856f502248f7ced2bf4d36529dd1ba1abb32f45`**, an ancestor of `main`.

*"`pytest-suite` green on the merge commit"* — **NOT met at that commit.** Check
run `100000861624` is `failure`: `1 failed, 8576 passed, 21 skipped, 338
deselected` — one assertion,
`tests/sequenced_after/test_sweep.py::test_the_live_sweep_reproduces_the_AUTHORING_measurement`,
`AssertionError: assert 18 == 19`.

**It is inherited, and that is measured rather than argued.** The same assertion
fails identically at `a951be76`, the merge's PARENT — run here in a detached
worktree at that commit:

```
$ python3 -m pytest tests/sequenced_after/test_sweep.py::test_the_live_sweep_reproduces_the_AUTHORING_measurement -q
E       AssertionError: assert 18 == 19
1 failed in 0.35s
```

One commit before this packet's bytes reached `main`, the red already stood.
`main`'s own remedy, `d308c012` (PR #568, *"Move the sequenced-after live pin:
active_co_modified 19 -> 18 since #563"*), re-derives the count per commit and
reaches the same place: PR #563's archive of `add-release-tag-publication-check`
moved it, PR #567 then landed a test carrying an authoring measurement taken
three minutes earlier, and `6856f502` was merely the first pytest run the
concurrency group did not cancel. `d308c012`'s own `pytest-suite` is SUCCESS.

The proposal disclosed an inherited red in advance and attributed it to *"PR
#510's carriage-ledger subject"*. **That attribution is not what stood at the
merge**, and the difference is recorded rather than smoothed: the proximate cause
measured above is #563 plus #567. The class the proposal named — a
carriage-ledger count pinned in a test, moved by somebody else's archive — is
exactly right.

**AND "INHERITED" IS NOT THE DISCHARGE. THE GREEN RUN IS, AND IT EXISTS.** The
gate says *"merged plus green realization evidence"*, and an inherited failure is
still a failure — so the question is not whose fault the red was but whether this
realization ever ran green on `main`. **It did, two commits later:**

```
$ git merge-base --is-ancestor 6856f502 d308c012   # exit 0 — the realization is IN it
$ git diff --stat 6856f502 d308c012
 tests/sequenced_after/test_sweep.py | 46 ++++++++++++++++++++++++++++++++++---
 1 file changed, 44 insertions(+), 2 deletions(-)
$ gh api …/commits/d308c012/check-runs   → pytest-suite  SUCCESS
                                            run 100071079428, 2026-09-02T00:15:02Z
```

`d308c012` (PR #568) **contains every byte of this realization** and its
`pytest-suite` is SUCCESS. The whole difference between the two commits is ONE
FILE — `tests/sequenced_after/test_sweep.py`, the sweep pin — which belongs to
neither this packet nor its code surface: not the schema, not the runtime, not
the validator, not a fixture, not a packaged example. So the green run is a run
of THIS realization's tree, not of a repaired one.

That is the evidence § 8.1 asks for, and it is named here rather than left to be
inferred from "inherited": **merged at `6856f502`, green at `d308c012`, with the
only intervening edit provably unrelated.** The red at the merge commit is still
recorded above, because a record that quietly moved to the greener commit would
hide that the suite was red on a commit somebody may one day bisect to.

### 8.3 — `openspec validate --all --strict`. **MET at this closure's head.**

```
$ python3 scripts/validate-openspec-cli-pin.py --all
Totals: 101 passed, 1 failed (102 items)
OK openspec-cli-pin: @fission-ai/openspec@1.12.0 verified against its content address;
   every target validated --strict with 0 UNDISPOSITIONED failures.          # exit 0
```

**Zero undispositioned failures.** The one failure is the accepted exception
`add-chain-attestation`, disposed in `contracts/openspec-cli-pin.yaml` and
unrelated to this packet; it reads the same on `main`. The item count falls
103 → 102 because this packet leaves the active corpus.

### 8.4 — the 4.7 validator run at `0 error(s), 0 warning(s)`. **MET, with an erratum.**

Reproduced verbatim at the tag under 7.1 above. **ERRATUM:** § 4.7's
parenthetical records *"81 negative example(s)"*; the validator says **80**, and
not because a fixture was lost. It says 80 at `6856f502` — the very commit § 4.7
measured — and 80 at `ff9ed815`, with `examples/ideation-dashboard/` byte-identical
between them. The box counted FILES and the validator counts CHECKS: 81 files, of
which `negative/duplicate-turn-pair/` is two files forming **one** paired check —
79 + 1 = 80. The line § 8.4 actually asks for, `0 error(s), 0 warning(s)`, is
exact.

### 8.5 — the seven refusal-class dispositions. **MET, in § 4.4's own table.**

§ 4.4 records all seven — escaping path, hash mismatch, identity subject, over
budget, unknown model, untyped proposal, duplicate turn pair — each as a **named
`-v2` fixture**, with **zero stated losses**, and each with the refusal string it
actually produces. All seven are present at the tag: six
`workbench-chat-turn-v2-*.negative.yaml` files plus the rewritten
`duplicate-turn-pair/{turn-a,turn-b}.yaml`, and all are inside the 80 the
validator confirms invalid. § 8.5 asks that they be *recorded*; they were
recorded when they were made.

### 1.8 — the adversarial review. **OPEN, AND ROUTED BY THE PACKET ITSELF.**

The box's own text disposes it: *"A CODEX PASS REMAINS OWED and is blocked by
nothing in this packet — it and those four [low-severity Copilot comments] route
to the amendment lane, not to doubt about the ratification."* Three `@codex
review` requests returned provider usage-limit refusals and no verdict; the
absence was disclosed to Brett Heap and he ratified against it. Archiving changes
none of that: the owed pass is owed to the amendment lane, at the same address it
was owed at on 2026-09-01. It is the one of the seven this closure does not
discharge, and it is named here so it is not lost in the move.

## Measurements at this closure's head

| | main `cb2d3a2c` | this head |
| --- | --- | --- |
| `validate-openspec-cli-pin.py --all` | 106 passed / 2 failed (108) | **101 passed / 1 failed (102)**, exit 0 |
| doc-health `promotion-fidelity`, `--single-repo` | 0 error | **20 error** — the four re-homed closures' 20, and **none of them this packet's** |
| the sweep ledger | consistent, 214 rows | consistent, 214 rows |

**The promotion is why the middle row does not move.** Archiving this packet
WITHOUT promoting would have added three findings — the two ADDED requirements
absent from the promoted spec, and the MODIFIED requirement reaching it without
3 of its 7 ratified scenarios (*The removal target the release recorded is
reached*, *A client sends a v1 turn after the removal*, *A consumer pinned below
the removal*). Measured both ways: **23 with the move alone, 20 with the
promotion.** That delta is the whole argument for promoting, taken as a number.
