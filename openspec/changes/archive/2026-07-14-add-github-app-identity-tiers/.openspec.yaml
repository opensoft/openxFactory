schema: spec-driven
created: 2026-07-14
origin:
  kind: staged
  id: openxFactory:staging:github-administration-plane
  path: ideation/staging/github-administration-plane
  reason: >-
    SWEPT BY RULING 2026-08-25, verbatim: "sweep the 23 derivable
    proposal-origin warnings". DERIVED FROM THIS PACKET'S OWN RECORD,
    never invented. The approval act is this packet's own lifecycle
    citation, quoted verbatim under approved_by below. The forcing fact is
    stated in this packet's own proposal under its Why heading, which this
    metadata-only edit leaves untouched. KIND IS staged, derived and
    verified in BOTH DIRECTIONS rather than assumed from a mention: the
    kept-current inventory ideation/staging/INDEX.md names this change as
    the EXIT of the github-administration-plane topic; that topic's folder
    still exists; and its primary fragment carries the matching Staging ID
    header for openxFactory:staging:github-administration-plane. The
    worker-credential lesson from PR #344 applies in reverse here — a
    topic merely MENTIONED is not an origin, and this one declares the
    exit. The created date 2026-07-14 was already recorded in this
    packet's own .openspec.yaml and is VERIFIED, NOT REWRITTEN: it is
    consistent with the packet's true first commit 1e107426 of 2026-07-14,
    which a packet is authored before. This declaration supplies the
    origin and approval pair the proposal-origin contract requires and
    changes nothing else about the packet: no lifecycle header, no
    content, no status.
  approved_by: >-
    Per this packet's own "Ratified" citation, quoted verbatim:
    "2026-07-14 by Brett — record: the archive commit `1e10742`, "Realize
    + archive add-github-app-identity-tiers (GitHub App identity tiers)",
    whose body opens "Ratified 2026-07-14", together with this change's
    own tasks.md 3.1 under its "## 3. Ratification and realization
    evidence" heading — "Obtain Brett's ratify-gate approval for this
    change ... this task is the formal OpenSpec ratify step for the change
    itself", ticked. The same act folded the delta into
    `openspec/specs/roles-authority-model/spec.md`. Backfilled 2026-08-23
    by `govern-openspec-corpus-membership` slice 5C under OQ-6's ruling
    that every headerless proposal is derived from its own record; no
    approving OpenSpec change exists to name, so this is the record-citing
    spelling. See tasks.md "Bookkeeping correction"." — recorded in its
    proposal at authoring time. That is the approval provenance the ad-hoc
    exception requires, and it is why this packet fell in the derivable
    class rather than the stop class of the 2026-08-25 sweep.
  approved_on: 2026-07-14
