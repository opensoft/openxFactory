schema: spec-driven
created: 2026-08-24
origin:
  kind: ad_hoc
  id: openxFactory:adhoc:2026-08-24-apply-branch-sessions-deltas
  reason: >-
    Ruled in-session by Brett on 2026-08-24: "apply via a proper change". Not
    a staged ideation topic — the forcing fact is a defect the
    promotion-fidelity family measured against this repository's own archive
    on its first run. `openspec/changes/add-promotion-fidelity-check/tasks.md`
    §3.2 records the two findings and §5.1 records the ruling that closes
    them: the archived `2026-08-01-add-workbench-branch-sessions` ratified an
    ADDED requirement (`Branch-session notebooks`) that never reached
    `openspec/specs/lifecycle-notebook-projection/spec.md` and a MODIFIED one
    (`Corpus scan scope`) that arrived a scenario short, and applying a
    ratified delta to canon is a governance act belonging to its own change.
    This is that change — the openxFactory equivalent of codexFactory PR #85,
    which discharged the same class for `merge-master-approval`, and it
    carries the same drift-check-before-apply discipline §5.1 names.
  approved_by: >-
    Brett — the 2026-08-24 four-question round, recorded verbatim in
    `openspec/changes/add-promotion-fidelity-check/tasks.md` §5.1: "RULED
    (2026-08-24, Brett, the same four-question round): APPLY VIA A PROPER
    CHANGE — the PR #85 equivalent, with its drift-check-before-apply
    discipline (the requirement region's history verified untouched before
    the ratified text lands). Box stays open until that change lands; this
    note records the ruling so the executing slice needs no new one." The
    authorization covers the DECISION TO APPLY. It carries no discretion over
    the text: this packet's delta is a byte-for-byte copy of the delta
    already ratified 2026-07-26, so there is nothing here for an approver to
    choose.
  approved_on: 2026-08-24
