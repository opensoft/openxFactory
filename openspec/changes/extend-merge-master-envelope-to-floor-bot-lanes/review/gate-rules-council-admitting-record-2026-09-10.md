# Gate-Rules Council admitting record: the `openxfactory-floor-regeneration` candidate class — RECONCILED AND SIGNED

Status: record
Record scope: **SIGNED — VERDICT ADMIT.** The signature is § 5. It was written by
  lane `openxfactory-2` on Brett Heap's word of 2026-09-10 ~17:2xZ, verbatim
  **"do 1 and 2, sign, file it, do 744"** — **his act, the lane's pen**; the lane
  took no decision of its own and none of the judgements below is the lane's.
  Ruling comment:
  <https://github.com/opensoft/openxFactory/issues/745#issuecomment-5622848416>
  (item 3). **THE SIGNATURE RATIFIES AN ACT ALREADY TAKEN** — the enrolment
  merged on 2026-09-07 and has approved twice since — and § 0.2 records that
  inversion rather than smoothing it.
Council: `gate_rules_council` (codexFactory
  `hermes/domain/review-councils/gate-rules.yaml`), **direct convener
  disposition** — **no seat was run, no seat return exists, and no model was
  invoked for this act.** § 0.1 states what that means and what makes it
  available.
Convener: Brett Heap (openxFactory repository owner; the convener of this body).
Date of the word: 2026-09-10. Date recorded: 2026-09-10, by lane
  `openxfactory-2` (display `openXfactory-2`).
Subject: admission of ONE candidate class, `openxfactory-floor-regeneration`,
  into `codeXfactory/codexFactory`'s merge-approval envelope
  (`.github/merge-approval-envelope.yml`) — the instrument that
  `extend-merge-master-envelope-to-floor-bot-lanes` OQ-2 / codexFactory OQ-C1
  selects for that grant, answered as recommended under the ratification word of
  2026-09-07.
Reconciles: codexFactory
  `openspec/changes/extend-merge-master-envelope-to-floor-bot-lanes/review/gate-rules-council-admitting-record-DRAFT.md`
  (`Status: draft`, unsigned, authored 2026-09-07 by this same lane). **That
  draft was written before the live cycles and is stale against the tree in six
  places.** This record keeps its structure and rewrites its content to what IS;
  every correction is named at § 7 rather than made silently. The draft is not
  edited here — see § 0.3 on placement, and § 8 on what is OWED in codexFactory.
Reviewed artifacts, each read at the revision named rather than cited from
  memory:
  codexFactory `.github/merge-approval-envelope.yml`, `.github/CODEOWNERS` and
  `openspec/specs/merge-master-approval/spec.md` at `main`, 2026-09-10;
  codexFactory pull requests **#302**, **#314** and **#325** and their
  merge-master records, from the API; org `codeXfactory` rulesets **22655338**
  and **22655341**, from the API; openxFactory
  `openspec/changes/extend-merge-master-envelope-to-floor-bot-lanes/`
  (`proposal.md`, `design.md`, `tasks.md`, `review/ratification-2026-09-07.md`,
  both spec deltas) at `main` `e0638f11`.
Precedents matched (the record FORM): codexFactory
  `records/2026-09-04-gate-rules-packaged-contract-floor-widening.md` — the
  no-seat record whose § 0 states plainly how the act was taken and what that
  means — and `records/2026-09-03-gate-rules-enrolled-routine-code-floor-widening.md`,
  the record shape it reproduces. Placement follows openxFactory
  `openspec/changes/add-chain-attestation/review/council-review-2026-08-30.md`
  § 5, the convening that could not write to codexFactory and declared the
  deviation instead of glossing it.

---

## 0. HOW THIS ACT WAS TAKEN

### 0.1 No seat was run, and that is a precedented shape rather than a shortcut

**No agent seat was convened for this admission. No seat return exists. No
ballot was produced.** The convener disposed it directly, in the word quoted in
the Record scope header.

What makes the direct disposition available is the council's own charter:
`gate-rules.yaml` declares `output_disposition:
convener_accepts_or_rejects_on_record` — the body's output is a RECOMMENDATION
the convener accepts or rejects, and the convener is the repository owner. The
2026-09-04 packaged-contract record established the shape of a disposition with
no returns to accept at all, and its § 0 is reproduced here for the same reason
it was written there: **a reader comparing this record with a seated one must be
able to see that this one rests on the convener's own authority and not on a
deliberation that did not happen.**

Accordingly: § 3 carries no seat verdicts and says so; there is no amendment
set, because no seat amended anything; and every measurement in § 3 was taken by
the lane from the tree and the API, and is checkable line by line by re-reading
the artifacts named in the header.

**`missing_required_seat: refused` IS NOT EVADED BY THIS, and this record says
so rather than being silent.** The charter carries that clause beside the
disposition one — `failure_semantics.missing_required_seat: refused`, read at
`gate-rules.yaml` on 2026-09-10, annotated there *"a gate-rules council without
its client seat cannot set rules for that repo"* — and it is the strongest thing
a reader can put against a record with no seats in it. The precedent answered it
in terms this record reproduces rather than paraphrases: *"with no convening run
there was no seat to be missing from one. A future act that needs seats is not
licensed by this record to skip them."* The clause governs a CONVENING whose
roster is incomplete; it is not a licence for a direct disposition, and no such
licence is claimed here. That this body does run seats when the act calls for
them is on its own record: it *"convened on 2026-08-28 and REFUSED,
unanimously, the candidate class put to it"* (openxFactory
`.github/merge-approval-envelope.yml`), and the 2026-09-03 seated convening
remains the form for an act that needs seats.

**The grounds for disposing THIS act directly — recorded for this act and NOT
offered as a general precedent**, which is the form the 2026-09-04 record's
§ 7.4 put them in:

- The act admits **ONE class, already enrolled and already exercised twice**,
  over an artifact whose content is DERIVED from a source of truth in another
  repository — custody of a decision taken elsewhere rather than a decision
  (§ 1, and the envelope entry's own description).
- **Nothing is spent** (§ 3.2). The surface was moved out from under the bar
  instead of the bar being narrowed for it, so there is no governance ground
  left for a seat to weigh — which is a materially smaller question than the
  one the draft put to this body on 2026-09-07.
- The convener is the repository owner and the accountable human, and
  `output_disposition` places the disposition with him.
- Every figure the disposition rests on is a MEASUREMENT of what the class did
  in production (§ 3), re-readable at the artifacts the header names, rather
  than a projection a seat would have had to judge.

### 0.2 THE ORDER DID NOT HOLD, AND IT IS RECORDED RATHER THAN SMOOTHED

The draft said, in its own words: *"merging the pull request that carries the
candidate class `openxfactory-floor-regeneration` in
`.github/merge-approval-envelope.yml` is the admitting act"*, and codexFactory's
envelope file says beside the entry that the merge **"must not happen before that
record is signed."**

**It happened first.** The enrolment landed on codexFactory `main` at commit
`4c0053c3` on **2026-09-07T21:16:37Z** (*"Enrol the floor-regeneration lane as a
second candidate class (tasks 2.1-2.5)"*), was amended twice more (`e912e260`
2026-09-08, the ruled `lane-line` exclusion; `d1f8bf1b` 2026-09-09, the
relocation's reference sweep; `cca0304d` 2026-09-09, the org move), and has since
approved two pull requests autonomously. **This signature therefore ratifies an
act already taken; it does not authorize a future one.**

The convener is told this plainly because it is the one thing a later reader
would otherwise get wrong about the sequence, and because the honest reading of
"sign" over a record whose own § 5 said "merge the enrolment pull request — that
merge is the admitting act" is **ADMIT, retrospectively, on the evidence of what
the class actually did** rather than ADMIT prospectively. § 3 is written to be
that evidence. **Nothing here is presented as having happened in the order the
draft prescribed.**

### 0.3 Placement — a convener act, declared rather than glossed

The Gate-Rules Council's records live in codexFactory
`hermes/domain/review-councils/records/`, and the draft this reconciles lives in
codexFactory's copy of the packet. **This lane holds no write authority over
codexFactory in this act** — its governing brief forbids touching that side — so
the record is filed HERE, under this repository's half of the same packet,
exactly as the 2026-08-30 council-review record was when the same constraint bound
it: *"it holds no write authority over codexFactory in this act and pushed nothing
anywhere. The deviation is declared rather than glossed."*

The 2026-09-07 ratification of this packet established that a PAIRED act is
recorded separately in each repository's own `review/` directory (*"each
repository records it separately in its own review directory"*). This is
openxFactory's record of the paired act. **codexFactory's half is OWED and is
named at § 8** — a mirror into `hermes/domain/review-councils/records/`, and a
supersession note on the DRAFT. Naming it here is an obligation, not a
performance.

### 0.4 What this record does NOT do

- **It ratifies no OpenSpec change.** `extend-merge-master-envelope-to-floor-bot-lanes`
  was ratified on 2026-09-07 by its own word and its own record
  (`review/ratification-2026-09-07.md`); this is the ADMITTING instrument that
  packet's OQ-2 names, and it is a different act. No proposal, design or spec
  delta of this or any packet is edited by it.
- **It archives nothing and merges nothing.**
- **It enrols no further class.** A third candidate reds
  codexFactory's `test_the_envelope_enrols_exactly_two_candidate_classes` before
  anyone reads the diff.
- **It does not admit the openxFactory re-pin lane**, which stays under a human
  merge word — decision N-1 standing as recommended, refused before any envelope
  decides by the floor composed over it
  (`.github/workflows/merge-master-approval.yml`, the `FLOOR_MATCHED` guard).
- **It disturbs no earlier ruling of this body.** The 2026-08-28 unanimous
  refusal refused a class over `openspec/changes/**`; this class admits no
  `openspec/` path.

---

## 1. WHAT IS ADMITTED — the class as it stands in the tree, 2026-09-10

| | |
|---|---|
| class id | `openxfactory-floor-regeneration` |
| declared in | codexFactory `.github/merge-approval-envelope.yml` (BASE branch only; the file is CODEOWNERS-gated at `/.github/`) |
| target repos | `codeXfactory/codexFactory` |
| author | `openxfactory[bot]`, exactly |
| head ref | `floor/bot-regeneration`, exactly |
| base ref | `main` |
| same repository | required (`require_same_repository: true`) |
| writable paths | `floor/openxfactory-review-authority-floor.yaml` and `tests/merge-master/test_repository_gate_floor.py`, ENUMERATED EXACTLY, no glob **(R-1 — the first path was `scripts/merge_master/…` when the draft was written)** |
| all checks green | required (`require_all_checks: true`), minus two exclusions |
| check exclusions | `merge-master-approval` (the self-reference) and `lane-line` (ruled 2026-09-08, exempt for this class's one Bot author) |
| revert suffices | asserted |
| kill switch | the entry itself — one code-owner-reviewed edit, read from the base branch, effective next evaluation |

**The class is present and it MATCHES.** Read from the API on 2026-09-10:
codexFactory **#314** (`openxfactory[bot]`, head `floor/bot-regeneration`, base
`main`, 1 changed file) and **#325** (the same) each reached
`decision == approve` from the pinned core and each merged. That is the
measurement § 3 rests on.

---

## 2. WHY THE INSTRUMENT IS A COUNCIL RECORD

`.github/merge-approval-envelope.yml`'s own rule, as openxFactory's envelope
states it for the estate: *"Adding a second is not a configuration change: it is
a new grant, needing its own word or its own council record."*

The packet answered which, and answered it against itself: **a council record**,
because on the 2026-09-07 measurement the act was read as NARROWING A RULE — the
regeneration lane's writable path was then `scripts/merge_master/…`, `/scripts/`
is CODEOWNERS-gated, and codexFactory's promoted `merge-master-approval`
§ *Bounded autonomous surface* forbids approving there. A word may lawfully grant
a class INSIDE a rule; it is the wrong instrument for changing one. Brett Heap
answered OQ-C1 as recommended on 2026-09-07 under the ratification word.

**THAT GROUND HAS SINCE FALLEN AWAY, AND THE INSTRUMENT STILL STANDS (R-2).**
The relocation of 2026-09-09 took the admitted artifact off every CODEOWNERS
prefix, so the act narrows nothing (§ 3.2, § 3.4). The instrument is unchanged
for two independent reasons, both recorded rather than assumed:

1. **The envelope file's own rule is disjunctive** — *"its own word or its own
   council record"* — and one of the two is required whatever the act's weight.
2. **The owner selected it and has not unselected it.** OQ-C1 was answered as
   recommended on 2026-09-07 and the word of 2026-09-10 is *"sign"*, not
   *"a word will do"*. An agent does not downgrade an instrument the owner chose
   because the ground for choosing it weakened.

`add-substantive-review-lane` task 3.2 — this body's general grant of an operable
candidate class for openxFactory — **remains open** and is not touched here.

---

## 3. WHAT WAS WEIGHED, MEASURED 2026-09-10

**No seat verdicts (§ 0.1).** Every figure below was read from the tree or the
API by lane `openxfactory-2` and can be re-read at the artifacts the header
names.

### 3.1 What is bought — and it is now MEASURED rather than projected

The draft claimed *"one human merge per cycle"* prospectively. The live figure:

| Cycle | PR | Approving review | Merge | Merged by | Human acts |
|---|---|---|---|---|---|
| 2026-09-09 | codexFactory **#314** | `codexfactory[bot]` APPROVED 22:49:36Z (review 5160667178) | `b08958ae`, 2026-09-09T23:27:22Z | `openxfactory[bot]` | **one hand dispatch** of the approval leg |
| 2026-09-10 | codexFactory **#325** | `codexfactory[bot]` APPROVED 01:54:56Z (review 5161853361) | `df42f803`, 2026-09-10T01:55:14Z | `openxfactory[bot]` | **NONE** |

**#314's heads are written out rather than compressed, because they do not tell
one story.** Its commits are `17cd5886` (21:32:22Z), `e27d64ff` and `fcb2bd13`
(both 22:23:06Z) and `2d8bf35c` (23:22:34Z); the approval's own body names
`fcb2bd13` as the head the envelope held for, while the API reports the review's
`commit_id` as `2d8bf35c`. Both readings are recorded and neither is resolved
here — what the box in § 3.1 of `tasks.md` needs is the unattended cycle, and
that is #325, whose review `commit_id` **is** its head `c4e40f33`.

**#325 is the unattended cycle.** Opened 01:32:58Z, merged 01:55:14Z — **22
minutes 16 seconds**, no human actor anywhere in the timeline: auto-merge armed
by the lane (`auto_merge_enabled`, actor `openxfactory[bot]`, 01:33:02Z — four
seconds after opening), merge-master evaluated on `pull_request_target` (run
`34425922954`) and again on `check_suite` (run `34427337240`), approval by the
App, merge by the platform under the arming. **The timeline is enumerated
rather than characterized**, because "no human actor" is a claim about an
absence: the only actors between opening and merge are `openxfactory[bot]`,
`github-actions[bot]`, `sonarqubecloud[bot]` and `codexfactory[bot]`, and the
one human login on the pull request — `brettheap` — first appears at 02:13:57Z,
eighteen minutes AFTER the merge, and only in `referenced` / `cross-referenced`
events. The full read-out is under § 3.2 of `tasks.md`. The before figure the packet
measured was TWO human merges per advance of the pinned core, one per
repository. The after figure, on this cycle, is **ONE** — openxFactory's re-pin
half only.

The openxFactory re-pin lane's merge is NOT bought and was not asked for. Its
last pull request, **openxFactory #764** (head `bot/review-lane-repin`), was
merged by **`brettheap`** — a human — at 2026-09-07T16:52:54Z.

### 3.2 What is spent — **NOTHING (R-2)**

The draft said ONE governance ground was spent: the CODEOWNERS-scoped
prohibition, narrowed in one clause. **On the tree of 2026-09-10 no governance
ground is spent at all**, and the three measurements that establish it were each
taken rather than inferred:

- **The admitted artifact is not on a CODEOWNERS-scoped path.** codexFactory
  `.github/CODEOWNERS` carries 27 owner entries; `/scripts/` is one of them and
  **`/floor/` is not one of them.** The class's first writable path is
  `floor/openxfactory-review-authority-floor.yaml`.
- **The promoted requirement is UNNARROWED.** codexFactory
  `openspec/specs/merge-master-approval/spec.md` § *Bounded autonomous surface*
  reads on `main` today, verbatim: *"MUST NOT approve any pull request outside
  the allowlisted bot-authored in-envelope surfaces, and MUST NOT approve any
  pull request on a surface scoped to a human gate (a CODEOWNERS-scoped path) or
  authored by a human."* The companion's `## MODIFIED` block does not reach canon
  until that packet archives, and it has not.
- **So the live approvals are lawful under canon AS WRITTEN.** They are not
  lawful because a bar was narrowed for them; they are lawful because the surface
  was moved out from under the bar.

### 3.3 What is NOT spent

- **No floor entry moves.** openxFactory `contracts/review-lane-pin.yaml` remains
  a declared never-clearable member; nothing was removed from the floor document;
  no carve was authored. N-1b stayed dormant.
- **The openxFactory re-pin lane is not admitted** (§ 0.4).
- **The 2026-08-28 unanimous refusal is undisturbed** (§ 0.4).
- **No tier-2 gate rule was needed, and this is now OBSERVED rather than
  computed.** The draft computed the class PERMANENTLY ADVISORY — unreachable by
  tier-2 clearance, approvable only through the tier-1 envelope. Both live
  approvals say so in their own words: *"tier-1 envelope holds; tier 2 not
  consulted."* And the fall-through is visible from the other side: before its
  checks completed, #325's first evaluation parked with *"no per-repo gate rule
  declares applies_to.candidate_id for surface 'openxfactory-floor-regeneration';
  tier-1 evaluation alone"*. **No per-repo gate rule declaring this candidate has
  ever been written, and none is needed.**
- **No decision core changes.** `scripts/merge_master/envelope.py` and
  `schemas/merge-approval-envelope.schema.json` are untouched; every condition
  declared is a member they already carry.
- **No ruleset was changed, and none needed to be (R-3).** See § 4.

### 3.4 The alternative that was not foreclosed — **AND IT IS THE ONE THAT WAS TAKEN (R-2)**

Companion decision **N-1 (e)**: *satisfy the bar as written instead of narrowing
it, by moving the floor document's machine-generated block off the
code-owner-gated surface.* The draft recorded it as the option not taken, and as
the successor to file if the record were refused.

**It was taken.** codexFactory `relocate-review-authority-floor` (pull request
#293) and its openxFactory lockstep mirror `relocate-review-authority-floor-mirror`
(pull request #817) were ratified together by Brett Heap on 2026-09-08, verbatim
*"ratify 293 and 817 when green, then realize them"*, and the move landed on
codexFactory `main` at `d1f8bf1b`, 2026-09-09T03:51:09Z: `scripts/merge_master/openxfactory-review-authority-floor.yaml`
→ **`floor/openxfactory-review-authority-floor.yaml`**.

**That is why § 3.2 has nothing to spend.** The council is not being asked to
weaken a ground; it is being asked to admit a class over a surface the estate
moved so that no ground has to be weakened. **The admission is materially
cheaper than the one the draft put**, and the record says so rather than letting
the draft's price stand.

### 3.5 What the live cycles found that nobody asked for — TWO MEASURED DEFECTS

Recorded here because a council record that omits what running the class taught
is a record of an expectation. **Neither is fixed by this record** — both sit on
codexFactory's surface, which this lane may not touch — and both are carried to
§ 8 as owed.

**(a) THE APPROVAL RECORD DOES NOT NAME THE CANDIDATE CLASS.** Both autonomous
approvals render, verbatim:

```
- Candidate class: `unrecorded` — the envelope enrols classes BY NAME and this
  approval is that class's and no other's.
```

`unrecorded`, on **#314** (review 5160667178) and on **#325** (review
5161853361). The PARK record on the same pull requests renders `none matched`,
so the field is populated on one path and not the other. This is a defect against
**this packet's own ratified requirement** — `specs/review-lane-floor-mirror/spec.md`
§ *An autonomous approval of a floor bot lane records the facts it measured*:
*"carrying the candidate class it matched"*, whose scenario says the reader must
be able to *"reconstruct why it was approved without consulting any other
artifact."* Every other fact that requirement names IS carried (measured changed
paths, the checks quantified over, the floor-matched rendering, the kill-switch
sentence). **It is one field.** Task 3.1 of `tasks.md` conditions its tick on the
approval record naming the class, so that box stays open on this and on nothing
else.

**(b) A FLOOR-MEMBERSHIP CYCLE STILL PARKS, AND THE PATH IT PARKS ON IS ONE THIS
CLASS EXPLICITLY ADMITS.** codexFactory **#302** (2026-09-09) changed **both**
enumerated paths — the floor document and
`tests/merge-master/test_repository_gate_floor.py`, the LS-A3 mirror, which moves
only when floored membership changes — and parked:

```
- Reason: gate_integrity_path: changed path(s) inside the never-clearable class:
  ['tests/merge-master/test_repository_gate_floor.py']
- Outcome: `parked_never_clearable`
```

It was then closed by hand. The path is inside the class's `path_allowlist` AND
inside codexFactory's tier-2 gate-integrity `never_clearable_paths`, and the
never-clearable class is evaluated ahead of the tier-1 envelope, so **the
envelope's admission of that path can never be reached.** The design measured the
bar correctly in 2026-09-07 (`design.md` § 2.2's correction: the mirror *"carries
the allowlist-and-floor bar instead"* of the CODEOWNERS bar) but neither the
packet nor the draft drew the consequence. **The consequence, stated plainly:
the autonomy this class actually holds is over ONE of its two enumerated paths.
A cycle in which floored membership changes still parks for a human**, which is
fail-closed and arguably right — but it is narrower than the draft described, and
a reader budgeting human clicks needs it.

---

## 4. WHAT SIGNING ADMITS, AND WHAT IT DOES NOT

**SIGNING ADMITS:** the one class of § 1, on those exact conditions, in that one
repository — and, per § 0.2, ratifies the approvals it has already given.

**SIGNING DOES NOT:**

- **make the lane unattended — IT ALREADY IS, AND BY A ROUTE THE DRAFT DID NOT
  ANTICIPATE (R-3).** The draft said the merge-master App's approval could not
  clear `Require Code Owner Review` (it cited that ruleset as id `18834180` on
  `opensoft/codexFactory`) and that a bypass actor was a separate owner's act,
  not performed. **No bypass actor was ever added, and none was needed.** Read
  from the API on 2026-09-10, after the repository's move to the `codeXfactory`
  org:
  - org ruleset **22655341** `Require Code Owner Review` — active,
    `require_code_owner_review: true`, `required_approving_review_count: 0`,
    bypass list `OrganizationAdmin / always` and nothing else. **Vacuous over a
    changed path no CODEOWNERS entry matches** (§ 3.2).
  - org ruleset **22655338** `xFactory Tier-1 main protection (require PR + 1
    approval)` — active, `required_approving_review_count: 1`,
    `require_code_owner_review: false`, `require_last_push_approval: true`,
    bypass list `OrganizationAdmin / always` and nothing else. **This is the
    rule the App's APPROVE review satisfied**, and on #325 the review's
    `commit_id` is the head, which is what `require_last_push_approval`
    demands.
  - **those two are the whole of the approval bar, and the enumeration is
    complete rather than selective.** The `codeXfactory` org carries exactly
    THREE active rulesets on 2026-09-10 and the third, **22655344** `Copilot
    Auto-Review All PRs`, declares only `non_fast_forward` and
    `copilot_code_review` and no `pull_request` rule at all — so it requires no
    approving review and bears on nothing here.
  - the ruleset ids in the draft and in `tasks.md` § 5.3 (`18834180`) are stale:
    they were the `opensoft` org's, and the ids moved with the repository.
- **arm any merge — THE ARMING IS ALREADY LAWFUL, BY A SUCCESSOR PACKET
  (R-4).** The draft said the lane MAY NOT arm auto-merge, on
  `add-floor-regeneration-automation` § *An automated floor regeneration only
  ever proposes*. That requirement was narrowed by
  **`amend-floor-regeneration-merge-authority`** (ratified 2026-09-08, verbatim
  *"ratify it when green, then realize it"*; merge `93f0f0d7`) to *"SHALL NOT
  merge BY ITS OWN ACT"*, admitting the lane arming the platform's auto-merge on
  its own pull request; `.github/workflows/floor-regeneration.yml` now arms it,
  and openxFactory's lockstep mirror is `amend-mirror-floor-regeneration-merge-authority`
  (ratified 2026-09-08). **`tasks.md` § 2.7 stays UNTICKED** — this packet did
  not perform the arming and its 2026-09-07 refusal stands as written; the
  successor that did is named beneath it.
- **admit a second class**, or widen this one. Both are code-owner-reviewed edits
  to a base-branch file, and the enrolled-surface tests assert the exact shape.
- **become irreversible.** The candidate entry IS the kill switch: one
  code-owner-reviewed edit, read from the base branch, effective on the next
  evaluation, visible in the file's history forever. **It has never been thrown**
  — `tasks.md` § 3.6 is open, and a switch never thrown is a switch nobody knows
  works.
- **discharge the packet's remaining behavioural evidence.** § 3.1, § 3.3, § 3.4
  and § 3.6 of `tasks.md` remain open; the 2026-09-10 addendum there records
  exactly what each is missing.

---

## 5. SIGNATURE

```
Convened:        2026-09-10 — NO SEAT RUN. Direct convener disposition under
                 `gate-rules.yaml`'s `output_disposition:
                 convener_accepts_or_rejects_on_record` (§ 0.1), on the
                 precedent of codexFactory
                 records/2026-09-04-gate-rules-packaged-contract-floor-widening.md
Instrument:      Gate-Rules Council record — OQ-2 of
                 `extend-merge-master-envelope-to-floor-bot-lanes`, OQ-C1 of the
                 codexFactory companion, answered as recommended 2026-09-07
                 under the ratification word and unchanged since (§ 2)
Seats:           NONE. No seat return exists and none is claimed.
Verdict:         ADMIT
Signed:          Brett Heap — openxFactory repository owner, the convener of
                 this body, and the accountable human the charter declares —
                 2026-09-10 ~17:2xZ, in session to lane
                 `openxfactory-2` (display `openXfactory-2`), verbatim
                 "do 1 and 2, sign, file it, do 744" — item 3 of that comment
                 names this record and says "then SIGN on his word 'sign'".
                 HIS ACT, THE LANE'S PEN: the lane wrote this block on his word
                 and decided nothing.
Recorded on:     openxFactory #745, comment 5622848416
                 <https://github.com/opensoft/openxFactory/issues/745#issuecomment-5622848416>
                 codexFactory #232 — OWED, § 8
Applied at:      ALREADY APPLIED (§ 0.2) — codexFactory
                 `.github/merge-approval-envelope.yml`, enrolment commit
                 `4c0053c3` (2026-09-07T21:16:37Z), as amended at `e912e260`,
                 `d1f8bf1b` and `cca0304d`. Exercised at codexFactory #314
                 (merge `b08958ae`) and #325 (head `c4e40f33`, merge
                 `df42f803`).
```

### 5.1 The verdict, and the dispositions NOT taken

The precedent enumerates the branches it did not take rather than only the one
it did, so that a reader can see the verdict was chosen and not merely reached.
The same four branches, against this act:

- **ADMIT — TAKEN.** The one class of § 1, on its landed conditions, in the one
  repository, retrospectively on the evidence of § 3 (§ 0.2).
- **ADMIT AS AMENDED — NOT TAKEN.** No amendment was proposed by anyone and the
  convener added none; with no seat run there was no return to amend from
  (§ 0.1). An amendment would in any case be an edit to a base-branch file in
  codexFactory, which this lane may not make (§ 0.3, § 8).
- **REFUSE — NOT TAKEN**, and what it would have cost is recorded below rather
  than left as a hypothetical.
- **PARK FOR LIAISON — NOT REACHED.** The charter's
  `failure_semantics.split_vote: park_for_liaison` requires a split, and with no
  seat returns there is none — the same reading the 2026-09-04 precedent
  recorded for the same reason.

**ON ADMIT** the draft directed: *"tick `tasks.md` § 4.2 … and merge the
enrolment pull request — that merge is the admitting act."* The merge is done
(§ 0.2). The box that answer belongs to in THIS repository is `tasks.md` § 5.4,
an **owner's-act box**, and this packet's own rule is that *"an agent may not
tick an owner's-act box at all"* — so the box is left UNTICKED and the act is
recorded beneath it in the dated form `tasks.md`'s own § 5.1, § 5.2 and § 5.5
already use (those are that file's boxes, not this record's § 5.1 above). **The
one thing left that only Brett Heap can do about this record is say the word
that ticks `tasks.md` § 5.3 and § 5.4**, and § 8 states it as a one-line ask.

**ON REFUSE** the draft directed a ruling on its § 4.4: file N-1 (e) as the
successor, or record that the two human merges per cycle stand. **That branch is
now moot in a way worth recording: N-1 (e) was filed and landed anyway** (§ 3.4),
so a refusal here would withdraw the class from an envelope whose surface already
satisfies the canon it was thought to breach.

---

## 6. RECORDS

- The word: openxFactory
  [#745](https://github.com/opensoft/openxFactory/issues/745#issuecomment-5622848416),
  comment 5622848416, item 3.
- Ratification of the packet: `review/ratification-2026-09-07.md` (this
  directory) and codexFactory's own copy.
- Governing issue: openxFactory
  [#745](https://github.com/opensoft/openxFactory/issues/745). First-cycle
  record: codexFactory
  [#232](https://github.com/codeXfactory/codexFactory/issues/232), comments
  5610185095 and 5611865608 (the two live cycles).
- The measurement the instrument was chosen on: `design.md` § 2.2, § 2.2a, § 2.3.
- The class: codexFactory `.github/merge-approval-envelope.yml`, second
  candidate.
- The live proof: codexFactory
  [#314](https://github.com/codeXfactory/codexFactory/pull/314) → `b08958ae`;
  [#325](https://github.com/codeXfactory/codexFactory/pull/325) → `df42f803`
  (runs `34425922954`, `34427337240`; review 5161853361);
  [#302](https://github.com/codeXfactory/codexFactory/pull/302), the park.
- The alternative that was taken: codexFactory `relocate-review-authority-floor`
  (#293) and openxFactory `relocate-review-authority-floor-mirror` (#817),
  landed at codexFactory `d1f8bf1b`.
- The arming: codexFactory `amend-floor-regeneration-merge-authority` (merge
  `93f0f0d7`) and openxFactory `amend-mirror-floor-regeneration-merge-authority`.

---

## 7. RECONCILIATION — 2026-09-10

**The draft is not edited; it is superseded by this record, and every difference
is named here.** Each row is a claim the draft made on 2026-09-07, what the tree
and the API say on 2026-09-10, and where this record says it differently.

| # | The DRAFT's claim (2026-09-07) | What the tree and the live cycles show (2026-09-10) | Where this record differs |
|---|---|---|---|
| **R-1** | § 1: the admitted writable path is `scripts/merge_master/openxfactory-review-authority-floor.yaml` | The envelope's `path_allowlist` names **`floor/openxfactory-review-authority-floor.yaml`**; #314 and #325 each changed exactly that one path. Relocated at codexFactory `d1f8bf1b`, 2026-09-09T03:51:09Z | § 1's table carries the landed path; the superseded spelling is named beside it |
| **R-2** | § 2, § 3.2: the act NARROWS A RULE, and ONE governance ground is spent — the CODEOWNERS-scoped prohibition of promoted `Bounded autonomous surface` | `/floor/` appears in NO codexFactory CODEOWNERS entry; the promoted requirement is **unnarrowed on `main`** and reads exactly as it did. The relocation is decision **N-1 (e)**, the option the draft called "not taken". The live approvals are lawful under canon **as written** | § 3.2 rewritten: **nothing is spent**. § 3.4 rewritten from "the alternative that was not foreclosed" to **the alternative that WAS TAKEN**. § 2 keeps the instrument and states the two reasons it survives its ground |
| **R-3** | § 4: signing does not make the lane unattended — `Require Code Owner Review` id **18834180** on `opensoft/codexFactory` blocks it; a bypass actor is a separate owner's act (its `tasks.md` § 4.3 / this packet's § 5.3), NOT performed | **No bypass actor was ever added and none was needed.** The repository moved to the `codeXfactory` org; the ruleset is **22655341** (`require_code_owner_review: true`, count 0 — vacuous over an unowned path) and the count-1 rule is **22655338** (count 1, code-owner review off), which **the App's APPROVE review satisfied**. Both bypass lists are `OrganizationAdmin` only | § 4 rewritten with the measured rulesets and their current ids; the enabling act is recorded as **not performed and not needed** |
| **R-4** | § 4: signing does not arm any merge — the lane MAY NOT, by `add-floor-regeneration-automation` § *An automated floor regeneration only ever proposes* | `amend-floor-regeneration-merge-authority` (ratified 2026-09-08, merge `93f0f0d7`) narrowed it to *"SHALL NOT merge BY ITS OWN ACT"* and admits the arming; the workflow arms it, and #325 merged under it with no human act | § 4 rewritten; the successor named, and `tasks.md` § 2.7 confirmed as still the packet's own unticked refusal |
| **R-5** | § 3.3: the class is PERMANENTLY ADVISORY and reaches approval only through the tier-1 envelope — computed from the class floor | **HOLDS, and is now observed.** Both approvals read *"tier-1 envelope holds; tier 2 not consulted"*; the pre-greenness park reads *"no per-repo gate rule declares applies_to.candidate_id … tier-1 evaluation alone"*. No tier-2 gate rule exists or is needed | kept, with the live citations added to § 3.3 |
| **R-6** | § 5: *"On ADMIT … merge the enrolment pull request — that merge is the admitting act"*, and the envelope: the merge *"must not happen before that record is signed"* | The enrolment merged **2026-09-07** at `4c0053c3` and has approved twice since | new § 0.2: the order did not hold; the signature ratifies an act already taken, said plainly rather than implied |
| **R-7** | — (the draft could not have known) | The approval record renders **`Candidate class: unrecorded`** on both #314 and #325, against this packet's own ratified requirement that the record carry the class it matched | new § 3.5 (a), and `tasks.md` § 3.1 stays open on exactly this |
| **R-8** | — (the draft could not have known) | codexFactory **#302** parked with `gate_integrity_path` on `tests/merge-master/test_repository_gate_floor.py` — a path the class explicitly admits but which the tier-2 never-clearable class refuses first — and was closed by hand. The autonomy is over ONE of the two enumerated paths | new § 3.5 (b) |

**Unchanged from the draft, and re-verified rather than assumed:** the class id,
author, head ref, base ref, same-repository predicate and `revert_suffices`
(§ 1); the kill-switch shape (§ 4); the 2026-08-28 refusal being undisturbed and
the re-pin lane staying under a human word (§ 0.4, § 3.3); and the floor
untouched (§ 3.3).

---

## 8. WHAT IS OWED AFTER THIS RECORD

**In codexFactory — not this lane's to perform, named so it is not forgotten:**

1. **Mirror this record** into `hermes/domain/review-councils/records/` as
   `2026-09-10-gate-rules-openxfactory-floor-regeneration-admission.md`, where
   the body's records live (§ 0.3).
2. **Supersede the DRAFT** at
   `openspec/changes/extend-merge-master-envelope-to-floor-bot-lanes/review/gate-rules-council-admitting-record-DRAFT.md`
   — a header naming this record as the signed successor. Its content is the
   2026-09-07 measurement and should stay as written.
3. **Repair § 3.5 (a)** — render the matched candidate id on the APPROVE path as
   the PARK path already renders it.
4. **Rule § 3.5 (b)** — either accept that a floored-membership cycle parks for a
   human (fail-closed, and possibly right), or take the second enumerated path
   out of the tier-2 never-clearable class. It is codexFactory's to decide.

**In openxFactory — the owner's, and ONE LINE OF HIS WORD DISCHARGES BOTH:**

5. `tasks.md` **§ 5.3** (the enabling act) and **§ 5.4** (OQ-2's instrument) are
   both owner's-act boxes, both are DISCHARGED on the evidence above — 5.3 by
   never having been needed, 5.4 by this record — and this packet forbids an
   agent to tick either. **The ask is exactly: "tick 5.3 and 5.4".** Until then
   the dated notes beneath them carry the acts, which is how 5.1, 5.2 and 5.5 are
   already carried.
