# Tasks: add-standing-policy-compliance-contract

Status: ratified
Ratified: 2026-08-26 — `review/ratification-2026-08-26.md`. These checkpoints
are authorized, not performed, by ratification.

This OpenSpec list records governance, handoff, realization evidence, and the
archive gate. Executable implementation tasks live only in the named Speckit
features, per the repository constitution.

## 1. Ratification and implementation handoff

- [x] 1.1 Ratify the neutral `intent-compliance` requirement set and preserve
      Brett Heap's decision record.
- [x] 1.2 Create the single openxFactory implementation handoff at
      `specs/015-intent-compliance-contract/`.
- [ ] 1.3 Merge the green Speckit realization that publishes the five-record
      family, canonical validator, conformance corpus, registration and next
      additive contract bundle.
- [ ] 1.4 Record exact release commit, bundle, tag and validation evidence in
      `realization-evidence.md`.

## 2. First-conformer handoff

- [ ] 2.1 Raise codexFactory `add-intent-compliance-gate` against the released
      openxFactory bundle, preserving domain ownership of the five concrete
      standing-policy classes.
- [ ] 2.2 Hand that ratified codexFactory change to exactly one Speckit feature
      implementing approval, pre-dispatch, admission, bounded classifier,
      current allowance, and domain-owned runtime revocation-race behavior.
- [ ] 2.3 Merge the green first-conformer realization and record FEAT-003
      evidence for no allowance, current allowance and revoked allowance.

## 3. Archive gate

- [ ] 3.1 Archive only after the released neutral bundle and merged
      first-conformer evidence are both named in `realization-evidence.md` and
      all OpenSpec/repository gates are green.
