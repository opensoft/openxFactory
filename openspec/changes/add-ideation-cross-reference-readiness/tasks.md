# Tasks: Ideation Cross-Reference Readiness Index

## 1. Sequencing And Contract Baseline

- [x] 1.1 C1 rebase done 2026-07-14: the doc-health MODIFIED "Deterministic check families" delta was rebased onto the promoted 13-family wording (it hardcoded a stale "fifteen"). Dropped the `ideation routing`/`proposal origin` family names, their two run-scenario bullets, the copied "Routing/Origin conformance checks fire" scenarios, and the `ideation-organizer` lane mention (both the ADDED readiness-lane requirement and the MODIFIED clause). Kept ONLY this change's edits: "and readiness scoring" in the deterministic clause and `ideation-readiness` appended to the separate-lane list. `openspec validate add-ideation-cross-reference-readiness --strict` and `--all --strict` (27/27) both pass.
- [x] 1.2 Verified 2026-07-14: `.openspec.yaml` origin (kind `staged`, id `openxFactory:staging:ideation-cross-reference-readiness`, path `ideation/staging/ideation-cross-reference-readiness`) matches `supporting-docs/manifest.yaml` origin + `origin_path`; the sole bundled file `ideation-cross-reference-readiness.md` carries `Staging ID: openxFactory:staging:ideation-cross-reference-readiness` and `Proposed by: add-ideation-cross-reference-readiness`; `remaining_paths: []` (full promotion). All three linkage points agree — no findings.

## 2. Index Contract (openxFactory)

- [x] 2.1 Realized 2026-07-14: `contracts/schemas/ideation-cross-reference.schema.yaml` — the structured index (source of truth `ideation/cross-reference.yaml`; `.md` is a generated projection, noted in the schema description). Envelope: `kind: ideation-cross-reference`, `schema_version` const 1, optional `repository` (routing-kernel id), and a `generation` determinism stamp $ref'd verbatim from the snapshot. Topic entries: id/name/topics/tag_sources (`topics-header`|`target-capabilities-header`|`document-catalog`), members (routing-kernel repository/path + `lifecycle_status` stage + matched_tags), `extension_fit` (promoted_spec REQUIRED when has_promoted_fit true; explicit no-fit statement otherwise; archive-pointer-only left to the validator), three-tier `readiness` (per tier: score 1-10 + organizer evidence, OR unscored_reason), `conflict_flags` (tier-spread), `recommendation` (flagged + const pending_review + optional human_disposition). C2 seam CLOSED: top-level `possibles_register` $refs `ideation-possibles-register.schema.yaml#/$defs/possibles_register` (kernel seam comment updated to name this schema as the resolved home). C4: `human_seen` intake $def ($ref score_evidence + pending_review). Evidence = documented structural mirror of the ORGANIZER recommendation (pins $ref the routing-reference kernel → byte-identical), NOT the cataloger facet. Meta-valid (draft 2020-12); four-schema co-load (index + kernel + snapshot + routing-reference) resolves every cross-file $ref.
- [x] 2.2 Realized 2026-07-14: `examples/ideation-cross-reference/`. Comprehensive valid `ideation-cross-reference.example.yaml` = one topic entry per case — multi-stage cluster, promoted-fit citation, gated recommendation (min>=8), spread conflict, missing-tier-with-reason, explicit no-promoted-fit, human-seen entry — plus the embedded `possibles_register` (reuses the register example content). `negative/score-out-of-range.yaml` fails at the SCHEMA layer (score 11 > 10); `negative/archive-pointer-only-fit.yaml` is schema-valid BY DESIGN and fails at the VALIDATOR layer (task 2.3) — every invalid names its violated rule + layer in its header. README carries the case map + four-schema co-load note. python-jsonschema (FormatChecker, four-schema registry): valid example 0 errors, both negatives as declared.
- [x] 2.3 Realized 2026-07-14: `scripts/validate-ideation-cross-reference.py` — strict validator over the four-schema registry (index + possibles-register kernel + snapshot + routing-reference kernel), FormatChecker attached, kind-detection + self-gating packaged-examples self-test + directory sweep, modelled on `validate-ideation-dashboard-contracts.py`. Validator-side rules: extension-fit citation resolution (promoted_spec must resolve to an `openspec/specs/` promoted dir OR a non-archived change's spec-delta capability; archive-folder/path or dangling name = error — `archive-pointer-only-fit.yaml` fails HERE by design); min>=8 gate arithmetic (flagged IFF all three tiers scored and min>=8; flagged-with-unscored-tier / flagged-below-min / gate-should-fire = errors); spread-conflict consistency (scored max-min >= 4 requires a `tier-spread` flag — documented implementation choice pending spec confirmation at 3.4); topic-entry id uniqueness; C3 ownership line — register entry shapes are schema-validated via `$ref`, id-uniqueness/transitions DELEGATED to the dashboard validator with a note. Wired into the per-repo preflight via the contracts/README "Contracts Pending Realization" rows (archive-gate registration). Self-test: 1 valid confirmed valid, 2 negatives confirmed invalid for their intended reason.
- [x] 2.4 Realized 2026-07-14: bootstrapped `ideation/cross-reference.yaml` (source of truth) + generated `ideation/cross-reference.md` projection. `scripts/bootstrap-ideation-cross-reference.py` reads `ideation/brainstorm/*.md` + `ideation/staging/*/*.md` `Topics:`/`Target capabilities:` headers and applies a documented deterministic clustering rule (token = seed; token on >= 2 docs forms a cluster; co-extensive tokens merged; id = `cl-<alphabetically-first token>`; entries sorted by id, members by path) — 15 topic clusters (top: doc-management+doc-workflow ×9, ideation-dashboard ×6, roles-authority-model ×6, client-infrastructure-liaison/-request ×5). Bootstrap posture: every `extension_fit.has_promoted_fit: false` with an honest "not yet evaluated" statement (no promoted-fit claim it cannot cite), every readiness tier unscored ("bootstrap — scoring worker not yet realized"), no recommendation flags, possibles_register ABSENT. `.md` rendered by `scripts/render-ideation-cross-reference.py` with a GENERATED-FILE warning. Guidance: added a surgical "Cross-Reference Readiness Index" section to `ideation/README.md` pointing at the index/schema/requirements and the `staging/INDEX.md` distinction, referencing the ratified requirements without duplicating. Bootstrap validates clean (0/0 strict) under the 2.3 validator.

## 3. Readiness Worker (codexFactory)

Realized 2026-07-14 by codexFactory Speckit feature `003-ideation-readiness`, merged to main at `1fc0bd7` — ideation_readiness.py scorer + prompt contract v1; pre-merge adversarial review ACCEPT WITH FIXES (two load-bearing: openxFactory-only clustering scope; uncited-resolution exemption preventing spurious nightly regression issues). Suites 519+244 green. Both W2 implementation choices confirmed (design.md note).

- [x] 3.1 Add the readiness-scorer module with a versioned prompt contract producing three independent tier scores (domain, company, project/buildability) per topic cluster.
- [x] 3.2 Reuse the organizer/cataloger evidence contract verbatim for every score — committed source revision, passage hash and section reference, rationale, confidence, alternatives, `pending_review` disposition — and validate worker output against it before persistence.
- [x] 3.3 Implement cluster membership from the header tag sources with deterministic tie-breaks, and the additive catalog-tag fold-in path guarded off until `document-cataloging` realizes.
- [x] 3.4 Implement the minimum-score gate, the unscoreable-tier block, the spread-conflict flag, and the extension-fit citation check emitting `ideation-readiness` findings.
- [x] 3.5 Enforce the non-mutating bound: the pass writes only the index and evidence artifacts; add tests proving any other write path is rejected.

## 4. Nightly Lane (xFactory aggregation, omnigent-install)

Realized 2026-07-14 (same merge + aggregation 669d60a child workflow + omnigent-install f31a019 readiness-scorer profile). Lane reports SKIPPED until a host advertises the profile — valid landed state per the lane scenarios. OPS LEDGER before enabling the worker: deploy the profile to the CPC host AND add the submodule commit-back orchestration (the lane persists into the openxFactory submodule; nothing yet commits it back or bumps the aggregation pin — scored output would be discarded otherwise).

- [x] 4.1 Wire the readiness lane into the nightly run after the deterministic pass, consuming its inventory snapshot; a skipped or failed lane is reported as skipped and never affects deterministic results.
- [x] 4.2 Add the bounded read-only readiness-scorer profile in omnigent-install reusing the existing document-analysis host pattern, with workload-specific readiness assertions and no repository credentials in the worker environment.
- [x] 4.3 Link the index and evidence artifacts from the dated report; recommendations appear in the ranked plan as `contested`, at most `warning`, report-only items.

## 5. Tests And Records

- [x] 5.1 Add tests: gate fires only at minimum >= 8; one low tier blocks despite two 10s; spread conflict flagged below threshold; recommendation performs no state transition; tag bootstrap reads both header fields; evidence contract validates against the existing organizer/cataloger schema; absent findings from a skipped lane are not treated as resolved.
- [ ] 5.2 Keep the openxFactory README "OpenSpec Records" entry current through ratification, realization, and archive.
