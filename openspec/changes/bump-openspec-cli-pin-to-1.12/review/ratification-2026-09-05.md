# Ratification — 2026-09-05

## Decision

RATIFIED by Brett Heap.

## Authority, and the two words

Brett Heap's instruction, verbatim: **"ratify 677"** — given 2026-09-05, in
his own message, first-hand to Claude session `opsXfactory-1`
(`3d06c6e7-d68e-48e9-bd36-21c376c099f9`,
https://claude.ai/code/session_01WXVKhuYSmo4iXXqBHZ7LeX), which writes this
record.

The merge itself (92005d70, 2026-09-05T16:00:22Z) had already been performed
by the authoring lane (`codexfactory-0d`) on Brett's word given in that
session — so the PACKET landed ratified-in-fact with its record still owed
(task 6.1 was unticked at merge). This record discharges 6.1 with the word
heard here; the two words are the same operator, same day, same decision, and
both are recorded because both happened.

## What this ratification covers

The packet as merged: the disposition mechanism in the pin validator and
contract, the two cited dispositions (Brett's exit-2 ruling on the
scenario-currency/`Merged into` tool-vs-canon conflict), and the bump of the
fleet OpenSpec CLI pin to 1.12.0 by content address, with its evidence.

## Limits

It does not repoint `pytest-suite.yml`'s literal 1.2.0 (#667 task 5.1, owed
separately), does not archive `add-openspec-cli-pin`, and does not close the
dependency-closure shortfall both pin contracts declare.
