# repo-boundary-governance

**ONE `## MODIFIED` REQUIREMENT, AND THE ONLY CHANGE IS WHERE THE OBLIGATION
STANDS IN ITS OWN FIRST SENTENCE.** The block below is written OVER CANON —
`openspec/specs/repo-boundary-governance/spec.md` as `main` states it at
`114d6e3d`, the authoring basis — and it is BYTE-FAITHFUL BY CONSTRUCTION
rather than by transcription: the block was generated by slicing that file's
lines `34`–`66` (the `### Requirement:` heading at `:34` through `:66`, the
last non-blank line before the next heading at `:68` — 33 lines, sha256
`24f6479c03641027e791c46d6a1bc29ac6b5af6f37b6ca3c1f4781bdd3c069ee`, 2,885
bytes), applying the ONE change as an exact SINGLE-OCCURRENCE substitution over
the first sentence — the generator REFUSES on any other count — and re-wrapping
ONLY the paragraph that substitution touched, at the file's own width of 79.
Every other line of the block is canon's own bytes, compared line by line and
asserted. The generator's output and the block below — its `### Requirement:`
heading through the `Removed from canon` marker, the marker being a
DECLARATION about the block and not a unit of the requirement — are ONE STRING:
34 lines, sha256
`50ced1fa72901a62ea0add5401b9a8511297d04c612d4985fe13aa8dd9d5b0b4`, 3,404
bytes, on both sides.

**WHAT MOVES: THE FIRST SENTENCE OF THE FIRST BODY PARAGRAPH, RE-ORDERED, WITH
FOUR WORDS OF SUBJECT ADDED AT THE FRONT AND NOT ONE WORD REMOVED.** Canon
opens the obligation with its ENUMERATION — *"`Hermes-Install`,
`Omnigent-Install`, `Keycloak-Install`, `OpenXPKI-Install`,"* at `:35` — and
defers the conjunction that closes it, its verb and its `SHALL` to `:36`. The
block opens with a subject and the modal instead: *"The following install
repositories SHALL be scoped to subsystem install, operations, backup, restore,
upgrade, verification, and disaster recovery: `Hermes-Install`, …"*, the five
names following the colon in canon's own order, canon's own spelling and
canon's own serial comma. Measured, whitespace-split and CASE-SENSITIVE: the
new sentence's tokens minus the retired sentence's are `The`, `following`,
`install`, `repositories`, `recovery:` and `` `OmniWorker-Install`. ``, and the
retired sentence's minus the new one's are `recovery.` and
`` `OmniWorker-Install` ``. That is FOUR WORDS ADDED, NONE REMOVED, and two
punctuation moves — the sentence's terminal period leaves `recovery` for the
last name and a colon stands where it was. 215 characters become 251.

**AND THIS PACKET DOES NOT PRETEND THAT ADDING FOUR WORDS IS THE SAME ACT AS
ADDING NONE.** Its predecessor
`amend-neutral-product-pin-lockfile-first-line` re-ordered canon's own words
and added not one; that sentence already carried the clause *"the pin SHALL
carry a VENDORED RESOLUTION"* and needed only to be permuted. **THIS SENTENCE
HAS NO SUBJECT TO PROMOTE** — its subject IS the five-name list — so there is
no zero-word re-order of it, and every candidate that puts `SHALL` on line one
either introduces a subject noun phrase or moves only the line breaks.
`design.md` **D1** writes out all three candidates with their costs and puts
the choice for the owner.

**THE ACCOUNTING, DERIVED BY THE FAMILY'S OWN CODE.** `derive_units` over the
promoted requirement returns **13 units** — 4 body sentences, 3 scenario titles,
6 scenario bullets — and over this block it returns 13 units in the same
three-way split, of which **exactly ONE** is not canon's and **exactly ONE**
canon unit is not carried: the retired first sentence, which the block's one
`Removed from canon` marker names, and which the marker's own re-parse confirms
as its single name with an empty `quoted` list. All 3 promoted scenario titles
are carried in canon's order, the block adds none, and no scenario bullet, no
later body sentence and no requirement heading moves.

**THE INHERITED MARKER IS NOT CARRIED, ON CANON'S OWN RULE AND NOT ON THIS
LANE'S PREFERENCE.** The promoted requirement ends with a `Removed from canon`
marker written by `refresh-install-repository-enumerations` (2026-09-08) at
`:66`, and this block does not restate it. `doc-health`'s promoted requirement
*Currency of an active change's MODIFIED requirement* rules the point in terms:
*"**A marker is NOT a carriage unit, in either direction.** A marker promotes
into canon with the requirement that carries it, and if it were a unit every
later block would have to restate every marker any predecessor ever wrote,
forever. The durable record of a deletion is the archived delta, which is where
every other archived governance act is read from"*
(`openspec/specs/doc-health/spec.md:1801-1805`). That durable record exists and
is named:
`openspec/changes/archive/2026-09-09-refresh-install-repository-enumerations/specs/repo-boundary-governance/spec.md:37`,
whose block is BYTE-IDENTICAL to the promoted one this packet slices (sha256
`24f6479c…` on both sides). `design.md` **D2** carries the measurement of the
alternative: carrying the marker forward would name a sentence that is no
longer a unit of canon and is not a unit of this block either, which is the
marker-defect class's ground THREE, and the family reports it — measured, on
both candidate wordings.

**A RE-WRAP ALONE WOULD OWE NO MARKER, AND THAT IS WHY THIS ONE DOES.** Canon
holds that *"a re-wrapped paragraph compares equal to the same paragraph
wrapped differently"* (`openspec/specs/doc-health/spec.md:1668-1672`), so a
change that only moved line breaks would change zero units and declare nothing
— measured, in `design.md` **D1** option 2, on a second generated block that
returns **13 units, 0 uncarried, 0 added**. This block changes the sentence, so
the sentence is a REPLACED unit and one marker is owed and written.

**WHAT IS DELIBERATELY NOT TOUCHED.** No other requirement of this capability
is modified, added, renamed or removed. No obligation, no scenario, no field
and no consumer's conformance outcome moves in either direction. The
enumeration stays an INDEX of five admitted install repositories and does not
become a claim about which install repositories exist — the distinction this
capability's own *Install-repository enumerations are an index with a named
authority* requirement (`:308`) draws, and the reason `design.md` **D1**
refuses the variant that hard-codes the count.

## MODIFIED Requirements

### Requirement: Install repository scope
The following install repositories SHALL be scoped to subsystem install,
operations, backup, restore, upgrade, verification, and disaster recovery:
`Hermes-Install`, `Omnigent-Install`, `Keycloak-Install`, `OpenXPKI-Install`,
and `OmniWorker-Install`.

`Keycloak-Install` (`opensoft/Keycloak-Install`, pinned at
`installs/keycloak-install`) and `OpenXPKI-Install`
(`opensoft/OpenXPKI-Install`, pinned at `installs/openxpki-install`) were
admitted to the top-level xFactory aggregation on 2026-08-21 by the
`admit-install-repos-to-aggregation` change, which recorded path, remote,
visibility, exact validated commit, checkout, compatibility, update, and
rollback behavior for each — the separate reviewed act their own boundary
requirements demand. `OmniWorker-Install` (`opensoft/OmniWorker-Install`,
pinned at `installs/omniworker-install`) was admitted on the same terms on
2026-09-05 by opensoft/xFactory#274 (merge commit
`648c8bd3fc4717e5b969cd95ea3e0207700e8925`), the record its own
*"Worker-host aggregation pin is proposed"* scenario demands. This enumeration
is an index of admitted install repositories; it neither widens nor narrows the
scope each repository's own `repo-boundary-governance` requirement fixes.

#### Scenario: Hermes runtime procedure is changed
- **WHEN** a change installs, restores, backs up, upgrades, or verifies Hermes runtime behavior
- **THEN** the implementation detail MUST live in `Hermes-Install`

#### Scenario: Omnigent worker procedure is changed
- **WHEN** a change installs, restores, backs up, upgrades, or verifies Omnigent/Polly worker behavior
- **THEN** the implementation detail MUST live in `Omnigent-Install`

#### Scenario: Worker-host runtime procedure is changed
- **WHEN** a change installs, restores, backs up, upgrades, or verifies worker-host runtime behavior
- **THEN** the implementation detail MUST live in `OmniWorker-Install`

**Removed from canon by amend-repo-boundary-governance-scope-first-line (2026-09-11):** `` `Hermes-Install`, `Omnigent-Install`, `Keycloak-Install`, `OpenXPKI-Install`, and `OmniWorker-Install` SHALL be scoped to subsystem install, operations, backup, restore, upgrade, verification, and disaster recovery. `` — the sentence is REPLACED IN PLACE by the one above it and is not dropped, and every one of the five repository names is carried into it in canon's own order, canon's own spelling and canon's own serial comma. What moves is WHERE THE OBLIGATION STANDS INSIDE ITS OWN SENTENCE: the subject and the modal now open the first body line, which is the shape the other ten requirements of this specification already have, so a reader who reads one line and a parser that reads one line both meet the obligation there instead of meeting a list of names whose verb has not arrived. Four words of subject are added at the front, the enumeration moves behind the modal under a colon, and nothing else in the sentence changes: the same five repositories are scoped to the same seven activities, in the same words and the same order. Nothing this requirement obliges, admits or refuses moves, and the enumeration remains the INDEX the sibling requirement on enumeration authority says it is rather than becoming a claim about which install repositories exist. This reason carries no code span, so the marker names exactly one unit under the grammar it is written in.
