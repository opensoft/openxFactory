schema: spec-driven
created: 2026-08-21
origin:
  kind: staged
  id: openxFactory:staging:identity-brokering-plane
  path: ideation/staging/identity-brokering-plane
  reason: >-
    Exit 1 of the staged topic — the neutral persona/claims contract. The
    topic was organized 2026-08-21 from the 2026-07-14
    `keycloak-identity-brokering` brainstorm on the day Brett Heap ruled all
    three of that brainstorm's exit blockers: realm topology (single realm
    per environment plus Organizations for tenant AND served companies;
    isolation escalates by broker INSTANCE, never by realm split; the
    neutral contract stays silent on instance count), merge safety (explicit
    account linking plus an admin-approved merge queue, never silent
    email-match auto-link), and the ownership split (the
    github-administration precedent: neutral contract in openxFactory,
    administration workflow in OpsxFactory, deployable runtime in a new
    install repo). Also ruled beyond what the brainstorm asked: workloads
    are NOT persona users. This change carries the neutral half only; the
    OpsxFactory `keycloak-administration` workflow (exit 2) and the
    `Keycloak-Install` runtime (exit 3) are named successors that
    consume this contract's vocabulary, which is why it ratifies first.
  approved_by: >-
    Brett Heap (xFactory family session rulings R1, R3, R4, R5 and R7,
    2026-08-21; recorded on the staged topic fragment)
  approved_on: 2026-08-21
