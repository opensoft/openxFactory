# Tasks: Possibles Derivation Lane

Governance tasks (sections 1-2, openxFactory contract) come first; realization
tasks (sections 3-4, codexFactory + aggregation + omnigent-install Speckit work)
are marked as such and are NOT done by authoring this change.

## 1. Origin And Contract Baseline (openxFactory governance)

- [x] 1.1 Verified 2026-07-16: `.openspec.yaml` origin is complete — `kind: ad_hoc`, id `openxFactory:adhoc:possibles-derivation-lane`, with `reason` + `approved_by` (Brett, openxFactory operator authority) + `approved_on: 2026-07-16`. No organized staging fragment for the derive-possibles surface exists (staging topics are avatar-pilot-hardening, github-administration-plane, proposal-origin-contract, qualify-avatar-live-voice — none is this surface); the only `possibles-derivation` mention is the ideation-dashboard brainstorm v5 direction the origin cites, so ad-hoc status substitutes for no organized fragment.
- [x] 1.2 Confirmed 2026-07-16 (additive determination recorded): the section-2 kernel delta adds ONLY optional `register_entry.origin` (enum `[human-authored, ai-derived]`; absent = human-authored), optional `register_entry.derivation` (`$ref` the new `$defs/derivation`), an `allOf` conditional that fires only when `origin: ai-derived`, and the `$defs/derivation` + `$defs/derivation_human_disposition` definitions. No existing `required` list is loosened, no field is retyped or removed, and the conditional never fires on an origin-absent (human-authored) entry — so per the kernel's own additive-growth rule NO `contract_schema_version` bump is warranted (it stays `1`; registration deferred to the realization commit per task 2.6).
- [x] 1.3 Verified 2026-07-16: `OPENSPEC_TELEMETRY=0 openspec validate add-possibles-derivation-lane --strict` reports the change valid, and `--all --strict` passes for every change; both green before commit.

## 2. Register Contract Delta (openxFactory)

- [x] 2.1 Realized 2026-07-16: `contracts/schemas/ideation-possibles-register.schema.yaml` — added optional `register_entry.origin` (enum `[human-authored, ai-derived]`; absent = human-authored), optional `register_entry.derivation` (`$ref: "#/$defs/derivation"`), and an `allOf` conditional requiring `derivation` when `origin: ai-derived`, modelled one-for-one on the index `topic_entry.origin` + `human_seen` conditional. Updated the kernel description with an AI-DERIVATION INTAKE note (the possibles-side sibling of human-seen cluster intake; lands ON this fragment) confirming the no-bump additive posture. Meta-valid (draft 2020-12); every existing register example stays green.
- [x] 2.2 Realized 2026-07-16: `$defs/derivation` = required `worker_run` (`correlation_id`, `worker_profile`, `prompt_contract_version` — all non-empty strings) + required `disposition` (`const: pending_review`, the machine output) + optional `human_disposition` (`$ref: "#/$defs/derivation_human_disposition"` — a LOCAL structural mirror of the index schema's `human_disposition`: `outcome` accepted|rejected|deferred + `authority` required, optional `at` date + `note`), copied field-for-field and NOT `$ref`'d back into the index schema (which embeds this kernel) to avoid a circular reference — the family's established mirror pattern (as `score_evidence` mirrors the organizer recommendation).
- [x] 2.3 Realized 2026-07-16: `examples/ideation-dashboard/` (the register example set the delta reuses). Valid `derived-possible-register.example.yaml` = an undisposed `origin: ai-derived` entry (`derivation.worker_run` + `pending_review` + a `claiming_clusters` edge + a `supporting_evidence` pin), an accepted derived possible (stays `latent`, RETAINS `origin: ai-derived`, carries the `human_disposition` mirror), and a human-authored possible (no `origin`) coexisting unaffected. `negative/register-derived-missing-derivation.yaml` (`origin: ai-derived` with NO `derivation` block — the top-level allOf conditional) and `negative/register-derived-missing-worker-run.yaml` (a `derivation` block missing its `worker_run`) both fail at the SCHEMA layer; `negative/register-derived-unsourced.yaml` is schema-VALID by design and fails at the VALIDATOR layer (no cluster edge / evidence pin); `negative/register-derived-bad-disposition.yaml` fails at BOTH layers (machine `derivation.disposition: accepted` violates the `const: pending_review` and the delegated rule) — each names its violated rule + layer in its header. Added one-way-disposition transition pairs (`valid-derived-disposition`, `invalid-derived-undispose`, `invalid-derived-origin-launder`). Proven against the FOUR-schema co-load registry (index + register kernel + snapshot + routing-reference): valid example 0 schema errors, missing-worker-run rejected at the schema layer, unsourced schema-valid (validator-layer) exactly as declared.
- [x] 2.4 Realized 2026-07-16: `scripts/validate-ideation-dashboard-contracts.py` (the C3 delegated register validator) now enforces — `origin: ai-derived` requires a `derivation` block (`register-derived-missing-derivation`, also schema-caught); a derived entry cites at least one `claiming_clusters` edge AND one `supporting_evidence` pin (`register-derived-unsourced`); machine output is `derivation.disposition: pending_review` only (`register-derived-bad-disposition`); the one-way disposition lifecycle in TRANSITION mode — a possible's `origin` is fixed so an accepted derived possible retains `ai-derived` (`register-derived-origin-changed`) and a disposed entry is never edited back to undisposed `pending_review` (`register-derived-undispose`); `id` uniqueness already spans derived entries (`register-duplicate-id`), and state-machine terminality already blocks resurrection. Each rule is exercised by a packaged negative: `register-derived-missing-derivation` (derivation-required conditional), `register-derived-missing-worker-run` (worker_run required), `register-derived-unsourced` (source citation), `register-derived-bad-disposition` (machine `pending_review` only), plus the two disposition transition pairs. Validator tests = the self-gating packaged fixtures (the family's house pattern): self-test confirms 9 valid / 25 negative / 8 transition-pair, 0 errors 0 warnings under `--strict`.
- [x] 2.5 Realized 2026-07-16: `scripts/validate-ideation-cross-reference.py` — extended the C3 ownership line (docstring + the `note_possibles_register` note) to state that the AI-derived `origin`/`derivation` shape, the source citation, the machine-only `pending_review` disposition, and the one-way disposition lifecycle are DELEGATED to `validate-ideation-dashboard-contracts.py` and NOT duplicated here (no checking logic added). `ideation/README.md` Cross-Reference Readiness Index section — added an additive "AI-derived possibles" pointer citing the schema (`origin` + `derivation`) and this change's `ideation-cross-reference` requirements by name, without restating SHALL/MUST wording.
- [ ] 2.6 Register the delta at realization in `contracts/manifest.yaml` / `contracts/CHANGELOG.md` / `contracts/README.md` (avatar-client / document-cataloging precedent: registration at the realization commit after merge order is known), not before.

## 3. Derivation Worker (codexFactory Speckit realization)

Realization follows the ideation-readiness two-repo split; this section is
codexFactory Spec Kit work, not done by authoring this change.

- [ ] 3.1 Add the derive-possibles worker module with a versioned prompt contract that reads the index (topic clusters, staged topics, member documents) and proposes candidate possibles (title, claim, rationale prose only).
- [ ] 3.2 Stamp orchestration-computed identity and evidence on every proposal (register `id`, correlation id, passage/content hashes, counts); discard and never trust worker-supplied precision values; void proposals whose worker precision values disagree.
- [ ] 3.3 Require and validate the register evidence contract before persistence: `origin: ai-derived`, `derivation.worker_run` + `pending_review`, at least one `claiming_clusters` edge and one `supporting_evidence` pin, and a `provenance` citing the primary source member + cluster context; reject unsourced or malformed proposals.
- [ ] 3.4 Implement the next-run merge into `possibles_register` under concurrency protection against stale overwrite, preserving `id` uniqueness and never editing a disposed entry; wire human gate-console dispositions (accept -> latent, reject -> rejected with reason+citation, defer) into the merge.
- [ ] 3.5 Enforce the non-mutating bound: the pass writes only the `possibles_register` section and evidence artifacts; add tests proving any other write path is rejected and that source documents are never modified.

## 4. Nightly Lane (xFactory aggregation, omnigent-install — realization)

- [ ] 4.1 Wire the possibles-derivation lane into the nightly run after the deterministic pass, consuming the same inventory/index snapshot; a skipped or failed lane is reported as skipped and never affects deterministic results; derived possibles appear in their own report section, excluded from the Ranked Plan.
- [ ] 4.2 Add the watchdog: report and cancel a derivation child queued beyond ten minutes or running beyond thirty minutes; prove offline/unauthorized/partial/invalid/failed worker output cannot delay or suppress deterministic reporting.
- [ ] 4.3 Add the bounded read-only `derive-possibles` profile in omnigent-install reusing the existing document-analysis host pattern, with workload-specific readiness assertions and no repository credentials in the worker environment.
- [ ] 4.4 Commit immutable derivation evidence under the factory identity, add the openxFactory submodule commit-back + aggregation pin bump that persist the merged register, and link the index + evidence from the dated report.

## 5. Tests And Records

- [ ] 5.1 Add tests: a derived entry carries origin/derivation/pending_review + source citations; an unsourced or worker-run-less proposal is rejected; a mis-copied worker precision value voids the affected proposals; accept -> latent retaining origin; reject -> rejected with reason+citation; no edit back to pending_review; a skipped lane leaves deterministic results and prior derived possibles untouched; undisposed derived possibles are excluded from ranked plans and carry a non-`indexed` edge class.
- [ ] 5.2 Keep the openxFactory README "OpenSpec Records" entry current through ratification, realization, and archive.
