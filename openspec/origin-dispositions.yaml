# Origin dispositions — the ACCEPTING half of `release-realization`
# § "Origin retention at archive" (issue #745; read by
# `scripts/proposal-support.py` ONLY where a packet's origin declaration
# differs from the one at its ratifying commit). An entry is owner-attributed,
# names both commits in full, lists exactly the origin keys that moved, and
# quotes the verbatim word that authorized the move. A valid `accept` moves
# the gate's comparison baseline to the accepted declaration; it switches
# nothing off. Entries are permanent: the archived packet carries the accepted
# origin for good, and this file is the only place that says why, on whose
# word (see docs/document-lifecycle.md, "Proposal origin declaration").
schema_version: 1
kind: origin_dispositions
dispositions:
  - change_id: extend-prose-tagging-target-to-pinned-capabilities
    ratified_at: 9c829d48d0805e78d22cc2728a59812e1835e441
    mutation_at: b18e08b4ecc23b424c62ed933f2c6d0a5a2c1586
    changed_keys: [approved_by, approved_on]
    disposition: accept
    disposed_by: Brett Heap (openxFactory repository owner)
    disposed_on: "2026-09-13"
    word: >-
      "Tighten to fail-closed after all" — Brett Heap's act 5 on task 1.2,
      2026-09-13T01:25:07Z (±3 min), an interactive multi-choice SELECTION
      whose description read verbatim: "Reopens 1.2: D-2 refuses a pinned
      target whose record carries no capabilities enumeration; the four
      markers stay unresolvable until openXwallet's pin publishes one.
      Reverts your 23:20Z selection; a fix round re-encodes and the record
      changes."
    cited_to:
      - https://github.com/opensoft/openxFactory/issues/992#issuecomment-5649935136
      - https://github.com/opensoft/openxFactory/issues/745#issuecomment-5649935244
      - https://github.com/opensoft/openxFactory/pull/1019 (Copilot thread PRRT_kwDOTAvnrs6h8a62, fix round 4)
    fact: >-
      The ratifying commit 9c829d48 recorded acts 1-4 (2026-09-12 ~23:20Z,
      approved_on 2026-09-12). Act 5 re-ruled task 1.2 alone and, by its own
      words, changed the record; commit b18e08b4 re-wrote `approved_by` to
      carry all five acts and moved `approved_on` to 2026-09-13. The accepted
      declaration is therefore the ratifier's own later word, not an edit by
      anyone else; every other origin key (`kind`, `id`, `reason`,
      `proposed_by`, `proposed_on`, `related`) is unmoved, as measured by the
      gate.
  - change_id: add-estate-repository-inventory
    ratified_at: 344ad3c7bc0b662a99590c836e4c91edcc77b3cf
    mutation_at: 0f23341fcf8c04ea234a57632c5ff321bc47c0f8
    changed_keys: [approved_by]
    disposition: accept
    disposed_by: Brett Heap (openxFactory repository owner)
    disposed_on: "2026-09-21"
    word: >-
      "Drop the pin admission on row 3" — Brett Heap's fifth word on this
      packet, 2026-09-21 at approximately 20:30Z, a MULTIPLE-CHOICE selection
      in the lane's terminal to lane `openxfactory-5` taking the recommended
      option, recorded verbatim as the RULED line "Drop the pin admission on
      row 3 (Brett Heap, 2026-09-21 ~20:30Z, terminal multichoice, recommended
      option): amend add-estate-repository-inventory design.md D0.2 row 3 to
      gitlink + workflow only, the pin kind's definition unchanged; encode as
      a ratified-text amendment PR on the packet on main, then re-class the
      inventory row in #1119 and resolve its thread".
    cited_to:
      - opensoft/brett-wip lanes/log/openXfactory-5.md line 144 — the RULED line for that word, 2026-09-21T20:28:46Z, against opensoft/openxFactory#1087 (brett-wip commit efc84511bab5d70d5545afa490a7dcd265cfae4b)
      - https://github.com/opensoft/openxFactory/pull/1130 (merge 0f23341fcf8c04ea234a57632c5ff321bc47c0f8) — the ratified-text amendment that encoded the word, appending it to `approved_by`
      - openspec/changes/archive/2026-09-23-add-estate-repository-inventory/review/ratification-2026-09-18.md § 7 — the full record of the word and of every site it was encoded at, `.openspec.yaml` `approved_by` among them
      - opensoft/brett-wip lanes/log/openXfactory-5.md line 167 — Brett Heap's archive word "archive it when the draft is up", RULED 2026-09-23T21:04:55Z against opensoft/openxFactory#1087 (brett-wip commit 837f44cf9dd0aa913e48b52bb2f5c097eef396ae), the act under which this entry is written
    fact: >-
      The ratifying commit 344ad3c7 (PR #1101's squash landing, the first
      commit whose `proposal.md` declares `Status: ratified`) carried an
      `approved_by` recording the first four words of 2026-09-18. The fifth
      word, three days later, re-ruled ONE row of `design.md` D0.2 and nothing
      under `openspec/specs/`; PR #1130 (`0f23341f`) encoded it and APPENDED a
      ten-line paragraph recording that word to `approved_by` — a pure
      addition, no existing line of the block moved, `approved_on` unmoved.
      The accepted declaration is therefore the ratifier's own later word,
      recorded, and not an edit by anyone else; every other origin key
      (`kind`, `id`, `reason`, `proposed_by`, `proposed_on`, `approved_on`) is
      unmoved, as measured by the gate. No entry was written at #1130's
      landing: the archive gate first compared this packet's origin block with
      its ratifying commit at the archive act on 2026-09-23 and refused, and
      this entry is written then, in the archive pull request, in the shape of
      this file's first entry, whose accepted declaration is likewise the
      ratifier's own later word. RESTORING the ratified bytes instead would
      delete the only origin-block record of that word.
