# expected_failure: subject.relation_to_baseline must be one of
# A relation outside the ratified enum, and the one this contract exists to
# refuse: `stricter_only` is the tenant layer's comparability invariant, whose
# partial orders are keyed to tenant-policy keys and would return
# review_required for every additive named constraint.
schema_version: 1
kind: hermes_subject_overlay

subject:
  id: project-alfa
  subject_kind: project
  display_name: Project Alfa Subject Hermes
  policy_namespace: opensoft.codexfactory.project-alfa
  relation_to_baseline: stricter_only
  policies:
    admission-conditions:
      policy_id: admission-conditions
      policy_namespace: opensoft.codexfactory.project-alfa
