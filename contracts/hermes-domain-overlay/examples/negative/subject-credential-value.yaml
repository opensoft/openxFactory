# expected_failure: prohibited credential value
# A subject overlay carries policy, never key material. Seeded content is
# replicated into hermes_layer_content rows; a baked credential there is a
# secret in a governance database.
schema_version: 1
kind: hermes_subject_overlay

subject:
  id: project-alfa
  subject_kind: project
  display_name: Project Alfa Subject Hermes
  policy_namespace: opensoft.codexfactory.project-alfa
  relation_to_baseline: additive_constraints_only
  policies:
    admission-conditions:
      policy_id: admission-conditions
      policy_namespace: opensoft.codexfactory.project-alfa
      enforcement_endpoint:
        url: https://hermes-opensoft-qa.xforge.us/api/v1
        token: ghp_examplenotarealtokenexamplenotarealtoken
