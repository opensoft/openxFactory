# expected_failure: authority_boundaries
schema_version: 1
kind: hermes_domain_overlay

domain:
  id: codex
  display_name: codex Domain Hermes
  approval_scope_kinds:
    - engineering_intent
  required_approval_fields:
    - approved_scope_ref
