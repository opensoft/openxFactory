# expected_failure: breach_declared_within_tolerance
# expected_failure_detail: within every missing witness's declared margin
# A breach declared thirty minutes into a twenty-four-hour horizon: the margin
# — horizon plus skew plus tolerance — is applied in ONE direction, and a
# horizon exceeded by less than it is not a breach. The harm is asymmetric:
# neither answer presents the item as anchored, but a false breach raises the
# operator obligation on a sound item and turns the signal into noise.
schema_version: 1
kind: xfactory_chain_anchoring_verification_result
result_id: vr-negative-0021
subject_ref: item-example-0003-anchor-bound-commitment
verification_mode: receipt_only
status: anchor_incomplete
horizon_base:
  kind: chain_derived
  chain_id: chain-example-operational
  chain_accepted_time: '2026-08-30T12:00:00Z'
witness_shortfall:
  configured:
  - w-example-op-0003
  - w-example-dur-0003
  present:
  - w-example-op-0003
  missing:
  - w-example-dur-0003
terminal_failure_distinguished: false
receipt_ref: rcp-example-0003
header_source_ref: hs-example-verifier-own-0001
evaluated_at: '2026-08-30T12:30:00Z'
record_version: 1
notes: 'the ordering-only witness proves the anchor and counts toward the configured set,
  and is NOT eligible as a horizon base — so with no eligible base the honest status is no_determination,
  a first-class answer rather than a gap: a claim the artifact cannot check is not a basis
  for a fail-closed answer'
incomplete_cause: undistinguished_receipt_only
