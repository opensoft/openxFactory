# expected_failure: proof-of-possession-missing
# expected_failure_detail: an unchecked signature is not a verified one
# requirement: OXW-R3
# VIOLATES core R3: a signature PRESENTED, never CHECKED, act permitted.
#
# The subtlest of the proof-of-possession probes, because the record looks
# complete: a signature is present, the algorithm and the signed material are
# named, the presenting key is recorded, and the event is filed as
# `authenticated`. What is missing is the verdict — no `verified` field at all.
#
# An earlier form of the rule tested `verified is False`, so an ABSENT verdict
# passed. That is presentation accepted as proof with extra ceremony, which is
# the whole of what this requirement forbids.
schema_version: 1
kind: xfactory_wallet_grant_exercise
exercise_id: exr-unverified-0009
grant_ref: grant-posting-derived-0002
act: post_transaction
object_ref: purchase-invoice:LXRP0021
occurred_at: "2026-08-07T12:50:00Z"
event_class: authenticated
proof_of_possession:
  presented: true
  signature_algorithm: ed25519
  signed_over: request
  presenting_key_ref: key-poster-0001
attribution:
  mode: key_attributed
  presenting_key_ref: key-poster-0001
  wallet_ref: wal-agent-poster-0001
  holder_ref: agent:ledger-poster
custody_model_in_force: isolated_per_use_authorized
revocation_check:
  performed_at_exercise: true
  result: active
constraint_evaluations:
  - constraint_ref: dhc-posting-sod-0001
    prior_act_holder_ref: agent:ledger-creator
    this_holder_ref: agent:ledger-poster
    satisfied: true
outcome: permitted
