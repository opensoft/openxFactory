# expected_failure: custody-model-unknown
# expected_failure_detail: not a member of the closed registry
# requirement: OXW-R4
# VIOLATES core R4: a wallet declaring a custody model that does not exist.
#
# Custody is declared FROM A CLOSED SET. This record names one that reads
# reassuringly and is in no registry, so every ceiling computed from it
# resolves to nothing — the cap failing open at the exact point it is supposed
# to bind. The closed-set rule was previously enforced only where an EXERCISE
# reported a model in force, which left the declaration itself unchecked.
schema_version: 1
kind: xfactory_wallet_record
wallet_id: wal-agent-unknown-custody-0009
holder:
  holder_id: agent:ledger-optimist
  holder_class: agent
key_reference:
  did: "did:web:xforge.us:wallets:agent-optimist"
  key_id: key-optimist-0009
  signature_algorithm: ed25519
custody:
  model: hardware_hsm_fully_trusted
  registry_version: 1
state: active
