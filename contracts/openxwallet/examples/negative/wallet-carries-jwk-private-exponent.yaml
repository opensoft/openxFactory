# expected_failure: key-material-embedded
# expected_failure_detail: JWK carrying
# requirement: OXW-R1
# VIOLATES core R1 as STRUCTURED DATA rather than as armored text.
#
# The sibling probe pastes a PEM block into a string. This one is the form key
# material most plausibly arrives in when a record is assembled by code rather
# than by hand: a JWK, where the private exponent hides behind the single
# letter `d`.
#
# `d` is far too generic a property name to blocklist on its own — plenty of
# innocent records could carry one — but alongside `kty` it is unambiguous,
# which is why the check keys on the pair rather than on the letter.
schema_version: 1
kind: xfactory_wallet_record
wallet_id: wal-agent-jwk-0009
holder:
  holder_id: agent:ledger-jwk
  holder_class: agent
key_reference:
  did: "did:web:xforge.us:wallets:agent-jwk"
  key_id: key-jwk-0009
  signature_algorithm: ed25519
custody:
  model: holder_readable
  registry_version: 1
  declared_by: '{"kty":"OKP","crv":"Ed25519","x":"11qYAYKxCrfVS_7TyWQHOg7hcvPapiMlrwIaaPcHURo","d":"nWGxne_9WmC6hEr0kuwsxERJxWl7MmkZcDusAxyuf2A"}'
state: active
