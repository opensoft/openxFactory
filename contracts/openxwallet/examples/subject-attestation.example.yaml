# VALID subject attestation: a subject carrying an OPTIONAL wallet reference.
# The reference is attestation, never identity. Resolution keys on the
# subject's own identifier, and the record declares it stays reconstructable
# with no wallet available — preserving MedxFactory's two ratified constraints
# as a core rule rather than working around them.
schema_version: 1
kind: xfactory_wallet_subject_attestation
subject_ref: subject:patient-04417
wallet_ref: wal-practitioner-0003
reference_role: attestation
resolution:
  resolved_by: subject_ref
  reconstructable_without_wallet: true
attested_at: "2026-08-07T10:00:00Z"
