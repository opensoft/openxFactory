# expected_failure: sentinel-floor
# A sentinel policy with a zero floor — drift detection, corpus freshness,
# and counterfactuals all die at zero; the floor is strictly positive.
schema_version: 1
kind: sentinel_policy
capability_ref: "cap:opensoft:neg"
epsilon:
  current: 0.0
  floor: 0.0
  schedule: "decayed-to-nothing"
mode: dual-run
expense_ref: "budget:opensoft:exploration"
