# VALID: one approved model that DECLARES the input modalities it accepts
# (contract-v2.2). The set is non-empty, carries no repeat, and contains
# `text` — a chat turn always carries text, so a model that could not accept
# it would not be routable here at all.
#
# This is the POSITIVE half of the closed vocabulary. Its negatives are
# `negative/workbench-model-catalog-modality-outside-the-vocabulary`,
# `negative/workbench-model-catalog-modality-image-only` and
# `negative/workbench-model-catalog-modality-empty-set`. The ABSENCE case — an
# entry that declares nothing and stays valid — is
# `workbench-model-catalog-local.example.yaml`.
{
  "schema_version": 1,
  "kind": "workbench-model-catalog",
  "models": [
    {
      "model_id": "local-vision-1",
      "label": "Approved authoring model (accepts images)",
      "provider_class": "on-tenant",
      "available": true,
      "input_limit_bytes": 800000,
      "output_limit_bytes": 900000,
      "data_handling": "Processed in the approved tenant boundary; no retention.",
      "modalities": ["text", "image"]
    }
  ]
}
