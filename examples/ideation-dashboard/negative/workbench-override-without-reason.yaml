# INVALID workbench manifest — the named case "override without reason rejected"
# (task 2.7). A member entered via `manual-include` but carries no `reason`. A
# human override is captured evidence, never a silent set edit; the schema's
# member allOf requires the reason.
schema_version: 1
kind: ideation-workbench
repository: openxFactory
name: Bad override
created: "2026-07-14T08:00:00Z"
updated: "2026-07-14T08:00:00Z"
members:
  - {document: doc-a, via: manual-include}
seed:
  kind: ad-hoc
