# INVALID widened success (contract-v1.39): a REDUCED posture with no reason.
# The ratified sentence is that the turn degrades "with the reduced posture
# STATED", and the reason IS the statement — without it the record announces
# that something was left out and refuses to say what or why, which is the
# silent degradation the requirement exists to prevent, now wearing a badge.
# Refused THREE times over, deliberately: by the released shape's
# `posture == reduced -> required: [reduced_reason]` conditional, by the
# delegated validator's `context-packet` pairing rule, and — before any of this
# could be built — by `ContextPacket.__post_init__`, which will not construct
# such a packet at all.
{
  "schema_version": 1,
  "kind": "workbench-chat-turn-v2-success",
  "client_turn_id": "turn-v2-0005",
  "assistant_turn_id": "srv-v2-0005",
  "model_id": "local-authoring-1",
  "selected_model": {
    "requested_model_id": "local-authoring-1",
    "routing_rule": false,
    "data_handling": "Local process only; no content leaves this machine."
  },
  "context_packet": {
    "posture": "reduced"
  },
  "bound_buffer": "ideation/staging/demo-topic/note.md",
  "observed_hashes": {
    "outline": "74d2e440df9974e0c1d82412165e497bb95611142b3ed1235c0bd74b3c2277ba",
    "ideation/staging/demo-topic/note.md": "9859eb7d779f1dee12579980045bb94ecbc2cc487d8160aa239bc1bfe3416825"
  },
  "assistant_prose": "An answer whose record cannot be trusted about its own context.",
  "proposals": []
}
