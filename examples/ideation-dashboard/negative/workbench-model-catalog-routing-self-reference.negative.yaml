# INVALID (routing-self-reference): THE ADVERSARIAL REVIEW'S OWN INSTANCE C,
# packaged. The rule names ITSELF in `routes_to` and resolves to itself. This
# was already refused, but only INCIDENTALLY — the chained-rule check reported
# "routes to something that is itself a routing rule", which is true and
# unhelpful. It is now its own refusal with its own name.
{
  "schema_version": 1,
  "kind": "workbench-model-catalog",
  "models": [
    {
      "model_id": "auto-self",
      "label": "Automatic",
      "provider_class": "routing-rule",
      "available": true,
      "input_limit_bytes": 8,
      "output_limit_bytes": 8,
      "data_handling": "self",
      "routing_rule": true,
      "routes_to": ["auto-self"],
      "resolved_model_id": "auto-self"
    }
  ]
}
