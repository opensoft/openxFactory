# INVALID (routing-badge): the ROUTED entry's own badge contains " / ", the
# routing badge's segment separator, so it could never be carried as ONE
# segment. Refused as ill-formed rather than silently split in half — the
# grammar's own collision, closed rather than hoped away.
{
  "schema_version": 1,
  "kind": "workbench-model-catalog",
  "models": [
    {
      "model_id": "auto-separator",
      "label": "Automatic",
      "provider_class": "routing-rule",
      "available": true,
      "input_limit_bytes": 2048,
      "output_limit_bytes": 8192,
      "data_handling": "Routes by role. / Read / write access to the tenant store.",
      "routing_rule": true,
      "routes_to": ["separator-in-badge"],
      "resolved_model_id": "separator-in-badge"
    },
    {
      "model_id": "separator-in-badge",
      "label": "Model whose badge holds the separator",
      "provider_class": "on-tenant",
      "available": true,
      "input_limit_bytes": 2048,
      "output_limit_bytes": 8192,
      "data_handling": "Read / write access to the tenant store."
    }
  ]
}
