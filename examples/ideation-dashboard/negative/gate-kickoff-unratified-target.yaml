# INVALID gate-action record (context-dependent — D17 kickoff precondition). This
# record is SCHEMA-valid (a kickoff with a workflow-job artifact), but its target
# change `add-unratified-experiment` carries NO recorded ratification in the
# supplied context, so the console/validator must refuse the kickoff (validator
# rule kickoff-unratified). The negative self-test validates it WITH the snapshot
# example as ratification context (which does not list this change as ratified).
# Validated standalone with no context, the precondition is SKIPPED, not failed —
# that is the intended cross-instance behavior, so this file is only a "negative"
# when context is present.
schema_version: 1
kind: gate-action-record
actor: brett
action: kickoff
target:
  change_id: add-unratified-experiment
at: "2026-07-14T12:00:00Z"
artifacts:
  - kind: workflow-job
    reference: workflow-jobs/xf-realization-add-unratified-experiment-0001
