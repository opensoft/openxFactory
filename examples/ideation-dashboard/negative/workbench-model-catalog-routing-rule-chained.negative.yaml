# INVALID (routing-target): a rule routing to another RULE. `resolved_model_id`
# is recorded as the model that ANSWERED the turn, so it must name something
# that answers rather than a second indirection — the inner rule is a lawful
# rule on its own, and it is still not a lawful DESTINATION.
{
  "schema_version": 1,
  "kind": "workbench-model-catalog",
  "models": [
    {
      "model_id": "auto-outer",
      "label": "Automatic (outer)",
      "provider_class": "routing-rule",
      "available": true,
      "input_limit_bytes": 2048,
      "output_limit_bytes": 8192,
      "data_handling": "Routes by role to any of: Routes by role to any of: Processed in the approved tenant boundary; no retention.",
      "routing_rule": true,
      "routes_to": ["auto-inner"],
      "resolved_model_id": "auto-inner"
    },
    {
      "model_id": "auto-inner",
      "label": "Automatic (inner)",
      "provider_class": "routing-rule",
      "available": true,
      "input_limit_bytes": 2048,
      "output_limit_bytes": 8192,
      "data_handling": "Routes by role to any of: Processed in the approved tenant boundary; no retention.",
      "routing_rule": true,
      "routes_to": ["chained-on-tenant"],
      "resolved_model_id": "chained-on-tenant"
    },
    {
      "model_id": "chained-on-tenant",
      "label": "Approved authoring model (routable)",
      "provider_class": "on-tenant",
      "available": true,
      "input_limit_bytes": 800000,
      "output_limit_bytes": 900000,
      "data_handling": "Processed in the approved tenant boundary; no retention."
    }
  ]
}
