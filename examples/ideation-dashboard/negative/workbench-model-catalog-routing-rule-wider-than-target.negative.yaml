# INVALID (routing-limit): the rule declares an input budget above that of a
# model it may route to. The effective turn limit is computed from the SELECTED
# entry — for a routed turn, the rule — so this shape admits a turn the model it
# routes to cannot take.
{
  "schema_version": 1,
  "kind": "workbench-model-catalog",
  "models": [
    {
      "model_id": "auto-too-wide",
      "label": "Automatic (routes by role)",
      "provider_class": "routing-rule",
      "available": true,
      "input_limit_bytes": 800000,
      "output_limit_bytes": 8192,
      "data_handling": "Routes by role. / Zero retention; content leaves the tenant boundary for inference only.",
      "routing_rule": true,
      "routes_to": ["too-wide-hosted"],
      "resolved_model_id": "too-wide-hosted"
    },
    {
      "model_id": "too-wide-hosted",
      "label": "Hosted zero-retention model (routable)",
      "provider_class": "hosted-zero-retention",
      "available": true,
      "input_limit_bytes": 2048,
      "output_limit_bytes": 8192,
      "data_handling": "Zero retention; content leaves the tenant boundary for inference only."
    }
  ]
}
