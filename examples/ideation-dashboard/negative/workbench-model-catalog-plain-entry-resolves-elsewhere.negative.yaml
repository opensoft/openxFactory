# INVALID (structural): a PLAIN entry — one that declares no routing rule —
# carrying a resolved-model field. Refused by the released shape itself
# (`dependentRequired`), not by a delegated rule: a directly answering model
# that resolves to something else is the hidden routing decision the ratified
# requirement forbids.
{
  "schema_version": 1,
  "kind": "workbench-model-catalog",
  "models": [
    {
      "model_id": "plain-that-resolves",
      "label": "Approved authoring model",
      "provider_class": "on-tenant",
      "available": true,
      "input_limit_bytes": 800000,
      "output_limit_bytes": 900000,
      "data_handling": "Processed in the approved tenant boundary; no retention.",
      "resolved_model_id": "somewhere-else"
    }
  ]
}
