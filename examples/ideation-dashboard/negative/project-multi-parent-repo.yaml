# INVALID project-register: repository `openxFactory` is listed under two
# projects (p-a and p-b). Violates the single-parent D10 hierarchy (validator
# rule project-multi-parent-repo) — the snapshot carries a singular `project`
# field per repository, so multi-parent membership is ambiguous.
schema_version: 1
kind: project-register
projects:
  - id: p-a
    repositories: [openxFactory]
  - id: p-b
    repositories: [openxFactory]
