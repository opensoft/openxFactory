# INVALID gate-action record: action=abandon-session with no `reason`. Violated
# rule: abandon-session requires target.ref AND reason
# (add-workbench-branch-sessions D9/D13). Layer: SCHEMA. The unreasoned-demotion
# rule applied to the session's other ending: a merge preserved the work and a
# merge commit says so, while an abandon preserved nothing — the reason is the
# ONE durable artifact this verb exists to leave behind, and the record is
# main-resident precisely so it outlives the branch a later cleanup may delete.
schema_version: 1
kind: gate-action-record
actor: brett
action: abandon-session
target:
  ref: draft/workbench-branch-sessions
at: "2026-07-26T16:20:00Z"
artifacts:
  - kind: other
    reference: draft/workbench-branch-sessions
