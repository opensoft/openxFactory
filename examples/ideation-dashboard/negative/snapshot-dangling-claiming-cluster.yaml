# INVALID snapshot: possible pos-a's claiming_clusters references cluster id
# `cl-missing`, which is not in `clusters[]`. Violates referential integrity
# (validator rule snapshot-dangling-cluster-ref).
schema_version: 1
kind: ideation-dashboard-snapshot
repository: openxFactory
generation: {source_revision: deadbeef}
documents:
  - {id: doc-a, path: ideation/brainstorm/a.md, stage: brainstorm}
clusters:
  - id: cl-a
    name: Cluster A
    topics: [t]
    document_edges:
      - {document: doc-a, matched_topics: [t]}
possibles:
  - id: pos-a
    title: A possible
    claim: Something the topic could become.
    state: latent
    claiming_clusters: [cl-missing]
staged_topics: []
changes: []
keyword_index: []
