# INVALID gate-action record (contract-v1.45): an `approve-model` action that
# names the declaration it approved and answers NONE of the questions an
# approval exists to answer — who put the credential into custody, who approved
# it for this console, when the approval stops being good, and which audit act
# it corresponds to.
#
# This is the exact record the change refused to allow. Approval is the second
# of two decisions, and a second decision whose accountability is empty is the
# first decision wearing a different name: `credential-contracts` holds that a
# grant lacking `issued_by`, `approved_by`, `expires_at` or `audit_ref` is
# invalid, and a model reached through a credential broker is answerable in
# exactly those terms.
schema_version: 1
kind: gate-action-record
actor: brett
action: approve-model
target:
  model_declaration: authoring-model
at: "2026-08-26T18:40:00Z"
artifacts:
  - kind: other
    reference: ideation/dashboard/model-declarations.yaml
