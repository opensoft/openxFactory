# INVALID widened success (contract-v1.40): the reduction reason LEAKS. It names
# a raw endpoint and a bearer credential, and it is STRUCTURALLY well formed —
# `reduced_reason` is free prose bounded at `maxLength: 500` (code points, not bytes), so the shape accepts it and
# only the delegated validator can refuse it. That is the point of packaging it:
# it is the ONE rule this release delegates because the shape cannot express it,
# and without an instance the rule would be unguarded prose.
#
# The class is the same one the FAILURE lane has always guarded — a redacted
# surface leaking through an ALLOWED field rather than through an extra one — and
# it is judged by the same scan. Realistic, too: a reduction reason describes why
# a retrieval failed, which is exactly the sentence a careless producer would fill
# with the provider's own error text.
{
  "schema_version": 1,
  "kind": "workbench-chat-turn-v2-success",
  "client_turn_id": "turn-v2-0009",
  "assistant_turn_id": "srv-v2-0009",
  "model_id": "local-authoring-1",
  "selected_model": {
    "requested_model_id": "local-authoring-1",
    "routing_rule": false,
    "data_handling": "Local process only; no content leaves this machine."
  },
  "context_packet": {
    "posture": "reduced",
    "reduced_reason": "the retrieval endpoint https://index.internal.example/v1 refused this call (authorization: Bearer sk-abcdef123456), so this packet carries the selected thread and the loaded buffers only"
  },
  "bound_buffer": "ideation/staging/demo-topic/note.md",
  "observed_hashes": {
    "outline": "74d2e440df9974e0c1d82412165e497bb95611142b3ed1235c0bd74b3c2277ba",
    "ideation/staging/demo-topic/note.md": "9859eb7d779f1dee12579980045bb94ecbc2cc487d8160aa239bc1bfe3416825"
  },
  "assistant_prose": "An answer whose record leaks the install's retrieval endpoint and its credential.",
  "proposals": []
}
