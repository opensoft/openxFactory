# INVALID (modality-empty-set): the entry declares an EMPTY modality set.
# Declaring nothing and declaring the empty set are different acts and this
# schema keeps them different: omitting the key entirely is valid and carries no
# claim in either direction, while `modalities: []` is a producer stating a
# capability set it then fails to state, which `minItems: 1` refuses.
{
  "schema_version": 1,
  "kind": "workbench-model-catalog",
  "models": [
    {
      "model_id": "local-empty-set-1",
      "label": "Approved authoring model",
      "provider_class": "on-tenant",
      "available": true,
      "input_limit_bytes": 800000,
      "output_limit_bytes": 900000,
      "data_handling": "Processed in the approved tenant boundary; no retention.",
      "modalities": []
    }
  ]
}
