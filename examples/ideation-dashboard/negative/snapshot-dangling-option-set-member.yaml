# INVALID snapshot: possible pos-a's option_set names sibling `pos-missing`,
# which is not a possible in `possibles[]`. Violates referential integrity
# (validator rule snapshot-dangling-optionset-member).
schema_version: 1
kind: ideation-dashboard-snapshot
repository: openxFactory
generation: {source_revision: deadbeef}
documents:
  - {id: doc-a, path: ideation/brainstorm/a.md, stage: brainstorm}
clusters:
  - id: cl-a
    name: Cluster A
    topics: [t]
    document_edges:
      - {document: doc-a, matched_topics: [t]}
possibles:
  - id: pos-a
    title: A possible
    claim: Something the topic could become.
    state: latent
    claiming_clusters: [cl-a]
    option_set: {id: os-a, members: [pos-a, pos-missing]}
staged_topics: []
changes: []
keyword_index: []
