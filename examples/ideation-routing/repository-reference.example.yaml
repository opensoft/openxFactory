# Valid structured repository references: each list item validates independently
# against contracts/schemas/xfactory-idea-routing-reference.schema.yaml
# `#/$defs/committed_reference` (add-cross-factory-ideation-routing task 2.2).
#
# This is a reusable $defs kernel embedded by the routing-record, routing-index,
# and organizer schemas (never a standalone top-level record), so this file
# carries no schema_version/kind envelope of its own. `committed_reference` is
# also the exact object serialized (as a compact JSON array element) into a
# Markdown destination's `Routing records:` provenance header (spec "Organize
# gate and lightweight destination provenance"). The `references:` wrapper key
# is a container for this file only, not part of the schema. Repository IDs are
# the reserved root `xFactory` or exact aggregation-relative `.gitmodules` paths;
# revisions are FULL committed git object names (40-hex SHA-1 or 64-hex SHA-256).
references:
  # openxFactory canonical routing record, SHA-1 revision.
  - repository: openxFactory
    path: ideation/brainstorm/cross-domain/XFI-2026-001/routing.yaml
    revision: a1b2c3d4e5f6a7b8c9d0e1f2a3b4c5d6e7f8a9b0
  # A pinned domain submodule source, SHA-1 revision.
  - repository: xFactories/MedxFactory
    path: ideation/brainstorm/omni-clinic-hosting.md
    revision: 7fbf65c5250e7a866da552761ed5d02fa457c042
  # A pinned install/runtime repository target, SHA-1 revision.
  - repository: installs/cloudpc-install
    path: planning/host-recovery-change.md
    revision: 3936c6dff1417735d56ce3973cfe6c29a09c7541
  # The reserved aggregation root, SHA-256 (64-hex) revision.
  - repository: xFactory
    path: .gitmodules
    revision: 2af1a250e456f7954d4c4dc0e86902336984572c967fd2237f82729d0ee5fce9
