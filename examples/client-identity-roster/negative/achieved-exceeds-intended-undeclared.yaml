# INVALID roster fragment — violates FR-010 (rule "achieved authority
# exceeding intended without a declared excess"): the entry INTENDS `observe`
# while its granted permission DECLARES `achieves: mutate`, and it carries no
# `declared_excess`. Provider-forced breadth is conformant once its provider
# reason, bounding mechanism, gate obligation and enforcement test are
# declared — and a finding when it is not. Distinct from
# `achieved-class-contradicted-by-permissions.yaml`, which proves the
# different rule that an achieved class may not be ASSERTED against its own
# permissions; neither substitutes for the other (finding
# achieved-exceeds-intended-undeclared).
schema_version: 1
kind: xfactory_client_identity_roster
client_ref: demo-client
client_tenant: 11111111-1111-1111-1111-111111111111
domain: demoxfactory
legend:
  blast_radius_units:
    unit_a: Unit A
  duties:
    observing: Read-only observation
entries:
- identity_ref: demo-bc-writer
  identity_kind: entra_app_registration
  home_tenant: 11111111-1111-1111-1111-111111111111
  principal_locations:
  - 11111111-1111-1111-1111-111111111111
  residency_model: client_tenant_single
  admission_surface: business_central
  duty: observing
  blast_radius_unit: unit_a
  authority_class_intended: observe
  authority_class_achieved: mutate
  granted_permissions:
  - id: Demo.ReadWrite.All
    achieves: mutate
    reaches:
    - business_central
  admission:
  - surface: business_central
    act: per-environment application user created
    achieved_scope: Unit A environment
    enforcement_mode: provider_enforced
    evidence_ref:
      repo: opensoft/DemoxFactory
      path: tenants/demo-admission-evidence-v1.yaml
    verified_at: '2026-08-01T00:00:00Z'
    exceeds_governed_unit: false
  per_unit_principal_available:
    business_central: true
  lifecycle_state: enrolled
  standing_credential_attestation:
    no_standing_credential: true
    attested_at: '2026-08-01T00:00:00Z'
  ratified_by: demoxfactory:demo-mutation-capability
  consent_ref: demo-consent-v1
