# INVALID roster fragment — violates FR-002/FR-010: a VERIFIED admission act
# declares `exceeds_governed_unit: true` — its achieved scope is tenant-wide
# with no scope selector — while the entry declares NO `declared_excess`. This
# is the change's own motivating measurement: a bound the provider enforced
# has been converted into one only gate logic enforces, and that must be a
# declared, CHECKABLE, tested fact. Its discrimination partner is the packaged
# Business Central worked case, which is the same shape with the excess
# DECLARED and validates with zero findings (finding undeclared-scope-excess).
schema_version: 1
kind: xfactory_client_identity_roster
client_ref: demo-client
client_tenant: 11111111-1111-1111-1111-111111111111
domain: demoxfactory
legend:
  blast_radius_units:
    unit_a: Unit A
  duties:
    observing: Read-only observation
entries:
- identity_ref: demo-bc-identity
  identity_kind: entra_app_registration
  home_tenant: 11111111-1111-1111-1111-111111111111
  principal_locations:
  - 11111111-1111-1111-1111-111111111111
  residency_model: client_tenant_single
  admission_surface: business_central
  duty: observing
  blast_radius_unit: unit_a
  authority_class_intended: observe
  authority_class_achieved: observe
  granted_permissions:
  - id: Demo.Read.All
    achieves: observe
    reaches:
    - business_central
  admission:
  - surface: business_central
    act: admin-center application authorization registered
    achieved_scope: tenant-wide, every environment (no scope selector)
    enforcement_mode: logic_enforced
    evidence_ref:
      repo: opensoft/DemoxFactory
      path: tenants/demo-admission-evidence-v1.yaml
    verified_at: '2026-08-01T00:00:00Z'
    exceeds_governed_unit: true
  per_unit_principal_available:
    business_central: false
  lifecycle_state: enrolled
  standing_credential_attestation:
    no_standing_credential: true
    attested_at: '2026-08-01T00:00:00Z'
  ratified_by: demoxfactory:demo-observation-capability
  consent_ref: demo-consent-v1
