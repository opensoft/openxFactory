# INVALID roster fragment — violates FR-004: the entry asserts
# `authority_class_achieved: observe` while a `granted_permissions[]` member
# DECLARES `achieves: mutate`. The contradiction is between two DECLARATIONS
# in the same record, which is the only way this is checkable: a network-free
# validator cannot resolve an identifier against a provider catalogue and MUST
# NOT infer provider semantics from how an identifier is spelled — so this
# file is authored around the declared class and never around a permission's
# NAME. It homes a different rule from
# `achieved-exceeds-intended-undeclared.yaml`: that one proves
# achieved-above-intended must be DECLARED, this one proves an achieved class
# cannot be ASSERTED against its own permissions (finding
# achieved-class-contradicts-permissions).
schema_version: 1
kind: xfactory_client_identity_roster
client_ref: demo-client
client_tenant: 11111111-1111-1111-1111-111111111111
domain: demoxfactory
legend:
  blast_radius_units:
    unit_a: Unit A
  duties:
    observing: Read-only observation
entries:
- identity_ref: demo-bc-identity
  identity_kind: entra_app_registration
  home_tenant: 11111111-1111-1111-1111-111111111111
  principal_locations:
  - 11111111-1111-1111-1111-111111111111
  residency_model: client_tenant_single
  admission_surface: business_central
  duty: observing
  blast_radius_unit: unit_a
  authority_class_intended: observe
  authority_class_achieved: observe
  granted_permissions:
  - id: Demo.ReadWrite.All
    achieves: mutate
    reaches:
    - business_central
  admission:
  - surface: business_central
    act: per-environment application user created
    achieved_scope: Unit A environment
    enforcement_mode: provider_enforced
    evidence_ref:
      repo: opensoft/DemoxFactory
      path: tenants/demo-admission-evidence-v1.yaml
    verified_at: '2026-08-01T00:00:00Z'
    exceeds_governed_unit: false
  per_unit_principal_available:
    business_central: true
  lifecycle_state: enrolled
  standing_credential_attestation:
    no_standing_credential: true
    attested_at: '2026-08-01T00:00:00Z'
  ratified_by: demoxfactory:demo-observation-capability
  consent_ref: demo-consent-v1
