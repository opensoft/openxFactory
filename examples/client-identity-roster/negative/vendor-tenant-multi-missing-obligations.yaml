# INVALID roster fragment — violates FR-012: `residency_model` is
# `vendor_tenant_multi` and the governed model's FIVE obligations are absent
# entirely — no tenant allow-list enforced at token validation, no per-client
# authorization state, no per-client revocation evidence, no cross-client
# credential-span statement, no per-client consent amendment. Client-resident
# single-tenant is the default for EVERY authority class; the multi-tenant
# model is admissible only carrying its full obligations, and there is no
# class-dependent relaxation anywhere (finding
# vendor-tenant-multi-missing-obligations).
schema_version: 1
kind: xfactory_client_identity_roster
client_ref: demo-client
client_tenant: 11111111-1111-1111-1111-111111111111
domain: demoxfactory
legend:
  blast_radius_units:
    unit_a: Unit A
  duties:
    observing: Read-only observation
entries:
- identity_ref: demo-bc-identity
  identity_kind: entra_app_registration
  home_tenant: 22222222-2222-2222-2222-222222222222
  principal_locations:
  - 22222222-2222-2222-2222-222222222222
  - 11111111-1111-1111-1111-111111111111
  residency_model: vendor_tenant_multi
  admission_surface: business_central
  duty: observing
  blast_radius_unit: unit_a
  authority_class_intended: observe
  authority_class_achieved: observe
  granted_permissions:
  - id: Demo.Read.All
    achieves: observe
    reaches:
    - business_central
  admission:
  - surface: business_central
    act: per-environment application user created
    achieved_scope: Unit A environment
    enforcement_mode: provider_enforced
    evidence_ref:
      repo: opensoft/DemoxFactory
      path: tenants/demo-admission-evidence-v1.yaml
    verified_at: '2026-08-01T00:00:00Z'
    exceeds_governed_unit: false
  per_unit_principal_available:
    business_central: true
  lifecycle_state: enrolled
  standing_credential_attestation:
    no_standing_credential: true
    attested_at: '2026-08-01T00:00:00Z'
  ratified_by: demoxfactory:demo-observation-capability
  consent_ref: demo-consent-v1
