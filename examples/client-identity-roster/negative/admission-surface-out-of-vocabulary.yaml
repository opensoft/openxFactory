# INVALID roster fragment — violates FR-007 (rule "out-of-vocabulary admission
# surface"): the entry declares `sharepoint`, a surface outside the closed
# first-release vocabulary of exactly `business_central` and `exchange`. The
# refusal names the closed set AND the extension route: a surface enters with
# the promotion of the capability that governs it, and non-Entra providers are
# a named successor routed by client-infrastructure-liaison. An identity on a
# surface outside this vocabulary is OUT of roster scope and is never a
# completeness finding (finding admission-surface-out-of-vocabulary).
schema_version: 1
kind: xfactory_client_identity_roster
client_ref: demo-client
client_tenant: 11111111-1111-1111-1111-111111111111
domain: demoxfactory
legend:
  blast_radius_units:
    unit_a: Unit A
  duties:
    observing: Read-only observation
entries:
- identity_ref: demo-bc-identity
  identity_kind: entra_app_registration
  home_tenant: 11111111-1111-1111-1111-111111111111
  principal_locations:
  - 11111111-1111-1111-1111-111111111111
  residency_model: client_tenant_single
  admission_surface: sharepoint
  duty: observing
  blast_radius_unit: unit_a
  authority_class_intended: observe
  authority_class_achieved: observe
  granted_permissions:
  - id: Demo.Read.All
    achieves: observe
    reaches:
    - sharepoint
  admission:
  - surface: sharepoint
    act: per-environment application user created
    achieved_scope: Unit A environment
    enforcement_mode: provider_enforced
    evidence_ref:
      repo: opensoft/DemoxFactory
      path: tenants/demo-admission-evidence-v1.yaml
    verified_at: '2026-08-01T00:00:00Z'
    exceeds_governed_unit: false
  per_unit_principal_available:
    sharepoint: true
  lifecycle_state: enrolled
  standing_credential_attestation:
    no_standing_credential: true
    attested_at: '2026-08-01T00:00:00Z'
  ratified_by: demoxfactory:demo-observation-capability
  consent_ref: demo-consent-v1
