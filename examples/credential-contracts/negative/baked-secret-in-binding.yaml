# NEGATIVE (expect: baked-secret). The binding's secret_ref carries a raw
# secret value instead of a vault reference — the credential is baked into
# config. Violates reference-delivery (and the no-raw-secrets core rule).
schema_version: 1
kind: xfactory_credential_binding_template
client:
  id: opensoft
credential_bindings:
  intent_dispatch:
    provider: azure_key_vault
    vault: kv-opensoft-xfactory-qa
    secret_ref: "github_pat_11ABCDEF0123456789exampleRawTokenNotAReference"
    owner: opensoft-platform
    rotation_policy: operator_managed
