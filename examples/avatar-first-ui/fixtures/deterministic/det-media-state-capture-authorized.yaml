schema_version: 1
kind: xfactory_avatar_first_ui_fixture
fixture_id: det-media-state-capture-authorized
evidence_ids: [AFU-008-S01, AFU-004-S01]
# Provenance: adopted verbatim from codexFactory branch 002-avatar-client-lab @ 3a8fbd5
#   (7/7 panel-confirmed; evidence in specs/002-avatar-client-lab/upstream-drafts/STATUS.md).
# Deterministic kernel-media.state fixture (avatar-client-lab P12 upstream candidate).
# `capture_authorized` follows the authoritative `media_authorized` event: capture is authorized
# (the answer may be applied, provider-bound audio permitted) though not yet actively streaming.
# It is the kernel peer of the landed `offline-acceptance.yaml` presentation reach.
# NOTE: `offline-acceptance.yaml` carries this IDENTICAL two-event, zero-observation arc
# (`session_started` + `media_authorized`, both authoritative) yet sets `state_revision: 1`, not `2`
# — an open cross-corpus divergence on `state_revision`, not a settled convention. Because neither
# side has an observation event, this pair isolates the disagreement to the count/off-by-one question
# alone (the purest instance of the corpus `state_revision` contradiction); see README.md design
# decision 5 for the reconciliation ask. Fixed clock/IDs/
# font/locale/platform + canonical AVC inputs -> declared view-state/record shapes. No live model,
# provider, clock, or RNG. Two identical runs yield equivalent shapes and identical evidence_ids.
inputs:
  clock: "2026-07-11T00:01:10Z"
  ids:
    session_id: sess-0103
    workflow_id: wf-0103
    persona_id: neutral.guide
    persona_version: 1
  font: pinned-neutral-sans-with-rtl-face
  locale: en
  platform_capabilities:
    keyboard: true
    screen_reader: true
    reduced_motion: false
canonical:
  commands:
    - {command_type: start_session}
  events:
    - {event_type: session_started, authority: authoritative}   # permission granted
    - {event_type: media_authorized, authority: authoritative}  # capture authorized -> capture_authorized
  snapshot:
    logical_session_id: sess-0103
    last_event_sequence: 2
    state_revision: 2
    control_health: healthy
    media_state: capture_authorized   # KERNEL denominator evidence: closed media.state `capture_authorized`
    session_outcome: granted
    presentation_mode: conversation
expected:
  view_state:
    presentation_mode: conversation
    media_state: capture_authorized
    control_state: healthy
    workflow_projection: intake
  records:
    - {kind: session_started, session_id: sess-0103}
    - {kind: media_authorized, session_id: sess-0103}
