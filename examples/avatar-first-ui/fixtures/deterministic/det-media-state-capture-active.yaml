schema_version: 1
kind: xfactory_avatar_first_ui_fixture
fixture_id: det-media-state-capture-active
evidence_ids: [AFU-008-S01, AFU-004-S01]
# Provenance: adopted verbatim from codexFactory branch 002-avatar-client-lab @ 3a8fbd5
#   (7/7 panel-confirmed; evidence in specs/002-avatar-client-lab/upstream-drafts/STATUS.md).
# Deterministic kernel-media.state fixture (avatar-client-lab P12 upstream candidate).
# `capture_active` is authorized capture actively streaming (microphone capture in progress,
# AFU-004-S01). It is distinguished from `capture_authorized` (authorized-but-not-yet-streaming)
# ONLY by the kernel media_state value. Fixed clock/IDs/font/locale/platform + canonical AVC inputs
# -> declared view-state/record shapes. No live model, provider, clock, or RNG. Two identical runs
# yield equivalent shapes and identical evidence_ids.
inputs:
  clock: "2026-07-11T00:01:15Z"
  ids:
    session_id: sess-0104
    workflow_id: wf-0104
    persona_id: neutral.guide
    persona_version: 1
  font: pinned-neutral-sans-with-rtl-face
  locale: en
  platform_capabilities:
    keyboard: true
    screen_reader: true
    reduced_motion: false
canonical:
  commands:
    - {command_type: start_session}
  events:
    - {event_type: session_started, authority: authoritative}     # permission granted
    - {event_type: media_authorized, authority: authoritative}    # capture authorized
    - {event_type: lifecycle_transition, authority: authoritative} # capture streaming -> capture_active
  snapshot:
    logical_session_id: sess-0104
    last_event_sequence: 3
    state_revision: 3
    control_health: healthy
    media_state: capture_active       # KERNEL denominator evidence: closed media.state `capture_active`
    session_outcome: granted
    presentation_mode: conversation
expected:
  view_state:
    presentation_mode: conversation
    media_state: capture_active
    control_state: healthy
    workflow_projection: intake
    capture_active: true
  records:
    - {kind: session_started, session_id: sess-0104}
    - {kind: media_authorized, session_id: sess-0104}
    - {kind: capture_active, session_id: sess-0104}
