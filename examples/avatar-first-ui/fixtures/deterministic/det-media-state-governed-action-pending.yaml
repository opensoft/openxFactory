schema_version: 1
kind: xfactory_avatar_first_ui_fixture
fixture_id: det-media-state-governed-action-pending
evidence_ids: [AFU-008-S01, AFU-001-S02]
# Provenance: adopted verbatim from codexFactory branch 002-avatar-client-lab @ 3a8fbd5
#   (7/7 panel-confirmed; evidence in specs/002-avatar-client-lab/upstream-drafts/STATUS.md).
# Deterministic kernel-media.state fixture (avatar-client-lab P12 upstream candidate). A
# consequential workflow value is submitted and accepted, proposing a governed action that awaits
# an AVC-06 structured confirmation before any governed write (AFU-001-S02, consequential values
# require review). The action is PENDING, not resolved: no session_outcome terminal, control healthy,
# governed commands enabled so the user may approve. The client renders only canonical intent/approval
# state and MUST NOT execute or imply execution (spec "Client re-derives authority and never decides
# it"). Fixed clock/IDs/font/locale/platform + canonical AVC inputs -> declared shapes. No live model,
# provider, clock, or RNG.
inputs:
  clock: "2026-07-11T00:01:30Z"
  ids:
    session_id: sess-0107
    workflow_id: wf-0107
    persona_id: neutral.guide
    persona_version: 1
  font: pinned-neutral-sans-with-rtl-face
  locale: en
  platform_capabilities:
    keyboard: true
    screen_reader: true
    reduced_motion: false
canonical:
  commands:
    - {command_type: start_session}
    - {command_type: submit_workflow_value}
  events:
    - {event_type: session_started, authority: authoritative}   # permission granted
    - {event_type: media_authorized, authority: authoritative}  # capture authorized
    - {event_type: command_accepted, authority: authoritative}  # consequential value accepted; governed action proposed, awaiting AVC-06 confirmation
  snapshot:
    logical_session_id: sess-0107
    last_event_sequence: 3
    state_revision: 3
    control_health: healthy
    media_state: governed_action_pending  # KERNEL denominator evidence: closed media.state `governed_action_pending`
    session_outcome: granted
    presentation_mode: conversation
expected:
  view_state:
    presentation_mode: conversation
    media_state: governed_action_pending
    control_state: healthy
    workflow_projection: intake
    governed_commands_enabled: true
    pending_confirmation: true          # AVC-06 confirmation awaiting decision; no governed write yet
  records:
    - {kind: session_started, session_id: sess-0107}
    - {kind: media_authorized, session_id: sess-0107}
    - {kind: governed_action_proposed, session_id: sess-0107}
