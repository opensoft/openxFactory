# name-order Specification Delta

## MODIFIED Requirements

### Requirement: Ordering is by declaration and never by name
The base rule holds.

The earlier writer adds this sentence, and whoever writes later must carry it.

#### Scenario: The base case runs
- **WHEN** the base case runs
- **THEN** it MUST hold
