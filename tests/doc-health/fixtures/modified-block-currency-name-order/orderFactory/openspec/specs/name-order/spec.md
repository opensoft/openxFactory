# name-order Specification

## Purpose
SYNTHESIZED. One requirement written by two active ratified changes, where the
DECLARATION points against change-id name order.

## Requirements

### Requirement: Ordering is by declaration and never by name
The base rule holds.

#### Scenario: The base case runs
- **WHEN** the base case runs
- **THEN** it MUST hold
