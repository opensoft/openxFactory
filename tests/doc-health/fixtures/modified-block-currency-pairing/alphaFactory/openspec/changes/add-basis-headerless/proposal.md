# Proposal: add-basis-headerless

NO lifecycle header at all, so the family's own `_standing` reader resolves
nothing. Treated as NOT ratified, deliberately: the whole point of the
disclosure is to warn about text no authority has been shown to accept, and a
standing nobody can read is no such showing. SYNTHESIZED.
