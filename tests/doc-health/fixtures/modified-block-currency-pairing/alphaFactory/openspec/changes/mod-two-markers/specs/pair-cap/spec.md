# pair-cap Specification Delta

## MODIFIED Requirements

### Requirement: Two markers
**Modified over `add-basis-ratified`'s addition by mod-two-markers (2026-09-01):** — the true declaration, and it is the FIRST paragraph on purpose.

**Modified over `mod-undeclared`'s addition by mod-two-markers (2026-09-01):** — a second basis for one text, naming a change that adds nothing.

The two-markers body holds, restated.

#### Scenario: The two-markers case
- **WHEN** the two-markers case runs
