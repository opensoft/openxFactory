# pair-cap Specification

## Purpose
The promoted side of the pairing tree, deliberately SMALL.

Canon carries exactly ONE requirement, and it is there for the own-rename carve
alone. Every other title this tree names is one canon does NOT carry, because a
title canon carries resolves `canon` and never reaches the pairing check at all.

## Requirements

### Requirement: Promoted old name
The old body holds. A second clause holds too.

#### Scenario: The old case
- **WHEN** the old case runs
- **THEN** it MUST hold
