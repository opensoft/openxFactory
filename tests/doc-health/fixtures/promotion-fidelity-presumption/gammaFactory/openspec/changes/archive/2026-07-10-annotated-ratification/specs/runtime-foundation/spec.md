# runtime-foundation Specification Delta

## ADDED Requirements

### Requirement: Layer seeding
The runtime SHALL seed each layer from its declared source.

#### Scenario: A layer is seeded
- **WHEN** the runtime starts
- **THEN** each declared layer MUST be seeded
