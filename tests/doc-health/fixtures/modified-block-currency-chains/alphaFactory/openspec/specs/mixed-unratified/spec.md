# mixed-unratified Specification

## Purpose
One arrangement of the N-writer chain rule per capability, so the
arrangements cannot interfere with each other.

## Requirements

### Requirement: Shared requirement
Shared clause one. Shared clause two.

#### Scenario: The shared case
- **WHEN** the shared case runs
- **THEN** it MUST hold
