# marker-cases Specification

## Purpose
One requirement per marker case, so no two blocks write one requirement and the
two-writers arm stays out of the way.

## Requirements

### Requirement: Suppression names exactly two of three
Body one. Body two. Body three.

#### Scenario: Something happens
- **WHEN** something happens
- **THEN** it MUST be handled

### Requirement: A marker naming a carried unit
Kept one. Kept two.

#### Scenario: Something else happens
- **WHEN** something else happens
- **THEN** it MUST be handled

### Requirement: A genuine removal carries its bullets
The rule holds.

#### Scenario: Gone
- **WHEN** the gone case runs
- **THEN** it MUST do one thing
- **AND** it MUST do a second thing
- **AND** it MUST do a third thing

#### Scenario: Stays
- **WHEN** the staying case runs

### Requirement: A retitle relabelled as a removal
The rule holds here too.

#### Scenario: Old title
- **WHEN** the old case runs
- **THEN** it MUST do one thing
- **AND** it MUST do a second thing
- **AND** it MUST do a third thing

### Requirement: A merge destination is present
The merge rule holds.

#### Scenario: Merged source
- **WHEN** the source case runs

#### Scenario: Merge destination
- **WHEN** the destination case runs

### Requirement: A promoted marker is not a carriage unit
Kept body.

**Removed from canon by add-an-older-change (2026-01-01):** `a unit somebody removed long ago` — and no later block should have to restate this paragraph forever

#### Scenario: The promoted case
- **WHEN** the promoted case runs

### Requirement: A prose dated note declares nothing
Kept body. Dropped body.

**CORRECTED 2026-08-25 ON A RULING — this note names `Dropped body.` in backticks as the clause that was RESTORED, which is what every dated note in this corpus records.**

#### Scenario: The prose case
- **WHEN** the prose case runs

### Requirement: A survivor of a removed scenario is carried
The survivor rule holds.

#### Scenario: Removed with a survivor
- **WHEN** the survivor case runs
- **THEN** it MUST do one thing

#### Scenario: Kept beside it
- **WHEN** the kept case runs
