# Catalog Entry Schema

Status: draft
Kind: reference
Repository context: openxFactory

Describes the catalog entry schema shape for the fixture corpus. As
Markdown under contracts/ this is ordinary governed content (artifact
type governance_markdown, like every non-spec Markdown document); the
corpus has no separate schema artifact type — real YAML schemas are
non-Markdown and stay outside the governed corpus by contract.
