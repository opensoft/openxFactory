# pending-cap Specification Delta

## ADDED Requirements

### Requirement: A requirement only a sibling adds
The adder's clause one. The adder's clause two. The adder's clause three.

#### Scenario: The adder's case
- **WHEN** the adder's case runs
- **THEN** it MUST hold
