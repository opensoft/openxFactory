# void-cases Specification

## Purpose
One requirement per case for the two grounds `amend-marker-defect-reporting`
adds, so no two blocks write one requirement and the two-writers arm stays out
of the way.

## Requirements

### Requirement: A reason quotes a unit the block drops
Kept body. Dropped body.

#### Scenario: The quoting case
- **WHEN** the quoting case runs
- **THEN** it MUST do one thing

### Requirement: A marker names a ghost
Kept body.

#### Scenario: The ghost case
- **WHEN** the ghost case runs

### Requirement: A reason quotes prose that is no unit
Kept body. Another dropped body.

#### Scenario: The prose-quoting case
- **WHEN** the prose-quoting case runs

### Requirement: A reason quotes a unit the block carries
Kept body. A third dropped body.

#### Scenario: The carried case
- **WHEN** the carried case runs
- **THEN** it MUST do one thing

### Requirement: An inherited marker declares nothing about a later block
Kept body.

**Removed from canon by an-older-change (2026-01-01):** `a unit removed long ago` — and a later block restating this requirement carries this paragraph forever, the unit it names having left canon when this marker was promoted

#### Scenario: The inherited case
- **WHEN** the inherited case runs
