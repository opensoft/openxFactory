---
code_surface: none
target_release: none
Status: record
---

# Proposal: theta-record-bait

More bait for the twelve. `record` is a controlled value, so `status-validity`
is silent; the document is not `ratified`, `standard` or `superseded`, so the
other three readers are silent too. `record-immutability` would fire if it
read the scan set — the harness gives it a capture blob that differs from this
text — and it must not, because its "revert the content edit" remedy
contradicts the archived-record append discipline the campaign runs on.
