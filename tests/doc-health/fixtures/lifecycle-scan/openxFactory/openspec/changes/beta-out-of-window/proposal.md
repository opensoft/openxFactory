---
code_surface: none
target_release: none
---
# Proposal: beta-out-of-window

> Realization note line 1: the banner that pushes the lifecycle header
> Realization note line 2: the banner that pushes the lifecycle header
> Realization note line 3: the banner that pushes the lifecycle header
> Realization note line 4: the banner that pushes the lifecycle header
> Realization note line 5: the banner that pushes the lifecycle header
> Realization note line 6: the banner that pushes the lifecycle header
> Realization note line 7: the banner that pushes the lifecycle header
> Realization note line 8: the banner that pushes the lifecycle header
> Realization note line 9: the banner that pushes the lifecycle header
> Realization note line 10: the banner that pushes the lifecycle header
> Realization note line 11: the banner that pushes the lifecycle header
> Realization note line 12: the banner that pushes the lifecycle header
> Realization note line 13: the banner that pushes the lifecycle header
> Realization note line 14: the banner that pushes the lifecycle header
> Realization note line 15: the banner that pushes the lifecycle header
> Realization note line 16: the banner that pushes the lifecycle header
> Realization note line 17: the banner that pushes the lifecycle header
> Realization note line 18: the banner that pushes the lifecycle header
> Realization note line 19: the banner that pushes the lifecycle header
> Realization note line 20: the banner that pushes the lifecycle header
> Realization note line 21: the banner that pushes the lifecycle header
> Realization note line 22: the banner that pushes the lifecycle header
> Realization note line 23: the banner that pushes the lifecycle header
> Realization note line 24: the banner that pushes the lifecycle header
> Realization note line 25: the banner that pushes the lifecycle header
> Realization note line 26: the banner that pushes the lifecycle header
> Realization note line 27: the banner that pushes the lifecycle header
> Realization note line 28: the banner that pushes the lifecycle header
> Realization note line 29: the banner that pushes the lifecycle header
> Realization note line 30: the banner that pushes the lifecycle header
> Realization note line 31: the banner that pushes the lifecycle header
> Realization note line 32: the banner that pushes the lifecycle header
> Realization note line 33: the banner that pushes the lifecycle header

Status: ratified
Ratified: 2026-01-04 by Test Owner — a citation nothing reads, because the
status above it is already out of the window.

## Why

A proposal whose lifecycle header sits past the header window, so the header
reads as absent.
