---
code_surface: none
target_release: none
Status: brainstorm
---

# Proposal: eta-non-reader-bait

BAIT FOR THE TWELVE, and the reason this document exists. Its `Status:` is a
controlled taxonomy value, it is not `ratified`, not `standard` and not
`superseded`, so all FOUR readers of the scan set are silent on it. Two
NON-readers would be loud if they read the scan set: `location-conformance`
(a `brainstorm` document outside `ideation/brainstorm/`) and `tag-hygiene`
(a candidate marker naming a capability that does not exist).

Without a document like this, adding a thirteenth reader is a mutation that
changes nothing observable on the fixture and survives the suite.

<!-- xspec:candidate target=no-such-capability -->
Prose selected for conversion, naming a capability nothing resolves.
<!-- /xspec:candidate -->
