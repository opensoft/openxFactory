---
code_surface: none
target_release: none
Status: ratified
---

# Byte-exact snapshot of ANOTHER packet's proposal.md

Frozen evidence that happens to be NAMED `proposal.md`, which is exactly the
file the first ruled glob (`openspec/changes/**/proposal.md`) would otherwise
sweep in at this depth. It carries the alpha packet's own defect — a
`ratified` header with no citation in either sanctioned spelling — so that a
build which drops the `EVIDENCE_PARTS` filter reports it, and the mutation
dies behaviourally as well as on the membership list.
