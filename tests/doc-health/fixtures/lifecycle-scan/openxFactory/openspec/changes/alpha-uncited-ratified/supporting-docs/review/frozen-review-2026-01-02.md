Status: ratified

# Frozen copy of a review record, held as supporting evidence

The SECOND ruled glob (`openspec/changes/**/review/*.md`) matches a `review/`
directory at any depth, so a preserved copy of somebody else's review record
filed under `supporting-docs/` matches it too. Byte-exact evidence again, and
excluded for the same reason. Its uncited `ratified` header is the bait.
