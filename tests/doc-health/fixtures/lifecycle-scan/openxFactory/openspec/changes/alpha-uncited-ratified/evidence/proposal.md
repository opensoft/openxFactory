---
code_surface: none
target_release: none
Status: definitely not a taxonomy value
---

# Captured proposal text, held as run evidence

The second `EVIDENCE_PARTS` segment, under a different name from the first, so
that dropping either segment from the filter is caught. The free-form status
is the bait: a build that lets this file into the set reports a
`status-validity` ERROR against a frozen record.
