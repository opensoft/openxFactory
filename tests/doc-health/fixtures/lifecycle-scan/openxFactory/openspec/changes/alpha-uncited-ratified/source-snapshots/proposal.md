---
code_surface: none
target_release: none
Status: ratified
---

# Byte-exact snapshot filed directly under the packet

`source-snapshots/` sitting beside `proposal.md` rather than nested under
`supporting-docs/`. In today's corpus every `source-snapshots/` directory is
nested, so `supporting-docs` alone would exclude them all and the
`source-snapshots` segment would be unpinned — a rule clause nothing tests.
This probe is what makes that third segment load-bearing. Its uncited
`ratified` header is the bait.
