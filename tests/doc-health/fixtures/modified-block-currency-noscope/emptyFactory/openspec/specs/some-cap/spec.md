# some-cap Specification

## Purpose
A repository with promoted specs and NO `openspec/changes/` directory at all.
The family cannot run here, and canon's skip rule is "cannot run".

## Requirements

### Requirement: Something
Something holds.

#### Scenario: The case
- **WHEN** it runs
