#!/usr/bin/env python3
"""Fixture: a generic legacy tool the register ALREADY cites (DTN-002
evidence), so the pre-filter must skip it as register-cited even though
its signals would otherwise fire."""

if __name__ == "__main__":
    print("legacy checker")
