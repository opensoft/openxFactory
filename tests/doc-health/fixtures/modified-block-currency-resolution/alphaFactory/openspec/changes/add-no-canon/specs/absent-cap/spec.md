# absent-cap Specification Delta

## MODIFIED Requirements

### Requirement: A requirement in a capability with no promoted spec
There is no `openspec/specs/absent-cap/spec.md` in this tree at all, which is
a DIFFERENT code path from a spec that exists without the title.

#### Scenario: The absent-capability case
- **WHEN** the absent-capability case runs
