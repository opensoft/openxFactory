# modified-block-currency-collision

SYNTHESIZED. Built by `govern-sibling-added-modified-deltas` (Speckit F2) for
the added-over-canon collision class.

**THE UNSAFE ARCHIVE ORDER, RECONSTRUCTED.** A modifying change archived BEFORE
the sibling its block rested on, so the requirement entered canon from the
modifying block and nobody reviewed it as an addition. The archive act is
outside what any health run can undo; what a run CAN see is what the breach
leaves behind — the sibling, still active, still writing that title into canon.

| change | basis form | expected |
| --- | --- | --- |
| `add-the-basis` | `## ADDED Requirements` | REPORTED |
| `rename-the-basis` | `## RENAMED Requirements` `TO:` | REPORTED, in the same class and on the same terms |
| `lawful-rename` | `## RENAMED Requirements`, `FROM:` in canon and `TO:` not | SILENT — the negative control |

`openspec/changes/archive/2026-08-31-mod-archived-first/` is the change that
took the order. The family reads no archived path by construction, so it is read
by nothing here; it is present so the reconstruction is a story a reader can
follow rather than a canon that appeared from nowhere.

**THIS TREE CARRIES NO ACTIVE `## MODIFIED Requirements` BLOCK AT ALL**, and
that is deliberate: the collision class must run BEFORE the family's
`if not blocks: continue` guard, or the one repository most likely to hold this
evidence — one whose modifying packet has already archived — is the one the
guard hides.
